# WORKBOARD — 회사 트랙 (광고·마케팅 writer)

> 🏢 회사 트랙 할일·활성 트랙·결정 기록. 운영 규칙은 ./GUARDRAILS.md, 공통은 ../CLAUDE.md.

---

## 지금 상태 (한 줄)
**§research-1 활성.** objective 기반 리서치 리포트 파이프라인(8단계). **선분 1(objective→쿼리) 종결(Q-2)**,
**선분 3(수집→색인) — S4 계열 9차수 종결(08-10) · S5·S6 쿼리 동결(08-10) · R12·R13 정찰 종결(08-12).**
🔴 **다음 = 8단계 관통 1회 (R3-2 착수).** 선분 3을 여기서 **일시 정지**한다.
**S9 유료 수집 보류** — 지시서는 작성 완료(`CC_S9_20260812_collect.md`, untracked). 크롬 제거 대조용이며
**관통의 성립 조건이 아니다.** 관통은 현행 색인(local 302 / web 416 청크)으로 돈다.
🔴 **정지 사유** — ⑥판정·⑦루프가 08-06 이후 무접촉이다. 그 두 칸이 빈 채로 ③④를 다듬어 왔고,
**"선분 3이 병목"이라는 가정은 검증된 적이 없다.** 한 번 관통시키면 병목이 관찰로 드러난다.
⚠️ 전수 라벨 320(세션 C)은 **열지 않는다**(이름사전 미채택, R12·R13 확정 7).
⚠️ **선분 3 산출은 사장되지 않는다** — `catch CA`·`BT`·`BS`·`BZ` 는 **⑥판정이 무엇을 잡아야 하는지의
목록**이다. 판정 설계에 그대로 들어간다. 순서를 뒤집었을 뿐이다(판정기 전에 판정 대상을 다듬었다).
§ad-track-1은 **완료(E열 미충족, 미스매치 판정)**로 이동.

## 활성 트랙

🔴 **유료 수집 착수 전 필수 — `GATE_KEEP_SOURCES` 가 현재 꺼져 있다** (`catch CU`)
`.env:208 =0` · 토픽 프리셋 설정 0건. **꺼진 채 돌리면 실행은 성공하고 `*_filtered.json` 만
안 생긴다 — 비용만 나간다.** 점화 후 **로그로 실동작 확인**까지가 게이트.
상세 = `R11_QUERY_FREEZE.md §10` 서두 블록

🔴 **점화 수단 정정 (2026-08-12, `R12` 발견 A) — `--gatekeep` 플래그는 무효다.**
플래그가 쓰는 `os.environ` 값을 `reload_config()` 의 `load_dotenv(override=True)` 가 되돌리고,
`GATE_KEEP_SOURCES` 는 보호키 5개에 **미포함**이다. → 점화는 **ENV 주입**으로만 성립.
⚠️ 단 **주입 수단 자체는 아직 미확정**이다 — `.env` 는 `override=False` 라 *import 전* `os.environ`
선주입이 이길 가능성이 있고(프리셋에 그 키가 없다면), 이 성립 여부는 **S9 Phase 1 §1 검증 대상**이다.
성립하면 `.env` **무편집**으로 간다(`R13` 후보 ③ untracked 원복 근거 부재 위험 회피).
⚠️ 실물 위치 — `settings_gatekeep.py` 는 `tools/web_rag/` 가 아니라 **`writer_project/` 루트**다
(`def gatekeep_enabled` `:221` · `return _flag("GATE_KEEP_SOURCES", False)` `:223`). `R14 §5` 실측.

### §research-1 — objective 기반 리서치 리포트 파이프라인
- **시작** 2026-08-03 / **토픽** `topics/experiential-marketing-media.env` (ad/research 트랙 공유)
- **venv** `../.venv_openai/bin/python`
- **북극성** objective → 쿼리생성 → 수집 → 색인 → objective별 retrieve → 충분/부족 판정 → 재쿼리 루프 → objective별 writer
  📄 `scripts/output/§research-1/R2c_NORTH_STAR.md` (착수 판정 테스트 포함)
- **현재** 🟢 **선분 1(쿼리 생성) 요건 확정 · Q-2 종결(2026-08-06).**
  🟢 **선분 3 / S4 계열 종결(2026-08-10, S4~S4-i 9차수, 전 구간 $0).** 아래 확정 6건
  🟢 **S5·S6 쿼리 동결 종결(2026-08-10, `da9f1ef8`)**
  🟢 **R12·R13 정찰 종결(2026-08-12, `4bedc840`·`c0412a61`).** 유료 수집 착수 요건 확정 8건
  🔴 **다음 = S9 유료 수집 1회.** 2단계 구조(Phase 1 검증 $0 → 정지·보고 → 챗 승인 → Phase 2 유료)
  ⚠️ 결정 4는 **선분 1로 대체됨**. 코드 반영 방식은 **미정**(방식 (나) 기각)
- **누적 비용** R1~R2 $0 / R3-1 유료 4회(≈73,281토큰) / **Q-1 $0.03 · Q-2 $0.015** / **S4 계열 $0** /
  **S5·S6 $0**(박제 재사용, LLM 재실행 0회) / **R12·R13 $0**(읽기 전용 정찰)

#### S4 계열 종결 — 확정 6건 (2026-08-10)

| 항목 | 확정 |
|---|---|
| **(d) 자수 분리축** | **유효.** 근거 3갈래 — `extra` 잔존 26건 전량 타 위치(출처 노드 실패 0) · 대조② `(a)51` body 94.8% / ≥10자 **100%** · 상한 민감도 8→32 **±0**. ⚠️ **축3 역전은 근거에서 제외**(인공물 혐의는 해소됐으나 2 URL 이 body 의 52.7%인 소표본) |
| **T** | **10 확정.** ④확정에 **8~9자 2건 존재**(`숏폼 콘텐츠 전략` 9자 · `브랜드경험디자인` 8자) → T=8 은 그 충돌을 뭉갠다 |
| **③ 계층** | 🔴 **오작동 확정.** 견본 103건 육안 → **본문 80.6%**(기준 ≥20%의 4배) |
| **CP 3분할** | **CP-1** 부분일치 유일 근거(중복 58 / 배타 33, 정밀도 **0%**) · **CP-2** 의미 불일치 완전일치(중복 30 / 배타 **18**, 44건 전건 본문) · **CP-3** 전역 요소(**9 URL**, ③ 452 중 188 파급) |
| **전수 대상** | **320** = ④구분가능 162 + ②header최근접 98 + **③CP-3 9(URL)** + CP-1 33 + CP-2 18. ⚠️ **단위 혼합**(CP-3만 URL) |
| **표본 편중** | ③ 452 상위 1 URL(`wedidit.kr`) **21.9%** · 상위 3 **40.0%** · 상위 5 **54.2%** |

~~**다음 판단 3건 (미정, 챗 소관)** — 계층별 표본 규모 / 라벨 부착 방식 / 봉인 절차~~
🔴 **소멸 (2026-08-12, R12·R13 확정 5·7).** 크롬 제거를 **이름사전이 아니라 태그 기반**으로 확정하면서
세 판단이 딛고 있던 전제(이름사전 규칙 R 을 만든다)가 사라졌다. **세션 C 를 열지 않는다.**
⚠️ **S4 9차수는 헛일이 아니다** — 산출물의 소임은 규칙 R 의 재료가 아니라 *"③ 계층은 본문을 80.6% 지운다,
이 길로 가면 안 된다"* 의 **증명**이었고, 그 증명이 태그 기반 채택의 근거다.
⚠️ 위 확정 6건·이월 4건은 **기록으로 존치**한다(태그 기반 결과가 미달일 때 되돌아올 자리).

**이월 4건** — ① 표본 편중 명시 문장(규칙 R 문안 시 URL별 기여도 확인) ② `top` 단독 3건 **보류 유지**(이름만으로 상단 구역/앵커 불가분, 라벨 대상) ③ 배치계열 목록 밖 접두어 후보 **0건**(목록 변경 사유 미관측) ④ 봉인 JSON 은 레포 밖 보관(`.gitignore:84` / `CLAUDE.md §4`) — `_archive_research1/s4_seal_20260810/`

**🔴 오판 원장 — 패턴으로 묶는다 (나열 금지)**

```
**패턴 α (단위 혼동) — 4회.** H · J · L + **52 오판**

🔴 **2026-08-10 정정 — "3회"는 누락분이 있는 수치였다.**
누락분 = `CC_S5S6_20260809_query_freeze.md §3-a` 가 정정한 *"objective 4개 · 52쿼리(13×4)"*.
52 는 Q-1 의 **arm 4종 합계**이지 objective 배분이 아니다(실측 확증: X5 + Y13 + Z17 + Z'17 = 52,
dist 5,200 = 52 × `N_RESULTS 50` × 2ns). 1회 산출은 **17쿼리**.

⚠️ **회차 성격 구분** — 이것은 **재발 4회차가 아니라 집계 범위 결함**이다.
발생일은 08-09 로, α 를 3회로 집계한 08-10 S4 종결 **이전**이다. 집계가 대상을 S4 산출물로
한정해 병행 트랙 지시서를 훑지 않았다. "경고 후 재발"로 읽으면 원인을 오도한다.
⚠️ 해당 오판은 **같은 문서 안에서 자진 정정**됐다. 정상 참작이나 계수 제외 사유는 아니다.

→ **원장 집계 시 병행 트랙 문서를 범위에 포함한다.**

패턴 β — 차단 대상을 차단 지시서에 배치 (1회)
  K: 앵커링을 막으려 한 눈대중을 같은 문서 본문에 실었다.
     → S4-i #85 에서 "판정 불변 0건을 결백의 증거로 쓰지 말라"로 처리.
     교훈: 차단 장치는 차단 대상과 **다른 파일**에 둔다(S4-e·S4-g 의 TEXT/NAMES 분리가 작동한 이유).

패턴 γ — 실물 미확인 상태로 대상 지정 (2회 → **5회**, 2026-08-12 추가 3건)
  M: CLAUDE.md·STANDARDS.md 대상을 ../ 로 적었다 (rev.2 정정)
  N: .gitignore·§4 미확인 상태로 JSON 커밋을 지시했다 (rev.3 정정)
  ⚠️ 둘 다 "에러 없이 잘못된 결과" — M=다른 파일을 읽는다 / N=커밋에서 조용히 빠진다.
     STANDARDS §5.1-a pathspec 함정과 동형.

  🔴 **2026-08-11~12 챗 발 3건 (전부 수신 측이 실물 보고 정정 → 사고 미발생)**
  O: `catch CK` 위치를 "기존 항목 말미"로 지정 → **그 조문 자체가 없었다**
  P: 신규 블록 헤딩을 `5-c` 로 지정 → 수신 문서 기준 실물은 `7-e`
  Q: push 지시에 "origin/main 3건 반영" → 실제 **2건**(`03bbde70` 은 이미 origin 에 있었다)
  ⚠️ **계상 주의** — 아래 「라벨 오독」 블록의 `catch CM` 건은 같은 계통(`catch CT`)이나
     **별도 기재분이므로 γ 에 중복 계상하지 않는다.** 이 세션 `catch CT` 발현 총 4회 = O·P·Q + CM.
  ⚠️ **O·P·Q 는 M·N 과 심각도가 다르다** — M·N 은 집행돼 정정 차수가 들었고, O·P·Q 는 **집행 전
     수신 측 실물 확인에서 걸렸다.** 재발 횟수로만 읽으면 방어선이 작동한 사실이 지워진다.

  → **규율 도출**: 지시서에 **개수·위치·라벨**을 쓸 때는 근거를 함께 적거나
    "실물 확인 후 확정"으로 남긴다. 기억에서 꺼낸 수치를 확정형으로 쓰지 않는다.

**🔴 라벨 오독 (2026-08-10, 챗 발 — `catch CT`·`CL` 보강)**
CC 의 정확한 인용 `(catch CM)` 을 신규 번호 부여로 읽고 제거를 지시했다. 원장 실물을 열지 않고
라벨만 보고 판단한 것이 원인이다. CC 도 동의해 `da9f1ef8` 에 반영됐고, 복원에 별도 차수가 들었다.
→ 원장 규율 지시 전 **해당 파일을 연다.** 수신 측 동의는 검증이 아니다.

개별 — A · C · D · E · F · G · I
```

**CC 발 이탈 4건** — venv 위반(catch CO) / `probe_s2_agg` 재실행 미수행 / diff 미수록 / S4-h 침묵 실패. **전부 자진 보고·해소.**
⚠️ **S4-h 침묵 실패는 자체 교차 검증으로 발견돼 `CLAUDE.md §9` 실증 사례가 됐다. 이탈로만 계상하지 않는다.**
⚠️ **2026-08-10 커밋 세션의 §0 정지도 기재한다** — 지시 내부 모순(머리말 "충돌 시 보고" vs §9 "§4 우선")을 발견해 **집행 전 정지**. 이탈이 아니라 규율 준수 사례다.

> 🔴 **착수 판정**: "이 작업은 8단계 중 어느 선분을 잇는가?" 답이 안 나오면 **이월표로.**

| 단계 | 결과 |
|---|---|
| R1 | 그래프·fast-path·근거사슬 정찰. **계기판 기발견**(9노드 `emit_event` + `/api/events`·`/api/state`) → 계측기 삽입 작업 소멸 |
| R1b 감사 | credential 이력 오염 발견 → STOP → 키 회전(사용자) + HEAD 정리 + `.gitignore` 보강 + 감사 절차 3축 개정 |
| R1b 대조 | 원형 BlockAGI Plan↔Evaluate 배선 실측 |
| R2-a | 설계 입력 수집 + R1b 정정 2건 |
| R2-b | 결정 1(JSONL 스키마)·3(2단) 확정, 결정 2 결론 |
| R3-a 정찰 | T1~T16 — 진입점·dedup·state·프롬프트 실측. 박제 `R3a_ENTRYPOINT_RECON.md` |
| **R3-1 run1** | 본선 retrieve 0회 · 마커 17(전부 고아) · 사이드카 0 — `flags.smoke_retrieve_done` 이월로 §2~7 스모크 스킵 |
| **R3-1 run2** | 본선 retrieve **0회** · 마커 17(전부 고아) — **쿼리 0개**(`(summary) total 0 queries`). 쿼리 소스 4관문 전부 빔 |
| **R3-1 run3** | 본선 retrieve 7회 · **마커 0** · 사이드카 0 — Direct QA 조기 반환(`:1305`×5/`:1342`×2) + `[이전 대화]`에 QA 산문 유입 |
| **R3-1 run4 (B안)** | **파이프라인 첫 작동** — 사이드카 5 · 마커 12(출처연결 11) · 요약 11콜 · 접힘 0%. **D4 ② FAIL**(§1 마커0 / §7 고아1) |
| **목표 재정의** | 08-06 — "연구 루프 구조 재설계" → "objective 기반 리서치 파이프라인". 결정 4 대체. 박제 `R2c_NORTH_STAR.md` |
| **Q-1 arm 측정** | X/Y/Z/Z' 4arm · 52쿼리 · dist 5,200값. **X 기각**(catch BZ) · web/local 내용 분리 확정(BX) · **크롬 29% 발견**(CA). M4 108건 육안 분류 |
| **Q-2 선분 1 종결** | Z''(요구형) 실측 — 술어 보존 58%→**78%** · 연도 0/4→**3/4** · 병기 국문만 4쌍→2쌍. X 회귀 없음. 박제 `Q2_SEGMENT1_CLOSE.md`(커밋 `16de443f`) |

**🔴 R3-1 3대 발견**
1. **B안 확정** — `vector_search_agent` 우회(`_dual_retrieve` → `merge_refs` → `section_writer`).
   그 노드는 D4 산출물을 생산하지 않는 래퍼다. **삭제 아님, 우회. 챗 UI 경로는 그대로**
2. **web 통과율 3.6%** (28→1) vs local 71.4% (14→10) — **실질 코퍼스는 local 302청크뿐** (catch BK)
3. 🔴 **조작은 환각이 아니라 지시 이행** — `[Executive Summary 규칙]` 블록이 **7/7 주입**(절단 분기 0).
   그리고 **§1 은 refs 0 에서 최대 조작을 하고 마커 0 이라 전 검사를 통과했다** — 검사망 최대 사각 (catch BC/BO/BR)

**🔴 R1의 3대 발견**
1. **근거 사슬은 본선에서 안 끊긴다.** X는 `research_synthesizer` 곁가지 1곳뿐이고 소비처가 0건.
   본선은 `[[N]]` 인덱스 + `.refs.json`(URL + 청크 풀텍스트)로 살아 있다
2. **Evaluate→Plan 되먹임 배선이 없다.** 목표는 env 5개 고정 순회(`research_planner.py:227`)
3. **원인은 배선이 아니라 출력 형식.** 원형은 Evaluate 출력에 JSON 스키마를 강제하고
   `json.loads` → dataclass로 필드를 꺼낸다. 우리는 마크다운 자유 서술이라 추출 불가
   → **R2 과제가 "슬롯 1개 추가"에서 "출력 스키마 + 파서 + 슬롯"으로 확대됨**

**우리가 원형보다 나은 2건** (이식 불요): 루프 종료(`no_new_url_streak` 보유) · citation 보증(`.refs.json`)

**이월 — §research-1 (R3b 시점, 2026-08-07)**

| # | 항목 | 우선도 |
|---|---|---|
| **선분 3** | 🔴 **색인 위생 정찰 범위 결정(챗 소관).** 후보 A 크롬제거 / B 플레이스홀더 / C `published_date` / D 조각ID / E 구조보존파싱. ⚠️ **범위를 먼저 좁힐 것** — 다 넣으면 §ad-track-1처럼 미달로 끝난다.<br>✅ **정찰 종료(R3b·R4·R5·R6·R7, 2026-08-08).** 남은 미지 = **크롬 판별 규칙** — 정찰로 풀 수 없고 규칙 초안 + 실측이 필요하다<br>✅ **크롬 판별 규칙 해소(2026-08-12) — 규칙을 만들지 않고 태그로 간다.** S4 9차수가 이름사전 경로의 오작동을 규명(③ 본문 80.6% 삭제)했고, 그 증명을 근거로 **태그 기반**(`nav`·`header`·`footer`·`aside`·`form`)을 채택했다. **세션 C(라벨 320) 미개설.** 남은 것은 규칙 설계가 아니라 **X/Y 분기 구현 방식**(미결 1) | **최상** |
| **A 측정 설계 전제** | 🔴 **`q1_arms_dist_raw`(29%)는 before로 쓸 수 없다** — 현존 `*_filtered.json`과 색인 118의 교집합 0, 즉 그 색인은 재현 불가. → **신규 수집 1회 → 같은 `*_filtered.json`에서 색인 2벌**(X 현행 / Y 크롬제거+날짜). before는 X에서 새로 잰다. 29%는 "문제 존재"의 근거일 뿐 **대조 기준선 아님.** ⚠️ 착수 조건 — 수집 직후 그 파일을 **별도 경로로 복사하고 경로·해시를 박제**한다(현재는 `catch CK`의 우연에 기대어 남는 상태). ⚠️ provider 고정 + 실효 NS 3키 실행 시 출력 확인<br>🔴 **원장 규칙 3건 (2026-08-08 실측)** — **(a)** 복사 대상 = 그 라운드의 **전량**. 파일 하나가 아니다. **(b)** 원장 규모를 셀 때 **빈 파일을 제외**한다 — 실측 43파일 = 빈 11 + 내용 32. ⚠️ 제외 기준은 바이트 크기가 아니라 **JSON 파싱 후 레코드 0건.** 크기 일치를 동일성으로 읽은 함정 3번째 사례(`STANDARDS §3.2` 빈 Chroma 188,416B · 7508B 3파일 · `find -size -10c`). **(c)** 🔴 **원장의 단위는 파일이 아니라 URL. 실측 고유 URL 28.** 같은 URL 이 최대 9파일까지 반복(분포 1:18 · 4:1 · 5:2 · 6:5 · 9:2). 확인 사례 `web_178556613{2,5,9}_0ec68454` — 크기 7508B 동일, `fetched_at` 만 다름(06:35:32/35/39), content·raw_content·url·title 전량 일치. ⚠️ **반복 원인 미확인** — 라운드 반복·재시도·쿼리 중복 중 무엇인지 정하지 않는다. ⚠️ **대표 선정 규칙** — 전 그룹 content 해시 대조가 **선행**이다. 근거가 1그룹뿐인데 반복 그룹은 10개다. 전건 동일 확인 후 파일명 사전순 첫 번째를 대표로. **한 그룹이라도 어긋나면 중단 + 별건 catch**<br>⚠️ **규모 4배 차이** — 현존 고유 URL 28 vs 현행 색인 118. 남은 43파일은 색인 118 을 만든 라운드가 아니라 **더 작은 다른 실행들의 잔여물**<br>🔴 **라벨 봉인 단위 = 청크가 아니라 문구(span). (챗 결정 2026-08-09, 작업 2 시점 · 미등재분 소급 기재)** 근거 — 청크 **65 중 45(69%)가 크롬·본문 혼재**라 청크 단위 이진 라벨이 성립하지 않는다(`R8_CHROME_LEXICON.md:53`·`:68`). 청크를 통째로 크롬/본문으로 찍으면 45건에서 둘 중 하나를 반드시 틀린다. ✅ 작업 3이 같은 단위로 수행돼 정합 — 매칭 단위를 span 으로 내렸고, 전문 매칭은 여러 DOM 노드에 걸쳐 **원리적으로 불가**함을 실측했다(`R9_CHROME_TAG_MAP.md:89`) | **최상** |
| **A 제거 지점** | 🔴 **`ingest_docs.py:260`** `web_results_to_documents`. `soup.get_text(separator="\n")` = 본문 추출 아님, 제거 태그 `script`·`style`·`noscript` 3종뿐. trafilatura·readability 전 레포 0건. **`get_text()` 이후 태그 정보 소실** → 하류 필터 9개는 문서 단위 또는 청크 단위뿐, **문서 내부를 도려내는 층이 없다.** 🔴 **수리 자리 = bs4 2곳**(`ingest_docs.py:386` 분기 · `ingest.py:677`). 둘은 문자 단위 동일 로직이고 `content_type`이 둘 다 `text/html`이라 **색인 메타로 가를 수 없다 → 대칭으로 고친다.** ⚠️ 종전 "사본 3벌"은 **챗 발 오판(39)** — `:260`은 진입점, `:386`은 그 분기다. 사문 2건 확인: `seed_web_namespace:1845` · `search.py:202`. R5 §2 · R6 §1<br>🔴 **2026-08-12 정정(`R13 §5-b`) — "bs4 2곳 대칭 수리" 전제는 무효.** 두 경로가 **배타적**임이 실측(**69/69 전건**)으로 확인됐다. 본선은 **`ingest_docs.py:385` 단일**이고 크롬 제거는 **한 곳만** 손댄다. ⚠️ 행번호 `386`↔`385` 불일치는 **실물 확인 전까지 통일하지 않는다**(위 R12·R13 블록) | **최상** |
| **선분 1** | 🟡 **쿼리 생성 = 종결(Q-2).** 요건 4건 확정 — 술어 보존 58→**78%** · 연도 0/4→**3/4** · 국내강제 제거 · 갈래 분해(X 회귀 없음). Z'' 문안 박제 `Q2_SEGMENT1_CLOSE.md`. 🔴 **남은 것 = objective별 아웃라인 + writer 설계.** 결정 4를 대체함(A 소멸 · B 흡수 · C 불변, `section_writer.py:277` 수정 0건) | **최상** |
| **catch CA** | 🔴 **사이트크롬 색인 오염.** nav·푸터·사업자등록번호·직원명단·댓글폼이 dist **0.669~1.081**로 상위 통과. web 통과 청크의 **29%**(19/65), local 0건. **임계 조정 불가 — 수집·색인 제거가 유일** | 최상 |
| **catch BX** | 🔴 **web/local 내용 분리** (구 BK 재해석). web min dist — 이론·측정 1.158~1.426(전량 탈락) / 사례·트렌드 0.545~0.811(대량 통과). local은 정반대(홀브룩 0.554·분트곡선 0.634·브라커스 0.738). **objective 1·4는 web에서 원리적 불가.** BK를 "인덱스 부실"로 읽은 것은 오독 — `RETRIEVE_WEB_RATIO` 조정 사안 아님 | 최상 |
| **catch BZ** | 🔴 **형태 분류로 부적합을 못 잡는다.** X obj1 web rank1·2 = 본문형·통과·완결문단이면서 주제 완전 불일치(뷰티 팝업 vs 이론 계보 쿼리). 쿼리 고유명사 13개 중 실재 **0**. 동일 쿼리 local은 정확 매칭(0.5342). **BT(껍데기)에 이은 제2 유형 사각** | 최상 |
| **catch BT** | 🔴 **껍데기 청크 유도 조작.** 목차·헤딩 수준 청크가 "항목 존재"만 전달 → writer가 내용을 채움. §2 부재 7건. refs 2건 + 마커 정상이라 전 검사 통과 | 최상 |
| **catch BS** | 🔴 **objective 원문이 근거 대체재.** round-02 `## 근거 요약` 4건 중 3건이 objective 1 재진술에 `[저자(연도)]` 라벨 부착. 근거 얇을 때 objective가 findings로 역류. ES 블록과 독립된 **제2 조작 경로** | 최상 |
| **판정선** | 🔴 **`refs.docs == 0 && 본문 길이 > N → 즉시 FAIL`.** run4 실측 = refs 0 섹션(§1·§7)이 조작 2섹션과 정확히 일치. 밀도 휴리스틱 불요. ⚠️ **프롬프트 교체가 이 사각을 닫지 않는다 — 별건으로 살아 있다** | **최상** |
| 판정선 개정 | refs==0 단독 불충분(§2 미검출). D4③ **표본 → 전수** 확정(표본이 최악 §2를 놓침, 전수 실비용 $0 확인) | 최상 |
| **catch AY** | `published_date` 메타데이터 부재 — objective에 "2023~2026" · "2025~2026 최신 동향"이 명시돼 **날짜 없이는 구조적으로 충족 불가.** 선분 3의 선행 조건. 중 → **상 승격** | 상 |
| **catch CB** | local 플레이스홀더 오염 — `🟨 실무 사례 자리` 슬라이드가 색인 포함. 2건, web 0건<br>⚠️ **2026-08-08 — 선분 3(A 크롬 제거)에 편입 불가.** A 의 수리 자리는 web HTML 파서 2곳(`ingest_docs.py:386` · `ingest.py:677`)이고 CB 는 local(PPTX) 경로다. **같은 훅을 공유하지 않는다. 별건 이월.**<br>✅ 근거 실측(2026-08-09) — `q2_zdouble` 원자료 내 `🟨` 청크 **13회 전량이 `source=file://…/refs/experiential-…`**(고유 청크 2종: YOUR CASE · SWEET SPOT), **web(http) 0건.** `R5_INGEST_CHAIN_RECON.md:456` 과 정합(local 은 파서 분기 1 `file://`·`item.content` 직통 → **bs4 미경유**). 파서 행번호 2건도 실물 일치 확인<br>⚠️ 이로써 인계 메모 §2 의 **"B = A 훅의 시험재" 전제는 무효**다 | 상 |
| **catch BU** | 🔴 **근거 미활용.** §5 refs text에 92%·별점 4.8·아마존 실재, 본문 사용 0. **검사 항목 자체가 부재** | 상 |
| **코드 반영** | 🔴 **미정.** 방식 (나)"v2 함수 신설" **기각** — 호출부 `research_planner.py:229` 1곳이 챗 UI 본선. 후보 (다)조건분기/(라)전용노드/(마)state주입. **결정 시점 = R3-2 루프 설계 후** | 상 |
| **선분 1 이월** | ① obj4 취약(14중 7, 참여지표 갈래 미배정) ② 영문 소실 `experiential grid`·`customer journey map` ③ 자수 하한 미달 6건. ⚠️ **규칙을 추가하지 말 것** | 상 |
| **catch CE** | 🔴 `CHROMA_DIR` 2순위 경로 사문. **읽는 자리 = `tools/web_rag/utils.py:1734`** (`_cfg_str("CHROMA_DIR", default="")`). 그 `_cfg_str`(**utils.py:122**)이 `getattr(CFG, key)`만 읽고 `os.environ`을 안 본다. `Config`(config.py:228)는 `@dataclass`이고 `CHROMA_DIR` 필드 0건 → `AttributeError` → `default=""`. `core/topic.py:142`의 환경변수 미러링은 이 경로에 미반영. **에러 없이 조용히 기본 경로.** 색인 격리는 `CHROMA_NAMESPACE`(L3 topic preset)로만 가능. ⚠️ CFG 재빌드 경로는 미확인<br>⚠️ **2026-08-07 보강 실측** — `.env:125`에 `CHROMA_DIR=data/chroma_store`가 **주석이 아니라 활성 상태로 존재**한다. 값이 없어서가 아니라 **값이 있는데 무시된다.**<br>⚠️ 이 건은 `STANDARDS §1.3` **0단**이 이미 규정한 경우다(CFG 선언 0건 → `.env` 무효, 2~4단 생략). 0단을 밟지 않아 우회로로 갔다. 거짓 0의 원인 = **grep 심 치환(ugrep `--ignore-files`)이 `.gitignore:16-17` 대상인 `.env*`를 배제** → `catch CG`(CLAUDE.md §9)<br>⚠️ **2026-08-08 R4 off-by-one 정정** — 구 문안이 가리킨 `utils.py` `:121`은 `def`가 아니라 `if "_cfg_str" not in globals():` **가드 줄**이다. 정의는 `:122`. 같은 파일 `_cfg_bool`은 `:742`에 **가드 없이** 별도 정의(시그니처도 키워드 전용) — 동명 사본이라 인용 시 위치 확정 필요. R4 §5-a | 상 |
| **재색인 실측** | 🔴 **정정(R7)** — `research/resources_*.json`은 **기록용 스냅샷이고 색인 입력이 아니다**(`_to_safe_json_record:581-583`이 4000자 절단, 113/113 정확히 4000). 색인 입력은 `*_filtered.json`이며 **`raw_content` 보존**(수집 시점 raw_bytes 14,584~16,569,880). 실행 로그 65행 전수 — 스냅샷을 읽은 기록 **0행**. ⚠️ 종전 "원자료에 본문 없음"은 **파이프라인이 안 읽는 파일을 잰 것**(catch CI 계열). → **드리프트 없는 대조가 가능할 수 있다** — 같은 `*_filtered.json`에서 색인 2벌. ✅ **잔존 확인(R7 §1-b-9)** — 삭제 코드 **0건**(`_filtered` 2행 = 생성부·주석, `os.remove`/`unlink`/`rmtree` 14행 전량 대상 확인). 현존 **43건**(mtime 06-01~08-01, 합 19.9MB). `raw_content` 80/80 보유, **>4000 15건 · ==4000 0건**(같은 라운드 스냅샷은 `raw_preview` 276 · >4000 0 — 정반대 형태로 공존). 🔴 **단 현존 43건 ∩ 색인 118 = 0.** 드리프트 없는 대조는 **신규 수집 1회분으로만** 가능. 링크로트 97.5%는 그대로 유효. 🔴 배치 = **46**, `CHROMA_MAX_BATCH=16`에 걸린 배치 **0/46**(64로 올려도 46) — 실제 제약은 하드코딩 `MAX_TEXT_SUM=18000`(`ingest_vector.py:1236`), **env로 못 바꾼다.** `INDEX_TIMEOUT_SEC=60`은 중단이 아니라 **사후 경고 로그**(`_batched_add:646-651`) → 시간 추정 근거 아님. ✅ **링크로트 GET 기준 115/118(97.5%) · 413/416청크(99.3%) 생존** → 재수집 성립. 잔여 3건은 SSL 미해결(죽은 링크 아님 — 논문 트랙 certifi 전례 확인 필요). R5 §4 · R6 §3 | **상** |
| **catch CH** | 🔴 **refs 병합층 접힘.** `utils/rag_utils.py:234` `_doc_key_from_any` 키 = `norm_url\|src:…##part`. web 메타에 `part`/`page`/`fragment`가 **없어**(R4 §2-a 4키) 같은 URL의 N청크가 **1건으로 접힌다.** local은 `source` 자체에 `#part=…&chunk=N`이 박혀 안 접힌다. 호출부 8곳 전량이 그래프 노드 3개(`research_planner`·`vector_search_agent`·`web_search_agent`) **몸통 안**. 정의 2벌 중 `utils/refs.py:255`는 **사문**(본선 import 0곳) — R2c의 `_canonicalize_src_for_dedup` 판정과 정합.<br>⚠️ **논문 트랙 실행 X 확인**(R6 §4). 단 근거는 **함수 경계 스캔**이라 R3b(디렉토리 부재, 물리 증거)보다 약하다. R5 §3 · R6 §4 | 상 |
| **C 게이트** | 🔴 **`published_date` 비용이 분기마다 다르다.** 사본 ①(`ingest_docs.py:386`) = DOM `:385~390`, 반출 경로 **있음**(`:398` 메타 dict) → 한 줄. 사본 ②(`ingest.py:677`) = DOM `:675~682`, 반출 경로 **없음**(계약 `(url,timeout) -> str`) → 시그니처 변경 + 호출부 연쇄. **어느 쪽이 본선인지 미확정.** 폴백(except) 경로는 양쪽 다 정규식이라 DOM 부재 → 불가. ⚠️ 날짜 추출 코드 전 레포 0건, `soup.find`/`find_all` 0건 — **C는 확장이 아니라 신규 기능.**<br>✅ **열림(R7).** 분기 조건 = `ingest_docs.py:382 if raw_content:` truthy 단독(길이·품질·CFG·env 참조 0건). 색인 입력에 `raw_content` 보존 → **갈래 ①이 기본.** → **C 비용 = ① 한 줄.** ②(enrich 실패 폴백)로 빠진 문서는 날짜 없이 통과(열화 허용). ⚠️ 현 색인 118건이 실제 어느 갈래였는지는 **확인 불가** — 해당 라운드 파일 부재, 로그에 07-31 없음. 코드 경로 근거이며 실행 흔적 근거 아님. R6 §2 · R7 | 상 |
| **catch CK** | 🔴 **정규 writer 산출물을 아무도 안 읽는다.** `web_search.invoke`가 `(results, path)`를 반환하나 `:848`에서 path 폐기 → `:948 json_path=""` → fallback writer(`:966-967`)가 **항상** 발동. 결과 ① 매 라운드 두 파일 쓰기 ② 정규 writer 사문 ③ **`_to_safe_json_record` 절단이 파이프라인에 미발동.** ⚠️ ③이 지금 `raw_content`를 살리고 있다 — **우연이다.** `:848`을 "고치면" 절단이 살아나 원본 HTML이 사라진다. **손대기 전 이 행을 볼 것.** R7 | 상 |
| **D 재기술** | 조각ID 신설은 **소멸** — `<sha1>-<6자리>`가 이미 있고 전건 유일. 저장층 접힘 0(118 URL → 416 청크). 남은 것은 **`source` 입도 불일치**(web=문서 단위 / local=`#part=&chunk=N` 청크 단위). **하류에서 `source`를 키로 쓰는 자리가 있을 때만 문제** → 정찰 2에서 grep 1회로 판정 | 중 |
| 🔴 **URL 간 반복 — 제거·채점 동어반복** | **미결. 규칙 R 설계 시 결정 (승격 등재 2026-08-09, 근거 `R9_CHROME_TAG_MAP.md §7-c-6`).** 작업 3 실측 — `(b)` 유형 10건은 **구조로 본문과 구별 불가**다(조상 태그 없음 · `class`/`id` 없음 — `div.wrap → div.content` · `body` 직속). 이들을 `(b)`로 분류한 **유일한 식별 신호가 URL 간 반복**이었다.<br>⚠️ **그런데 URL 간 반복 n-gram 은 선분 3 채점 설계의 L2 독립 지표다.** 제거와 채점에 같이 쓰면 **동어반복** — 제거기가 반복 n-gram 을 지우고 채점기가 반복 n-gram 이 줄었다고 점수를 준다.<br>**선택지 3** — ① 제거에 안 쓰고 **0.73% 잔류** ② 제거에 쓰고 **L2 대체 설계** ③ **임계 분리**.<br>⚠️ 세 선택지에 **우열을 적지 않는다.** 판단은 규칙 R 자리에서 한다 | **최상** |
| **R3-2** | 결정 1 스키마(`round-NN.jsonl`) + 파서 + planner 슬롯 = **선분 6~7(판정→루프).** 착수 시 `research/<slug>/` 구·신 형식 혼재 처리 방침 필요<br>🔴 **2026-08-12 승격 (중 → 최상, 선분 3 종결 후 1순위).** 북극성 대조 결과 **⑥판정·⑦루프는 08-06 이후 한 번도 손대지 않았다** — 이 트랙 **최대 공백**이다. 8단계 중 두 칸이 ✖ 로 남아 있고, 선분 ①의 🟡 도 **문자열 동결에 의한 우회**라서 ⑥이 열려야 정식 해소된다(`Q2 §2` 미결 3안) | **최상** |
| **catch BV** | `utils/query_filters.py:107` `r"\\b"` 리터럴 → BOOLEAN_TOKENS 제거 무작동. 선분 2 착수 시 수리 | 중 |
| catch BL | `chunk_summary` 섹션 간 캐시 없음 — 동일 청크 재요약(유료 콜 중복). objective 단위 전환 시 재측정 | 중 |
| catch BQ | 모델 입력 = snip 350자 ≠ 대조 대상 = 청크 원문. 7건 중 5건 절단(최저 15%) | 중 |
| **catch CF** | "공유"가 3의미로 갈린다 — **파일 / 로드 / 실행**. 결과를 바꾸는 건 실행뿐. `ingest_vector.py`는 논문 트랙에서 **로드되지만 호출되지 않는다**(R3b). 판정 근거는 코드 추적이 아니라 `academic-trademark-*` 디렉토리 부재(물리 증거).<br>⚠️ **제2축** — "두 트랙"이 어느 두 트랙인지도 문서마다 다르다. GUARDRAILS:41-42 = §ad-track-1·§research-1(명시). 인계 메모·WORKBOARD:124는 회사/논문으로 읽혔다. `ingest_vector.py`는 전자 기준으로는 공유가 맞다 | 중 |
| ~~**Q2 §3.1 대조**~~ | ✅ **해소(2026-08-09) — 갈래 ①(다른 경로로 검색).** 행번호는 옳다. `probe_q1_arms.py:481`·`probe_q2_zdouble.py:523` 둘 다 실물이 `z_prompt = get_research_planner_prompt()` 로 **정확히 일치.** 🔴 **미확인의 원인은 검색기가 아니라 경로 오기다** — 두 probe 는 `scripts/§research-1/` 에 **없다.** 실위치 = `writer_project/` 루트(`scripts/` 하위를 뒤지면 심·실물 **둘 다 0건** — 양성 대조가 성립하지 않는 검색이었다). 루트 기준 재측정 = 심 **16** vs `command grep` **25**, **차이 9건**(probe 2파일 6 + `prompts.py.bak_proposal_20260626` 1 + 바이너리 `.pyc` 2). **심에만 있는 행 0건 → 심 동작 균일, ③ 아님.** ⚠️ `.gitignore` 매칭 실측 — `:106 probe_*` · `:67 *.bak_*`. → **`CLAUDE.md §9` `catch CG` 문안은 틀리지 않았다**("ignore 대상이면 빠지고 `!` 예외면 잡힌다" 그대로 성립). 정정 불요<br>부수 관측 — 심 grep 은 **명시 경로 인자에는 ignore 대상도 검색한다**(`.env.openai` 실측 2건). `--ignore-files` 배제는 **재귀 탐색에만** 걸린다 | 해소 |
| **catch CJ** | `seed_web_namespace` 경로 2중 사문 — ① `SEED_URLS`가 CFG·`.env` 4파일 전부 0건 → `_get_seed_urls` 항상 `[]`(`supervisor.py:235` 반환) ② 호출/정의 인자 불일치(`topic_slug` 미존재 파라미터)를 `except Exception`(`:243`)이 삼킨다. ⚠️ **재수집 설계에서 seed 경로를 쓰면 조용히 실패한다.** R6 §1 | 하 |
| ~~catch BW~~ | **철회** — "임계 변별력 부재"의 근거 distinct 32~44%는 52쿼리 **합집합 커버리지**였다. 쿼리별로는 0/50~50/50. 임계는 작동한다 | 철회 |
| ~~catch BY~~ | **정정** — "objective 3 = 코퍼스 구멍"은 오판. 자료는 있고 쿼리가 못 찾았다(X obj3 local 0.667 · Y 3-c 0.713). 선분 1로 이관 | 정정 |
| ~~catch BC~~ | **원인 확정으로 종결** — `[[N]]` + "~에 따르면" + 근거 부재 = **명시적 지시 이행.** 지시 제거는 선분 1에 포함. 증거물 `_FAILED_*` 보존 | 종결 |
| ~~catch BO~~ | **선분 1에 흡수** — 규칙 합성("수치 필수" + "출처 표기 필수"). 신규 프롬프트 작성 요건 | 흡수 |
| ~~catch BR~~ | **선분 1에 흡수** — 장르 미스매치. 전용 프롬프트 신설로 해소, 챗 UI 광고 틀은 불변 | 흡수 |
| ~~§8-4~~ | **조사 취소** — 참조 전 섹션 공용 8건. objective별 refs 주입으로 구조 소멸. 신규 드라이버 요구사항 **"objective별 refs 교체(누적 금지)"** 1줄로 대체 | 취소 |
| 임베딩 기술 불일치 | 🟢 **강등 — 🔴 미해결 갈래 → 일반 정리 항목 (챗 결정 2026-08-09).** **유료 단계 전 판정 불요.**<br>**강등 근거** — 실제 형태가 "규정 vs 설정"의 **계통 분기가 아니라** `ad/README-dev.md` **한 파일 안의 신·구 기술 병존**이기 때문이다. 같은 파일에 `002` 기술(`:210`·`:1409`)과 `3-large` 기술(`:1368`·`:1371`·`:2001`·`:3234`)이 함께 있다. 갈라진 두 정본을 골라야 하는 문제가 아니라, **낡은 행이 안 고쳐진 채 남아 있는** 문제다.<br>**종전 승격 사유가 소멸한 근거** — "색인 X·Y 가 다른 모델을 쓰면 대조가 무효"였는데, X·Y 는 **같은 `.env` 로 두 번 돌린다.** 실효값은 어느 쪽이든 **하나**이고 두 색인에 동일 적용된다. 문서 기술이 무엇이든 대조의 내적 타당성은 흔들리지 않는다.<br>**남는 일(정리 작업)** — 실효값 `text-embedding-3-large` 기준으로 `002` 기술 행 정리 + 규범 문서(`ad/GUARDRAILS.md:20`·`:42`) 동기. 착수 시점 제약 없음<br>⚠️ 여전히 **한쪽을 골라 손대지 않는다** — 정리 시 실효값을 **실행으로 확인한 뒤**(`STANDARDS §1.3` 0단) 고친다 | 하 |
| **`RAG_DISTANCE_THRESHOLD` 기준 모델** | 🟡 **신규 정찰 항목(등재만, 착수는 다음 세션 — 챗 지시 2026-08-09).** 현행 `1.10` 이 **어느 임베딩 모델을 기준으로 정해진 값인지** 확인한다. 물음 = `pet-food-premium` 과 **같은 유형인가** — 그 토픽은 `0.60` 으로 잡혀 있었고(`README-dev.md:446` — *"글로벌 0.65, pet-food-premium 0.60 (text-multilingual-embedding-002 기준)"*) 모델이 바뀌면 임계는 그대로인데 거리 분포가 옮겨간다.<br>🔴 **등재 시점 관측 — `README-dev.md:1371` 이 이미 답을 적고 있다.** *"`RAG_DISTANCE_THRESHOLD=1.10` — local 68% / web 21% kept. 0.45→0.80→0.95→1.10 단계적으로 측정·조정 (vertex `text-multilingual-embedding-002` 의 0.65 와 **분포 차원 자체가 다름**)"* + `:1368` *"`text-embedding-3-large` + Chroma `hnsw.space=l2`(squared L2) 실측 분포"*. → **1.10 의 기준 모델 = `text-embedding-3-large`** 이고 **모델 교체를 인지한 상태에서 재튜닝된 값**으로 보인다. 즉 `pet-food-premium` 유형(방치된 구 임계)과 **다를 가능성이 높다.**<br>⚠️ 위는 **문서 기술이지 실행 확인이 아니다**(`CLAUDE.md §9` — 기록된 것은 현재가 아니다). 다음 세션 과제는 발견이 아니라 **검증**으로 축소된다: `1.10` 이 박힌 커밋의 날짜·당시 `RAG_EMBEDDING_MODEL` · 토픽 `.env` 별 임계 목록 · `0.60`/`0.65` 잔존 여부 | 중 |
| **`RAG_DISTANCE_THRESHOLD` 단조 증가 경위** | 🟡 **신규 갈래 — 등재만. 착수 안 함 (챗 지시 2026-08-09).** 위 `기준 모델` 행과 **물음이 다르다.** 그쪽은 *"1.10 이 어느 모델 기준인가"*, 이쪽은 **"왜 한 방향으로만 올라갔는가"**다.<br>**물음** — `README-dev.md:1371` 이 기록한 `0.45 → 0.80 → 0.95 → 1.10` 이 **네 번 모두 완화(상향)**다. 조정이 한 방향뿐인 것이 (a) 분포 실측에 따른 정상 튜닝인지 (b) **통과량 부족에 밀린 완화**인지 확인한다. 같은 행이 결과를 *"local 68% / web 21% kept"* 로 적고 있는데, **web 21%** 는 상향 후에도 낮다 — 상향의 동기가 통과량이었는지 물을 근거가 된다.<br>**확인 방법(다음 세션)** — 네 값이 박힌 커밋을 각각 찾아 **당시 메시지·동반 변경·로그**를 본다. 상향과 같은 커밋에 `no_summary_min_qa` 루프·0-hit 대응이 있으면 (b) 쪽 신호. `README-dev.md:1385` 가 이미 *"silent 0-hit 가 무한 supervisor 루프로 이어진다 — 5단계 디버깅 중 가장 큰 함정"* 을 적고 있다<br>🔴 **`catch CA` 와의 관계는 가설로만 둔다. 판정 금지.** catch CA = 크롬 청크가 `0.669~1.081` 로 통과. 이 구간이 `0.95` 위·`1.10` 아래에 몰려 있어 *"완화가 크롬 유입을 열었다"* 는 그림이 그려지나, **임계 상향이 원인인지 크롬이 원래 가까웠는지 이 기록만으로 가릴 수 없다.** `R8 §7-4` 실측은 오히려 반대 방향 — 전량 크롬 8건의 최소 dist `0.6813` 이 같은 쿼리 본문 청크 `0.7424` 보다 **가깝다.** 즉 **임계를 낮춰도 크롬이 먼저 걸러지지 않는다.** 인과는 다음 세션 실측 소관 | 중 |

`CC·CD = 결번 (Claude Code 약칭과 충돌)`


- **드라이버** `scripts/§research-1/run_r3a_straight.py` (**실위치 확인 2026-08-09**, `find` 실측 1건. **tracked** — `git ls-files --error-unmatch` 통과) · **`probe_q1_arms.py`·`probe_q2_zdouble.py` = `writer_project/` 루트** (이 2건만 untracked, `.gitignore:106 probe_*`)
  ⚠️ **2026-08-09 정정 2건** — (1) 구 문안은 세 파일을 `scripts/§research-1/` 로 묶어 적었으나 **probe 2건은 그 경로에 없다.** 이 오기가 `Q2 §3.1` 이월 건의 원인이었다(위 해소 행). (2) 구 문안의 **"전부 untracked" 는 틀렸다** — `run_r3a_straight.py` 는 추적 중이다. 미추적은 probe 2건뿐
- **박제** `R1_FINDINGS.md` · `R1b_{CREDENTIAL_AUDIT,HYGIENE_CLOSE,BLOCKAGI_COMPARE}.md` · `R2a_DESIGN_INPUTS.md` · `R2b_DECISION_1_3.md` · `R3a_ENTRYPOINT_RECON.md` · **`R2c_NORTH_STAR.md`** · **`Q2_SEGMENT1_CLOSE.md`**(커밋 `16de443f`) · **`q1_arms_dist_raw_20260806.md`**(커밋 `dda58a70`·`5b16cdd5`, **push 완료**) · `R3b_INGEST_SHARE_RECON.md` · `R4_D_PREMISE_RECON.md` · `R5_INGEST_CHAIN_RECON.md` · `R6_PARSER_LIVENESS_RECON.md` · `R7_PARSER_BRANCH_RECON.md` · **`R8_CHROME_LEXICON.md`**(선분 3 작업 2 산출, 2026-08-09)
  🔴 **2026-08-12 보강** — 아래 5건이 목록에서 누락돼 있었다. 전부 `scripts/output/§research-1/` 하위:
  **`R9_CHROME_TAG_MAP.md`**(작업 3 산출, 2026-08-09 — 본문 이월표에는 인용돼 있었으나 목록 미기재) ·
  **`R11_QUERY_FREEZE.md`**(쿼리 동결 · §10 게이트 8항) · **`R12_PRECOLLECT_SCOUT.md`**(설계 강제 3건 · 미결 4건 값) ·
  **`R13_NS_SPLIT_SCOUT.md`**(NS 수단 후보 5개 · bs4 단일 경로 · 태그 3종 실측) ·
  **`Q_FROZEN_20260809.txt`**(동결 17줄 · 1,142 B · sha256 `06e5f818…03a8`)
  🔴 오기 — R10 계열 9건 실재(tracked). 2026-08-12 실물 확인. 문단 전체 교체 예정.
  ⚠️ **`R10` 은 인용 0건** — 이 워크보드·인계문·S9 지시서 전체에서 `R10` 문자열 검출 0.
  **양성 대조 병기** = 같은 검색에서 `R9_` 2건 검출(검색 자체는 작동). 결번인지 미작성인지 **판정하지 않는다** —
  다음 차수에서 `ls scripts/output/§research-1/` 로 실물 확인한다.
- **원자료** `q1_arms_20260806-161606.json`(16.0MB, ignored) · `q2_zdouble_20260806-210440.json`(ignored)
- **실행 로그** `scripts/output/§research-1/R3a_run_*.log` 6건 · 결과 JSON 4건 · 산출물 `sections/…/_FAILED_20260805-run{1..4}_*`(삭제 금지)
- **대조 자산** `~/dev/blockagi-ref`(upstream, 읽기전용) · `~/dev/blockagi-run`(포크, `ahead 1` 미push)

#### S5·S6 종결 — 쿼리 동결 완료 (2026-08-10, 커밋 `da9f1ef8`)

- **동결 산출** `scripts/output/§research-1/Q_FROZEN_20260809.txt` — 17줄 · 1,142 B ·
  sha256 `06e5f818…03a8`. 박제 `R11_QUERY_FREEZE.md`
- **갈래 = $0.** `Q2 §7` 박제에 17개 전문이 전량 있어 **LLM 재실행 0회.**
  3경로 대조(박제 §7 / `llm_rows[].queries` / `raw_output` 행분해) 불일치 0
- **정본 = 정규화본.** 말미 공백 제거는 문서 정리가 아니라 파이프라인 변환
  (`research_planner.py:243-277` — 2026-03-02 이후 무변경, byte 대조 완료).
  미채택 원출력 판본 해시 병기(+26 B = 2칸 × 13행)
- **주입 자리 2축** — A 수집 `web_search.py:181 web_search_agent`(정의 181~1645, ast 실측) /
  B 검색 `vector_search.py:391 _dual_retrieve`. **A≠B**
- ⚠️ **동결본은 `_core_subst`(`research_planner.py:307-347`) 미경유.**
  플레이스홀더 잔존 0건(양성 대조 후 확인). 옛 색인과 비교 시 차이축 1개 추가
- ⚠️ **1라운드 고정** — 절대 수치 진술 금지. **X 대비 Y 차이만 읽는다**
- 🔴 파일명 `20260809` 는 지시서 작성일. **실행일은 2026-08-10**
- 부수 — `Q1_JSON`(16MB, ignored) 의존 사슬이 끊겼다. 17줄이 tracked 로 나옴

#### R12·R13 정찰 종결 — 유료 수집 착수 요건 확정 (2026-08-12, 커밋 `4bedc840`·`c0412a61`)

- **박제** `R12_PRECOLLECT_SCOUT.md`(수집 전 정찰) · `R13_NS_SPLIT_SCOUT.md`(NS 분리 정찰)
- **비용 $0** — 양 차수 전 구간 읽기 전용
- **HEAD** `c0412a61` · **origin 동기 완료**(ahead 0 · behind 0 · 미push 잔여 0)
- 잔존 미커밋 = `scripts/§paper-writer-1/measure_paper.py` 1건(논문 트랙 상주, 무접촉 — `catch AS`)

**🔴 확정 8건 (재론 없음)**

| # | 확정 | 근거 |
|---|---|---|
| 1 | 🔴 점화 = **`GATE_KEEP_SOURCES` ENV on.** `--gatekeep` 플래그는 **무효** | `R12` 발견 A — 플래그의 `os.environ` 쓰기를 `reload_config()` 의 `load_dotenv(override=True)` 가 되돌린다. 보호키 5개 미포함 |
| 2 | `MAX_INDEXED_PER_ROUND=0` = **자동 색인 차단 + 라운드 캡 해제 동시** | `R12 §4-c`. **상수 1개가 두 역할** — 수집·색인 분리의 유일 수단이자, 17줄 전량 집행을 여는 열쇠 |
| 3 | `MAX_SEARCH_QUERIES_PER_ROUND=0` — LLM 생성 쿼리 집행 억제(실효 상한 6) | `R12 §7-a` |
| 4 | NS 분리 = **후보 ④ CFG 직접 주입** | `R13` 실증 — 호출 시점 `getattr`, 캐시 상수 0건. `reload_config()` 로 소멸하는 **1회성** |
| 5 | 🔴 **Y 크롬 제거 = 태그 기반** `nav`·`header`·`footer`·`aside`·`form` | 이름사전(③ 계층)은 S4 가 **본문 80.6% 삭제**로 오작동 규명(CP-1·2·3). 태그 기반은 그 기전을 안 밟는다 |
| 6 | `published_date` **이번 사이클 제외** — 보유율 **측정만** 수행 | `R12 §5` — 소비처 0건 · 수정 필요 3곳 · 8.7% 는 일반화 불가 |
| 7 | 🔴 **세션 C(라벨 320) 열지 않는다** | 이름사전 미채택으로 불요. 위 S4 블록 「다음 판단 3건」 소멸 처리와 연동 |
| 8 | 🔴 **본선 bs4 = `ingest_docs.py:385` 단일** | `R13 §5-b` — 두 경로 **배타적**, 실측 **69/69 전건** 해당 |

**🔴 확정 8 이 정정하는 기존 기재 — 「A 제거 지점」 행**

기존 문안은 *"수리 자리 = bs4 **2곳**(`ingest_docs.py:386` 분기 · `ingest.py:677`) → 색인 메타로 가를 수
없으니 **대칭으로 고친다**"* 였다. `R13 §5-b` 가 두 경로를 **배타적**으로 실측하면서 **대칭 수리 전제가
무효**가 됐다. 본선은 단일 경로이고, 크롬 제거는 **한 곳만** 손댄다.
⚠️ **행번호 `386` vs `385` 는 통일하지 않는다** — 기존 기재(`:386` 분기 / DOM `:385~390`)와 `R13` 표기가
어긋나는데, **어느 쪽이 오기인지 실물 미확인**이다. 크롬 제거 구현 차수 착수 시 **실물을 열어 확정**한다
(`catch CT` — 기억이 아니라 실물을 읽는다). 지금 한쪽으로 맞추면 그것이 새 오기가 된다.

**🔴 R13 deviation 종결 — 자기 보고가 아니라 실물로 확인됨**

`R13` 이 인계한 경고(*"`_resolve_persist_dir` 계열은 `mkdir` 부작용이 있어 드라이런에서도 쓰기가 발생"*)에
대해 커밋 차수에서 실물 대조를 마쳤다. **색인 기준선 오염 0.**

| 확인 | 결과 |
|---|---|
| `data/chroma_store/` 하위 목록 | S8 착수 스냅샷과 **diff 0줄**, 10 = 10 |
| 색인 3종 mtime | `-local`·`-web` 08-06 21:04 / base 08-05 20:24 — **무변동** |
| seen-hash 파일 | `venfobel-vitamin-web` 1건뿐. **신규 0** |

**미결 3건 (챗 판정 대기)**

| # | 미결 | 상태 |
|---|---|---|
| 1 | 🔴 **크롬 제거 X/Y 분기 방식** — ENV 플래그 신설 vs 경로 복제 | `R13 §5-c` 기존 플래그 **0건** 확인. **`ingest_docs.py` 파서 주변 구조를 실물로 보고 정한다** — 미확인 상태로 정하지 않는다(`catch CT`) |
| 2 | 수집 후 게이트 결과에 따른 판정 | S9 §9 도메인 쏠림·껍데기 비율 값을 보고 결정 |
| 3 | `published_date` 채택 여부 | 신규 수집분 보유율 측정 후 |

**🔴 알려진 위험 — 네이버 iframe 껍데기 (`R12 §5-c`)**

기존 코퍼스 **63건 중 62건이 ~2800자 껍데기**로 본문이 `raw_content` 에 없다.
`ALLOW_SUBDOMAINS=True` 라 `blog.naver.com` 이 `naver.com` 항목으로 통과한다.

→ 신규 수집도 같으면 **크롬 제거 전후를 비교할 본문 자체가 없다.** X·Y 차이 0 이 나와도 그것은
*"효과 없음"* 이 아니라 **"잴 게 없었음"** 이다. 두 결론을 가르지 못하면 선분 3 전체가 무효 판정이 된다.
→ S9 §9 게이트에서 **색인 전에 측정하고 정지**하도록 설계했다.
⚠️ **허용 도메인을 먼저 손대지 않는다**(`catch CA` — 사이트 구성을 비틀면 크롬 비율이 왜곡된다).
**먼저 재고, 그다음 정한다.**

**📌 정리 이월(착수 시점 제약 없음)** — 위 「A 측정 설계 전제」·「URL 간 반복」 행이 원장 문서를
`R8_CHROME_LEXICON.md:53` 식 **행번호로 인용**하고 있다. `catch CT ④`(원장 인용은 catch 라벨 + 절로만)
신설 **이전** 기재분이다. ⚠️ **일괄 치환하지 않는다** — 절 번호를 실물로 확인하지 않은 채 바꾸면 추적성이
끊긴다. 해당 행을 손대는 차수에서 **그때 실물을 보고** 절 표기로 옮긴다.

## 완료 트랙

### §ad-track-1 — 체험마케팅과 미디어 콘텐츠 (홍익대 대학원 15주) — **완료(E열 미충족)**

> **종결 판정**: 목표(E열 14칸)에 도달하지 못했다. arm Z로 3요소 5건 + 준확정 3건까지가 실적이다.
> 미충족 사유는 수집 부족이 아니라 **파이프라인과 목표의 미스매치**로 판정됐다 —
> 리포트 생성 경로는 브랜드·연도를 깎고(catch AN), 추출 경로는 소스 그룹핑·`published_date`가 없다.
> 잔여 계단(3-d-1 ④~⑧, 3-d-2 ⑨)은 아래 **이월** 표로 이관.
- **시작** 2026-07-30 / **토픽** `topics/experiential-marketing-media.env`
- **목표** 15주 설계표 E열 14칸을 **브랜드 + 캠페인 + 연도 + URL** 4요소로 채운다
- **현재** 계단 3-d-1 (①②③ 완료 / ④⑤⑥⑦⑧ 이월)
- **누적 비용** ≈ $0.25

| 계단 | 결과 |
|---|---|
| 0~2 | 환경·인덱스 구축. `-local` 302청크 / `-web` 416청크 |
| 3-a | 웹 수집 4주차(04·07·13·15). catch J~AA |
| 3-b | LLM arm 리포트 경로. **실패** — 브랜드 27회 참조에 본문 0회(catch AN), 연도 0건. $0.12 |
| 3-c | **arm Z(비-LLM) PASS.** `retrieve` → 청크 원문 + `metadata.source` 직결. 3요소 5건. $0 |
| 3-d-1 | ①git ②커밋 ③catch AT 종결. 코드 변경 0건 |

- **박제** `scripts/output/§ad-track-1/step3{a,b,c}_close_§ad-track-1.md`
- **도구** `probe_Z_extract.py`(arm Z 드라이버) · `probe_dist_adtrack.py`(거리 분포) · `scan_local_coverage.py`

## 결정 기록

| 일자 | 결정 | 근거 |
|---|---|---|
| 08-02 | **추출 경로 = arm Z (비-LLM)** | 목표물이 원문에 있으면 추출이 생성보다 낫다. LLM은 브랜드·연도를 깎는다(catch AN) |
| 08-03 | **`RAG_DISTANCE_THRESHOLD` 조정하지 않음** | 1.10에서 04·13은 100% 통과. 상향하면 04·13 최악(0.98)보다 먼 자료가 섞인다 |
| 08-03 | **NS별 임계 분기 도입하지 않음** | ① local 5.6% 통과는 정상 작동 ② 1.10~1.25 구간에 E열용 사례 없음 ③ `ingest_vector.py`는 두 트랙 공유<br>⚠️ **2026-08-07** — 근거 ③의 "두 트랙"이 어느 쌍인지 원문으로 확정되지 않는다. 회사/논문 독법이면 파일 공유일 뿐 실행 공유가 아니다(R3b, 논문 트랙 호출 0건). §ad-track-1/§research-1 독법이면 그대로 참이다. 어느 쪽이든 근거 ①②가 독립적으로 성립하므로 이 결정은 유지된다. `R3b_INGEST_SHARE_RECON.md` · `catch CF` |
| 08-03 | **07·15주 재수집 필요 확정** | 04·13 최대값 ≤ 07·15 최소값. 임계가 아니라 자료 이격 |
| 08-03 | 논문 트랙 미커밋분 **A안**(개별 add 유지) | stash 분실 위험(B) · 남의 트랙 커밋 월권(C) 기각 |
| 08-05 | **history scrub(`filter-repo`) 기각 — 재론 금지** | 회전하면 옛 키는 무효 문자열. scrub은 죽은 문자열을 가릴 뿐인데 **모든 커밋 해시가 바뀌어** 1년치 박제의 추적성이 파손된다. 두 레포 모두 적용 [STANDARDS §5.4-a] |
| 08-05 | **`blockagi-run` push 하지 않음** | 키가 콘솔에서 이미 삭제돼 무효. B-2 보류로 실익 0. 재사용 사유 발생 시 push |
| 08-05 | **credential 감사 = 파일명 축 1순위** | 구 절차(현행 키 prefix 검색)가 이전 세대 키를 원리적으로 놓쳤다. prefix 없는 키(naver·serpapi)는 정규식으로 검출 불가 [STANDARDS §5.1-a] |
| 08-05 | **§ad-track-1 완료 처리(E열 미충족)** | 미충족 사유 = 수집 부족이 아니라 파이프라인·목표 미스매치. 잔여 계단은 이월 |
| 08-06 | **목표 재정의 — objective 기반 리서치 파이프라인** | 기존 목표문("연구 루프 구조 재설계")이 8단계 중 하나만 가리켜 다음 세션이 루프만 보게 된다 [R2c_NORTH_STAR.md] |
| 08-06 | **코드 반영 방식 (나)"v2 함수 신설" 기각** | 호출부가 `research_planner.py:229` **1곳**이고 그 1곳이 챗 UI 본선이다. v2를 추가해도 :229가 무엇을 부르냐만 남아 트랙 분리가 성립하지 않는다 |
| 08-12 | 🔴 **크롬 제거 = 태그 기반. 이름사전 미채택** | S4 9차수가 이름사전 경로(③ 계층)의 오작동을 실측 규명했다 — 견본 103건 육안에서 **본문 80.6% 삭제**(기준 ≥20%의 4배). 태그 기반은 그 기전을 밟지 않는다 [R12·R13 확정 5] |
| 08-12 | 🔴 **세션 C(전수 라벨 320) 열지 않는다** | 이름사전 미채택으로 규칙 R 자체가 불요해졌다. S4 산출물의 소임은 규칙의 재료가 아니라 **경로 기각의 증명**으로 완료 [확정 7] |
| 08-12 | **`published_date` 이번 사이클 제외 (보유율 측정만)** | 소비처 0건 · 수정 필요 3곳 · 8.7% 는 일반화 불가. 지금 넣으면 선분 3 의 차이축이 하나 늘어 X·Y 대조가 흐려진다 [`R12 §5`] |
| 08-12 | **수집과 색인을 같은 차수에 붙이지 않는다** | `MAX_INDEXED_PER_ROUND=0` 이 그 수단이다. 색인은 X·Y 2벌이라 별건이고, 붙이면 게이트(껍데기 비율) 판정 **전에** 색인이 돌아 되돌릴 수 없다 [S9 §0-a] |
| 08-12 | **S9 = 2단계 구조. Phase 1 검증($0) → 정지·보고 → 챗 승인 → Phase 2(유료)** | 유료 착수 전 억제 3건이 실제로 드는지 **호출 없이** 증명해야 한다. 특히 `MAX_SEARCH_QUERIES_PER_ROUND=0` 이 주입 17줄까지 죽이면 **수집 0건인데 실행은 성공으로 보인다** [S9 §2-b] |

## 이월 — §ad-track-1 잔여 계단 (재개 시)

| | 단계 | 비용 |
|---|---|---|
| ④ | 게이트 위치 정찰 → catch P 재판정 | $0 |
| ⑤ | `published_date` 추출률 실측 → 구현 + diff STOP | $0 |
| ⑥ | arm Z 소스 그룹핑 개선 (catch AV 대응) | $0 |
| ⑦ | 목록 토큰 A/B 검색 dry-run → 10주 쿼리 확정 | $0 |
| ⑧ | 준확정 3건 URL 확인 → 승격 | $0 |
| ⑨ | a) aimatters 목록 4페이지 / b) 10주 신규 / c) 07·15 재수집(쿼리 변형 동반) | **유료** |

## 별건 이월 — credential·외부 서비스 (§research-1 R1b 발)

| # | 항목 | 기한 | 우선도 |
|---|---|---|---|
| 1 | **NAVER Search API → API HUB 이관** — NCP 계정 신규 가입 + 인증 체계 변경(개발자센터 Client ID/Key → API HUB Key) | **2027-06-30 지원 종료** (프로모션 2026-09-30) | 중 |
| 2 | **Naver Client Secret 회전** — 1번과 동시 처리 | 상동 | 중 |
| 3 | SerpAPI 계정 정리 (`.env` 미참조) | — | 하 |
| 4 | `gemini-rag-project-new` 프로젝트/계정 정리 | — | 하 |
| 5 | `blockagi-run` push (평문 키 제거 커밋, `ahead 1`) | blockagi 재사용 시 | 하 |

⚠️ 2번을 단독 선행하지 말 것 — **개발자센터 신규 신청 차단일(2026-07-31)이 지나** 재발급 가능
여부가 불확실하다. 섣불리 회전하면 **현행 키만 죽을 수 있다.** 현행 키는 200 응답으로 유효 확인됨.

> 회전 완료분·미착수분 상세 = `scripts/output/§research-1/R1b_HYGIENE_CLOSE.md` §E

## 보류 트랙 (재개 시 승격)
- **§13-8-3** Haiku 4.5 평가, dual/triple track cost·latency 세분화 [README-dev-2.md]
- **다른 토픽 일반화 검증** pet-food-premium·height-growth 등 [README-dev-2.md]
- **§13-14-α-sonnet R2 prompt 패치** [README-dev-2.md]

## 미결 과제 (원본: README-dev.md §12)
> 요약+포인터. 상세는 원본 §12. 종결·STANDARDS 이관분 제외.

- **메타데이터 풍부화** — `published_date`·`language` 추가. [§12-1] ⚠️ **§ad-track-1 catch AY와 동일 항목.** 3-d-1 ⑤에서 착수
- **distance threshold 재튜닝 절차** [§12-2] ⚠️ **체험마케팅 토픽도 절벽 부재형**(2026-08-03 실측, 인접 최대 점프 +0.11). venfobel과 동일하게 본 절차 적용 불가
- **BM25 키워드 검색 보강** [§12-6] — 좁은 분포 토픽의 대안 mechanism. §12-2 불가 판정이 반복되어 **우선순위 유지**
- **Vertex grounded search 운영** — 영어 자료 토픽만 `SKIP_VERTEX_SEARCH=0` [§12-3]
- **`VertexAIEmbeddings` deprecation 모니터링** [§12-4] / **lazy validation 보강** [§12-5]
- **백업 파일 정리** [§12-8] / **`CLEAR_CHROMA_ON_START` 개선** [§12-9] / **ingest 큐레이션 점검** [§12-10]
- (참고) §12-11~13 sub-track 묶음, §12-14~23 대부분 `closed` → 재개 시 원본에서 open 재확인

### 신규 (§ad-track-1 발)
- 🔴 **`published_date` 메타데이터 추가 (catch AY)** — §12-1과 동일 항목. **우선도 상.**
  ⚠️ **누적 인덱스의 선행 조건이다.** §research-1 R2 설계에서 다시 걸린다
- **arm Z 소스 그룹핑 개선 (catch AV)** — 우선도 중. 이월 ⑥과 동일
- **`topics/pet-food-premium.env`에 vertex 시절 `RAG_DISTANCE_THRESHOLD=0.60` 박제** — openai 오버레이로 돌리면 전량 컷. 우선도 하
- **`1982_Holbrook.pdf`가 텍스트 diff 처리**(5,717줄) — `.gitattributes`에 `*.pdf binary` 검토. 우선도 하

### 신규 (§research-1 발)
- **`.gitignore` 차단 잔여** — `api_secrets.txt` 류 접미 변형 · `*.p12` · `*.pfx` · `id_rsa` 계열 미차단.
  현행 차단분은 루트 `.gitignore:21-36`. 우선도 하
- **`research/<slug>/` 구·신 형식 혼재** — R3-2에서 결정 1 스키마(`round-NN.jsonl`)를 적용하면
  기존 `round-01-findings.md` · `round-02-findings.md`(구 형식, 마크다운 자유 서술)와
  **한 디렉터리에 섞인다.** 파서가 무엇을 읽을지·구 형식을 이관할지 폐기할지 착수 시 방침 필요.
  실물 확인(2026-08-05): `research/experiential-marketing-media/` 에 findings 2건 + `state/` 2건.
  우선도 **중**. (출처: §research-1 R3-1 착수 정찰)

## 아카이브
- README-dev.md, README-dev-2.md = 기존 기획서/보고서 에이전트 개선 기록
- README-dev-§14.md = §14 vertex 백엔드 점검·개선 아카이브

---

# §R19 반영분 (2026-08-13) — 미배치. 챗이 추후 각 절로 옮긴다

### ⓪ 라우팅 — 북극성 사슬 앞의 0번째 칸 (R17·R18)

북극성 사슬은 ①~⑧을 직렬로 그렸으나, 실물에는 그 앞에 라우팅 분기가 있고
그것이 관통의 실행 범위를 결정한다. R17 관통 1회차는 이 칸에서 ①②③④가 잘렸다.
- 신호 = supervisor fast-path 진입/미진입 (`supervisor.py:721`, 2항 AND)
- 원장에 ⓪칸을 세운다. **①의 비발화가 ①의 고장을 뜻하지 않는다.**

### 규율 — 「존재 ≠ 도달」 (R17 발, 챗 발 오판)

코드에 분기가 있다는 사실은 실행 시 그 분기를 밟는다는 뜻이 아니다.
- 발생: `R16 §4-b` 표(*"후보 2 = ①~⑧ 전부"*)를 챗이 "실행하면 밟는다"로 읽고
  진입점을 정했다. R17 에서 후보 2도 조건부로 접혔다.
- 파생 오판 2건: ④ 실색인 예상(미도달) · ⑦ 1회 발화 산술 예측(미진입)
- 규율: 분기를 「있다」고 적을 때 **도달 조건을 함께** 적는다.
  지시서가 지목한 위치도 기재분이지 확정이 아니다.

### 규율 — 「양성 대조가 0건이면 대조가 무효」 (R18 발)

0건 주장에 양성 대조를 붙이는 규율은, **대조 자체가 0건이면 무력하다.**
- 발생: `R18 A-c` 에서 `: bool$` 패턴이 0건을 내어 대조가 무효였다.
  CC 자진 검출·재측정(126건)으로 판정 유지.
- 그대로 뒀으면 "검색기가 고장난 채 낸 0건"을 "정말 없음"으로 보고할 뻔했고,
  그것이 하필 「끄는 키가 없다」 결론의 근거였다.
- 규율: 양성 대조가 0건이면 **대조 수단이 유효함을 먼저 보인 뒤** 0건을 주장한다.
- ⚠️ 특히 **코드 형식을 가정한 패턴**(타입 어노테이션·대입·데코레이터 변형)에서 자주 난다.
  R18 `: bool$`, R19 `\s*NAME\s*=` — 같은 세션에 2회. 형식을 가정하기 전에 이름별 존재를 먼저 확인한다.

### 확정 — 색인 검증축 재설계 (R17·R18)

실행 전후 원본 대조에서 sha256·mtime 을 쓸 수 없다. Chroma 가 **조회 없이 열기만 해도**
변동하며, `data_level0.bin` 해시는 열 때마다 다른 값이라 **비결정적**이다
(백업 `316d2d69` → R17 `19913cb7` → R18 `0e3a0b5c`).
- 1차축 = **청크 수** (`sqlite3 -readonly`)
- 2차축 = 파일 크기 · 파일 목록
- 폐기 = mtime
- 조건부 = sha256 — **정지 상태 대조(백업 대 백업)에는 유효.**
  R18 에서 열지 않은 NS 16파일은 4축 전부 불변.

### 정정 — `N_RESULTS 850` 오판 (챗 발, 패턴 α 5회차)

챗이 S9 예상 수집 규모를 *"17줄 × N_RESULTS 50 = 850건"* 으로 적었다.
실측은 **85건**(Tavily 가 5로 제한).
워크보드의 `N_RESULTS 50` 은 탐침 드라이버의 Chroma `n_results` 인자이며 수집 설정이 아니다.
**워크보드 기재는 정확했고, 챗이 범위를 넘겨 읽었다.**
- 근거: `R15a`
- 계통: 패턴 α(단위·범위 혼동) 5회차. 앞 4회와 달리 **챗 발**.

### 정정 — 08-06 「방식 (나) 기각」의 근거 무효 (R15b)

기각 근거였던 *"호출부가 `research_planner.py:229` 1곳이고 그 1곳이 챗 UI 본선"* 이
실물과 어긋난다. `:229` 는 호출부가 아니라 **노드 본문의 프롬프트 로드 행**이고,
진입은 LangGraph 라우터가 결정한다.
- **결론의 당부는 별개다.** 근거만 무효이며 재검토 대상으로 남긴다.
- ⚠️ `R1` 발견 2가 인용한 `:227`(env objective 순회)도 인접 행이므로 함께 의심 대상.

### 미결 — 토큰 계측 수단이 없다 (R17)

R17 관통 1회차에서 LLM 3요청의 토큰을 재지 못했다. 응답 `usage` 가 로그에 없고,
`x-ratelimit-remaining-tokens` 는 시간에 따라 회복되는 값이라 차분을 소비량으로 쓸 수 없다.
→ **북극성 성공 기준의 「$1 이내」를 현재 계측할 방법이 없다.**
관통 규모가 커지면 상한 판정이 불가능해진다.

---

# §R22 반영분 (2026-08-13) — 미배치

### 정정 — R21 정지 지점은 ⑦이 아니라 ⑧ (챗 발 지시서 결함)

⑥ 로그 `round=1/1` — `rounds_done < max_iter` 가 `1<1`=False 이고 AND 첫 항이라
`new=68` 이어도 `continue=False`. **자료 부족이 아니라 라운드 소진**이다.
`iteration_count=1` 에서 ⑦은 **원리적으로 못 선다.**
- ⑦ = 「비발화」가 아니라 **「억제(설계)」**. 진짜 정지 지점 = **⑧**
- 계통: 억제해놓고 비발화로 읽음. `R32` 의 ③④에 이어 **2회차**
- ⑦ 발화 조건 = `iteration_count ≥ 2` (R22 §4-c 로 코드 확정 예정)

### 정정 — R17 §2-d 산술 예측 오류

*"`rounds_done=0` 인 첫 판정에서 `0<1` True → ⑦ 1회 발화 예상"* 이 틀렸다.
실제 판정 시점에 이미 `rounds_done=1`. **planner 가 라운드를 먼저 올린다.**
→ 산술 예측에는 반드시 **「산술 예측」** 라벨을 붙인다.

### 규율 — 「존재 ≠ 도달」 3회차 (⑧)

`R20` 이 *"⑧은 선다"* + 「코드로는 미확정」을 병기했고 챗이 **「선다」에 걸었다.**
`R21` 실측 — **안 섰다**(`[agent.section_writer]` 0건).
`_resolve_title` 3단 폴백 + `AUTO_WRITE` 2키 True 라는 **코드 근거가 있었는데도** 도달하지 않았다.
- 1회차 = R16→R17 진입점 · 2회차 = ④ 실색인/⑦ 산술 예측 · **3회차 = ⑧**
- 이번엔 CC 가 「미확정」을 먼저 걸었고 **챗이 넘겼다**

### 규율 보강 — 로그 검색의 자기 참조 (R21 발)

로그 파일에 **LLM 프롬프트가 통째로 실린다.** 프롬프트에 과거 로그가 들어가면
**본문 문자열 검색이 자기 자신을 재귀적으로 잡는다.**
- 발생: `R21 deviation ②` — `Supervisor fast-path` grep 히트가 openai 프롬프트 페이로드
  한 줄이었다. 그대로 뒀으면 **유실 구간을 발화로 오인**할 뻔했다
- 규율: **로그 검색은 로거 태그로 앵커한다.** 본문 문자열 검색은 거짓 양성을 낸다
- ⚠️ 앞선 2건(`R18 : bool$`, `R19 \s*NAME\s*=`)은 **거짓 음성**, 이번은 **거짓 양성**이다

### 미결 — 로그 유실 (R21)

`RotatingFileHandler(1MB × 3)` 가 관통 2회차 **초기 3분 46초**를 버렸다.
1회차 26KB / 2회차 8MB+. 지시서의 *"로그 전량을 파일로"* 는 **구조적으로 불가능한 요구**였다.
- 유실 구간에 ⓪① 직접 로그가 있었다 → 간접 증거로 판정(직접/간접 구분 기재)
- 🔴 **라운드를 늘리면 더 커진다.** 관통 3회차 전에 해소 필요 (R22 §2)

### 규율 — 「게이트는 박제 앞에 온다」 (R21 deviation ① 발)

`R21` 지시서가 `tracked 등록 → 실효값 검증` 순이었으나 CC 가 뒤집어 집행했고 **그쪽이 옳다.**
검증 실패가 중단 사유인데 먼저 커밋하면 **검증 안 된 산출물이 이력에 박제**되고,
되돌리려면 revert 커밋이 또 남는다.
- 규율: 검증이 중단 사유인 항목은 **검증을 커밋·박제보다 앞에 배치**한다

---

# §R27 반영분 (2026-08-13) — 미배치 · 코드 수정 직전 상태 박제

## 🔴 데드락 — 이 트랙의 핵심 진단 (R25·R26)

`agent/supervisor.py:721` fast-path 블록 안에 **세 가지가 함께** 있다.

| 행 | 내용 |
|---|---|
| `:721` | fast-path 진입 = `extract_write_title(last_text)` **AND** `has_on_disk` |
| `:725` | 🔴 `Task(agent="section_writer", …)` — **프로덕션 유일 생성 지점** (양성 대조 53건) |
| `:731` | `requested_write_title` 세팅 |

→ **`:721` 이 닫히면 Task 와 제목이 동시에 사라지고, 열리면 ①②③④가 잘린다.**

| 문안 | ⑧ | ①②③④ | 실측 |
|---|---|---|---|
| write 어간 있음 | 🟢 | 🔴 스킵 | R17 관통 1회차 |
| write 어간 없음 | 🔴 | 🟢 | R21 관통 2회차 |

🔴 **관통 1·2회차는 우연히 반대 결과를 낸 것이 아니라, 구조가 허용하는 두 상태의 전부였다.**
이 파이프라인은 현재 **"조사하고 쓴다"를 할 수 없다.** 설정으로 나갈 길 없음 → **코드 변경 확정.**

## ⑧이 서는 최소 조건 (R26 §1-d)

> **`write:` prefix 를 가진 `section_writer` Task 1건** (+ `suppress_writer` False)

- 라우터(`core/routers.py:278-315`)는 **Task 목록만** 본다
- `flags["router"]["writer_pending"]` · `pending_write_title` · `requested_write_title` 은
  **진입에 불필요** — 주석 `routers.py:288-291` 이 *"여기서는 보지 않는다"* 로 명시
- ⚠️ `R22 §3` 의 ⑧ 진입 조건 표는 **노드 내부** 조건이다. 이 표는 **노드까지 가는** 조건 — 층이 다르다

## 「예약」의 실체 (R25 §1-c · R26 §1-e)

`schedule_writer_if_needed` 는 `router_flags["writer_pending"] = True` **flag 1개만** 세팅하고
**Task 를 만들지 않는다**(`utils/tasks.py` 내 `Task(` 0건).
→ **성공해도 ⑧은 서지 않는다.** `R22` 가 어긋난 항으로 지목한 *"`write:` pending task 0건"* 이 이것으로 설명된다.

## 죽은·미완성 경로 3건

| # | 대상 | 성격 | 차수 |
|---|---|---|---|
| 1 | `flags["force_writer"]` | 주석은 *"플래그로 무시 가능"*, **세팅 코드 0건** | R24 |
| 2 | `state["title"]` (`tasks.py:331` 제3 폴백) | 조회는 하는데 **대입 0건** | R25 |
| 3 | `flags["router"]["writer_pending"]` | 주석은 *"실제 라우팅 변경은 라우터에서"*, **그 flag 를 보는 라우터 0건** | R26 |

🔴 이 코드베이스는 **미완성 배선이 완성된 것처럼 보인다.** 셋 다 진단을 왜곡했다 —
`force_writer` 를 보고 챗은 "탈출구가 있다"고 읽었고, `:331` 폴백 때문에 "제목 후보가 셋"으로 보였다.
→ **상시 조항: 읽는 자리를 찾으면 쓰는 자리도 확인한다.**

## 동명이의 3건

| # | 표기 | 실물 | 차수 |
|---|---|---|---|
| 1 | `CFG.DIRECT_QA` | `:310` 이 읽는 것은 `flags["DIRECT_QA"]` — **다른 저장소** | R23 |
| 2 | 지역변수 `ALLOW_SUMMARY` | 실제 CFG 키는 `ALLOW_LOCAL_SUMMARY`. `CFG.ALLOW_SUMMARY` 는 `<ABSENT>` | R24 |
| 3 | `.env:179 ALLOW_SUMMARY` | Config 필드가 아니라 **ENV 폴백 이름** (`config.py:495`) | R24 |

⚠️ #2 를 변수명대로 읽었으면 `<ABSENT>` → False → *"부업이 꺼져 있다"* 는 **정반대 결론**이 나왔다.
→ **상시 조항: 지역변수·CFG 키·`.env` 키·flags 키는 이름이 같아도 다른 것일 수 있다.**

## 규율 — 「인용은 인접 행까지 열어라」 (R25 발, 2회 연속 적중)

| 회차 | 놓친 것 |
|---|---|
| R25 → R23 정정 | `R23` 이 `:330` 만 인용해 `:331` **제3 폴백**을 놓쳤다 (조회처 2곳 → 실제 3곳) |
| R26 → R25 확장 | `R25` 가 `:721`·`:731` 두 행만 인용해 그 사이 `:724-727` **Task 생성**을 지나쳤다 |

⚠️ 두 번 다 **챗이 CC 요약을 그대로 물려받아 다음 지시서에 재생산**했다.
→ 규율: 인용 시 **인접 행을 함께 연다.** 남의 요약을 범위 확정에 쓰지 않는다.

## 규율 — 「실험 설계 무효」 (R22 deviation ① 발)

`LOG_MAX_BYTES=0 → 1048576` 이 나와 *"clamp 결과"* 로 읽힐 뻔했으나, 실은 **`.env` 회귀값**이었다.
**측정하려는 기전과 다른 기전이 값을 만들어냈다** — 결과가 그럴듯했기에 더 위험했다.
「양성 대조」 규율로는 잡히지 않는다.
→ 규율: 실험 결과가 **예상과 다른 방향으로 그럴듯하면**, 같은 실행의 다른 결과와 앞뒤를 맞춰본다.

## 고정 엣지로 인한 라우터 부재 (R24 §3-b)

`R22 §3-a` 의 *"content_strategist 이후 라우터 로그 0건"* 이 해소됐다 —
`graph.py:125 _add_edge_safe("content_strategist", "communicator")` **고정 엣지**다.
**로그가 누락된 게 아니라 판정 지점 자체가 없었다.**
⚠️ 반면 `section_writer` 진입 엣지는 **전부 조건부**(라우터 6개, 고정 엣지 0 — R26 §1-a).

## 미결 — 로그 인프라 (R21·R22)

`RotatingFileHandler(1MB × 3)` 가 관통 2회차 **초기 3분 46초**를 버렸다(1회차 26KB / 2회차 8MB+).
`R22 §2` — **「주입으로는 안 된다 — `.env:32-33` 회귀」**. `min_=1024` clamp 로 **끄는 수단도 없다.**
🔴 **관통 3회차는 `iteration_count≥2` 라 로그가 더 커진다.** 코드 수정 차수에 함께 처리 필요.

## ⑦ 최대 발화 = `iteration_count − 1` (R22 §4-c, 코드 근거 판정)

`research_planner.py:416` 이 판정보다 먼저 라운드를 올리고, `routers.py:969` AND 첫 항이 `k < N`.
→ `N=1` 이면 **0회**. R21 의 ⑦ 「비발화」는 **「억제(설계)」**가 맞다.

## 진행 상태 (코드 수정 직전)

- 관통 1회차(R17) 2칸 / 2회차(R21) 7칸 — ⓪①②③④⑤⑥ 발화, ⑦ 억제(설계), ⑧ 미발화
- 새 NS `research1-through2` = web 829 / local 302. 🔴 **기존 NS 416/302 전건 불변**
- 백업 `_archive_research1/r17_pre_20260813/` · 스크래치 `r18_scratch_20260813/` 유지
- 남은 후보 = **(나) fast-path 분리** · **(라) `tasks.py:337` 이 Task 생성** — 부작용 측정이 R27

---

# §R33 반영분 (2026-08-13) — 미배치 · 🔴 9칸 성립 기준선

## 🔴 관통 7회차 — ⓪~⑧ 9칸이 한 실행에 전부 섰다

R17 2칸에서 시작한 이 트랙의 이정표다. `research1/r28-writer-fix` 브랜치.

| 회차 | ⓪① | ②③④ | ⑤ | ⑥ | ⑦ | ⑧ | 비고 |
|---|---|---|---|---|---|---|---|
| R17 (1회차) | ⓪만 | ❌ | ✅ | ❌ | ❌ | ✅ | fast-path 로 ①②③④ 스킵 |
| R21 (2회차) | ✅ | ✅ | ✅ | ✅ | 억제(설계) | ❌ | `iteration_count=1` |
| R28 (3회차) | ✅ | ✅ | ✅ | ✅ | ✅1회 | ❌ | Task 가 auto-close 로 죽음 |
| R29 (4회차) | ✅ | ❌ | ❌ | ❌ | ❌ | ✅ | Task 를 살리자 ②③④ 잘림 |
| R30 (5회차) | ✅ | ✅ | ❌ | ❌ | ❌ | ✅ | `after_web` 이 ⑤ 가로챔 |
| R31 (6회차) | ✅ | ✅ | ✅ | ✅ | ❌ | ✅ | `after_synth` 가 ⑦ 가로챔 |
| **R32 (7회차)** | ✅ | ✅ | ✅ | ✅ | 🔴 **✅1회** | ✅ | 🔴 **9칸 성립** |

### 수정 4건 (전부 분리 브랜치, `main` = `0eb3d37e` 무접촉)

| # | 커밋 | 대상 | 내용 |
|---|---|---|---|
| 1 | `c9f29dcd` | `core/config.py` | 로그 보호키 2개 추가 — 로그 유실 해소 |
| 2 | `4e43d236` | `agent/supervisor.py` | δ-1 — fast-path 밖 writer Task 생성 + `CFG.TOPIC_TITLE` 제목 |
| 3 | `2b040400` | `agent/supervisor.py` | δ-1 을 auto-close 뒤로 이동 |
| 4 | `6453bfd4` | `core/routers.py` | `after_planner` writer 분기를 쿼리 검사 블록 안으로 |
| 5 | `909aba54` | `core/routers.py` | 🔴 `has_pending` 인자 타입 오용 교정 (`state` → `tasks`) |
| 6 | `f3dcd161` | `core/routers.py` | `after_synth` writer 분기를 ⑦ 판정식 뒤로 |
| — | `2c62bd55`·`24f8fa23`·`d90d10d9`·`17ae300f`·`e9dcbd05` — **5건, 실물은 through3~7** | `topics/*.env` | 프리셋 신설 (through2~7) |

### 🔴 핵심 진단 — 하나의 Task 가 양쪽을 결정했다

`supervisor.py:721` fast-path 블록 안에 **Task 생성(`:725`)·제목 세팅(`:731`)이 함께** 있었다.
→ 블록이 닫히면 ⑧이 안 서고, 열리면 ①②③④가 잘린다.
그리고 Task 를 살리자 **라우터 3곳이 차례로 writer 를 최우선으로 잡아** 조사 단계를 가로챘다.

**이것은 세 개의 버그가 아니라 하나의 설계 습관이었다** — 라우터마다 writer 검사를 본래 검사보다 앞에 둠.
`after_planner` · `after_web` · `after_synth` 순으로 드러났고 3곳을 교정했다.

### 🔴 `has_pending` 인자 타입 오용 — 주석이 예고한 사고

`core/routers.py:565` 가 `has_pending(state, …)` 로 **dict 를 넘겼다.**
dict 순회는 키 문자열을 내고 `getattr(str,"agent","")` 가 `""` 라 **예외도 경고도 없이 항상 False.**

`:563-564` 주석이 정확히 이 사고를 예고한다 —
*"writer 펜딩이라도 vector_search 가 대기중이면 먼저 실행. 그렇지 않으면 RAG 가 있는데도
retrieval 단계가 통째로 스킵되어 빈 본문이 나간다."*

🔴 **폴백 3중이 전부 죽어 있었다** — `:586`·`:593` 이 `Task`(dataclass)를 못 잡고 `:605` 는 비어 있다.
→ **이 규칙은 만들어진 이래 한 번도 발동한 적이 없다.** 교정 후 `:842` 로그가 첫 발화 기록이다.
⚠️ `CLAUDE.md §9` 「이터러블 타입 오용 → 조용한 0건」 계통.

### ⑦의 정체 — 재수집이 아니라 재검색

`R32` 라운드 2에서 ③수집이 돌지 않았다 —
`:968 [router.after_planner] refs/docs or rag_on_disk detected → vector_search_agent`.
북극성 ⑦ 정의는 *"부족 판정 → 재쿼리 → **재수집**"* 이나, 실측된 것은 **재검색**이다.
⚠️ `R20 §3-c` 가 예측한 **2라운드 자기잠금**과 같은 현상이다. 상세 = 🔴 **재쿼리는 생성됐다.** planner 라운드 2가 objective #2 로 새 쿼리 2개(사운드 로고·분트 곡선)를 만들고 `:967 [Planner] schedule next → web_search_agent (queries=2)` 로 ③을 예약했다. **집행만 안 됐다** — `after_planner`(`routers.py:895`)가 `vector_search_agent` 로 보냈고 `WEB SEARCH AGENT` 배너는 **1건**(라운드 1)뿐이다(양성 대조: 같은 로그에 `VECTOR SEARCH AGENT` 2건 · `RESEARCH PLANNER` 2건 검출). 코퍼스는 두 라운드 모두 `web=335 local=302 base=0` 으로 **증가 0**. 🔴 **재쿼리가 ⑤ 질의로도 안 쓰였다** — `:923`·`:1034` 둘 다 원 사용자 문안이다. `R20 §3-c` 는 *"2라운드 이후 관문 1·3의 입력이 달라진다 / `iteration_count`=1 이면 이 효과가 관측되지 않는다"* 고 적었고, `iteration_count=2` 인 R32 가 **처음 관측**했다. 박제 = `R33_BASELINE.md §6`

### 남은 것 — ⑧ 본문

⑧은 **섰다**(배너·Task 확보). 그러나 4회 연속 `outline chars=0` 으로
`section_writer.py:211` 미통과 → `:222` return, `_resolve_title` 미평가.
**칸이 안 서는 문제가 아니라 노드 간 순서 문제다** — `content_strategist` 가 writer 뒤에 온다.
`R24 §3-b` 가 이미 밝혔다: **「조사 후 writer 예약 지점」이 코드에 없다.**

## 죽은·미완성 경로 5건

| # | 대상 | 성격 | 차수 |
|---|---|---|---|
| 1 | `flags["force_writer"]` | 주석은 *"플래그로 무시 가능"*, 세팅 코드 0건 | R24 |
| 2 | `state["title"]` (`tasks.py:331`) | 조회하는데 대입 0건 | R25 |
| 3 | `flags["router"]["writer_pending"]` | 주석은 *"라우팅 변경은 라우터에서"*, 보는 라우터 0건 | R26 |
| 4 | auto-close B블록 (`supervisor.py:793-800`) | A블록이 먼저 return — 도달 불가 | R29 |
| 5 | 🔴 `has_pending` 폴백 3중 (`routers.py:586`·`:593`·`:605`) | 옛 형식만 인식 — **셋 다** 죽음 | R31 |

## 동명이의 6건

| # | 표기 | 실물 | 차수 |
|---|---|---|---|
| 1 | `CFG.DIRECT_QA` | `:310` 은 `flags["DIRECT_QA"]` 를 읽음 | R23 |
| 2 | 지역변수 `ALLOW_SUMMARY` | 실제 CFG 키는 `ALLOW_LOCAL_SUMMARY` | R24 |
| 3 | `.env ALLOW_SUMMARY` | Config 필드가 아니라 ENV 폴백 이름 | R24 |
| 4 | `CFG.TOPIC_TITLE` | `state["topic_title"]` 은 다른 저장소 — 같은 실행에서 값이 달랐다 | R28 |
| 5 | `state["qa_direct_reply"]` | `flags["qa_direct_reply"]` 와 다른 저장소 | R28 |
| 6 | 🔴 `BLOCKAGI_OBJECTIVE_N` | CFG 필드는 `RESEARCH_OBJECTIVES`(`config.py:555`) | R32 |

⚠️ **`state` 최상위 ↔ `flags` 하위 조합이 상습**(#1·#5). 그 형태를 먼저 의심한다.
⚠️ 정확한 표기 = 🔴 **층이 넷이다.** ① 프리셋/`.env` 키 **`BLOCKAGI_OBJECTIVE_<n>`**(n=1..5) → ② 변환 `load_research_objectives_from_env()`(`core/config.py:182..224`, `prefix` 는 `:183`) → ③ **CFG 정본 필드 `RESEARCH_OBJECTIVES`**(선언 `:313` / 대입 `:555`) → ④ **state 키 `research_objectives`**(소문자 — `state_types.py:104` 선언, `topic.py:154` 대입, `routers.py:799`·`rag_utils.py:703` 읽기). ⚠️ **`CFG.BLOCKAGI_OBJECTIVES`(복수형·`str`, 선언 `:332` / 대입 `:588`)는 별개 레거시 필드**이며 현행 값은 `''` 다. *"objective 5키"* 는 ①만 가리키는 표현이다. R32 오독의 원인 = `dir(CFG)` 를 **`.env` 키 이름**으로 걸렀다(CFG 에는 번호형이 없다).

## 규율 — 「범위를 자르지 마라」 (4회, 마지막이 가장 나쁨)

| 회차 | 놓친 것 | 범위를 정한 쪽 |
|---|---|---|
| R25→R23 | `:330` 만 보고 `:331` 제3 폴백 | CC 요약을 챗이 물려받음 |
| R26→R25 | `:721`·`:731` 만 보고 `:725` Task 생성 | 상동 |
| R28→R27 | `:743` 에서 끊어 `:775` auto-close | 🔴 챗이 지시서에 범위 명시 |
| **R30** | `:865-871` 7줄 창으로 보고 **순서 교정안을 설계** | 🔴 **챗. 안이 무효였고 CC 가 집행 전 반증** |

🔴 앞의 셋은 **놓친 것**이고 마지막은 **틀린 것을 지시한 것**이다. 유료 1회를 태울 뻔했다.
→ 규율: **함수를 고치는 안을 낼 때는 그 함수 전문을 먼저 받는다. 인용된 조각으로 설계하지 않는다.**
→ 지시서에 줄 범위를 박는 것 자체를 위험 신호로 본다.

## 규율 — 「실험 설계 무효」 6회 (변종이 매번 다름)

측정하려는 기전과 **다른 기전이 값을 만들어낸다.** 결과가 그럴듯하기에 더 위험하다.

| 회차 | 변종 |
|---|---|
| R22 | `.env` 회귀값을 clamp 결과로 오독 |
| R28 | BOM 을 파일 훼손으로 오독 (`ast.parse` 실패 / `import` 성공 모순으로 잡음) |
| R29 | 오프셋 가정 대조가 2행 어긋남 → 앵커 방식으로 재측정 |
| R31 | 테스트 픽스처(`refs_docs=1`)가 결과를 만듦 |
| R32 Ph1 | 🔴 **채점표가 틀림** — 코드가 아니라 기대값이 반대 |
| R32 Ph2 | 탐침 키 이름 오류로 objective 0개 오독 |

→ 규율: 결과가 예상과 다르면 **같은 실행의 다른 결과와 앞뒤를 맞춘다.** 대상보다 자를 먼저 의심한다.

## 함정 — `core/routers.py` 는 CRLF

1,025행 전부 CRLF. `splitlines()` 로 읽으면 `\r` 이 지워져 **박제 대조가 전건 불일치**한다.
→ 이 파일 편집·박제는 **바이트 분할**(`split(b"\n")`)로. 편집 전/후/커밋 후 줄끝 3회 확인.
⚠️ `.gitattributes` 부재 · `autocrlf` 미설정.

## 검증 방법론 — 3단계로 진화했다

| 단계 | 형태 | 차수 |
|---|---|---|
| 1 | 코드를 읽어 판정 | ~R29 |
| 2 | 함수 직접 호출 + **양방향**(고쳐진 것 / 안 깨진 것) | R30·R31 |
| 3 | 🔴 **구·신 모듈 동시 적재** — 같은 입력을 양쪽에 먹여 대조 | R32 |

+ 부작용 함수 **스텁으로 차단**해 색인·파일 쓰기 0 유지. 안전장치가 실제로 죽는지 역방향 확인.

## 원칙 검증 — 선제 수정 금지가 실측으로 뒷받침됐다

`R32 Phase 0` 이 writer 우선 분기를 전수해 **잔여 2곳**(`after_web:569`·`after_vector:759`)을 찾았으나
**관측된 `after_synth` 만 고쳤다.**
→ `R32` 관통 7회차 실측: 잔여 2곳 **발화 0건**(하나는 탈출구로, 하나는 도달조차 안 함).
**미관측 경로를 안 고친 판단이 맞았다.**

## 지표 앵커 정정

`R31` 보고의 「LLM 5건」은 **배너 기준**이었다. 같은 앵커로 재측정 시 **R31 76 / R32 73**, 임베딩 142 / 130.
→ 🔴 **회차 간 비교가 흔들렸다.** `catch CL`(지표가 무엇을 세는지 밝힌다) 계열.
→ 규율: 실적표에 **측정 앵커를 함께 적는다.** 형식 = 값 뒤 괄호에 **`앵커 문자열` / 로그레벨 / 전수·하한** 3항. 예: ``LLM 73 (앵커 `Request options` / DEBUG / 전수)``.
앵커 정본 — LLM `Request options`(DEBUG) · 임베딩 `POST https://api.openai.com/v1/embeddings`(DEBUG) · 검색 `call ok`(INFO) · 노드 `============ <NODE> ============`(INFO).
🔴 관통 1~7회차 로그 **전건 존재**(「로그 부재」 0건)이고 **DEBUG 행도 전건 존재**(46/139/586/30/444/571/575)하여 앵커가 전 회차에 유효하다.
전건 재측정 (LLM / 임베딩 / 검색) — R17 **3/0/0** · R21 **≥59/≥112/≥1** · R28 **73/130/8** · R29 **2/0/0** · R30 **60/114/7** · R31 **76/142/8** · R32 **73/130/8**.
⚠️ **R21 만 `≥` 다** — 로그가 회전 4조각이고 최초 조각이 유실됐다(조각별 20/19/12/8). **전수가 아니라 하한값**이다.

## 상태 (9칸 성립 시점)

- 브랜치 `research1/r28-writer-fix` · `main` = `0eb3d37e` **무접촉**
- 색인 = 전 NS 실측(`sqlite3 -readonly`, 1차축 = 청크 수) — `research1-through2` **829/302** · `through3` **309/302** · `through4` **0/0** · `through5` **332/302** · `through6` **378/302** · **`through7` 335/302**(7회차 신설) · `venfobel-vitamin` **47/810** · `_empty_ns_20260802.bak` **0** · `base` sqlite 없음
- 🔴 **기존 NS `experiential-marketing-media` web 416 / local 302 — 7회 관통 전부 불변**
- 관통 7회차 산출물 = `outlines/…/outline_report.md` **367 B** · `research/…/round-01-findings.md` **2,257 B** · `round-02-findings.md` **2,641 B** · `reports/…/qa/qa_…_222908.md` **539 B**(qa 1건뿐 — **본보고서 없음**) · `research/…/state/last_state.json` 41,940 B · `resources/…` 10건 ≈ 41.4 MB · 🔴 **`sections/research1-through7/` 디렉터리 부재 — 섹션 본문 0건** · refs `docs=5` `queries=1`
- 백업 `_archive_research1/r17_pre_20260813/` · 스크래치 `r18_scratch_20260813/` 유지

---

# §R37 반영분 (2026-08-13) — 미배치 · 🔴 ⑧ 본문 성립 기준선

## 🔴 관통 8회차 — ⑧이 본문을 썼다

9칸(R32)을 유지한 채 ⑧이 본문을 산출했다. 이 트랙의 두 번째 이정표다.

| 회차 | ②③④ | ⑤ | ⑥ | ⑦ | ⑧발화 | ⑧본문 |
|---|---|---|---|---|---|---|
| R32 (7회) | ✅ | ✅ | ✅ | ✅ | ✅ | ❌ |
| **R36 (8회)** | ✅ | ✅ | ✅ | ✅ | ✅ | 🔴 **✅** |

산출물 = 🔴 **`sections/research1-through8/` 2건** — 본문 `event-exhibition-tickets-행사-티켓-광고-체험-효과-측정-관리-고객-여정-맵.md` **5,076 B** + 동 `.refs.json` **4,326 B** · `outlines/…/outline_report.md` **371 B** · `research/…/round-01-findings.md` **2,184 B** · `round-02-findings.md` **2,510 B** · `reports/…/qa/qa_…_002604.md` **565 B**(🔴 **본보고서 없음**) · `research/…/state/last_state.json` 32,592 B · `resources/…` 10건 ≈ **131.4 MB** · refs `docs=3` `queries=1`

### 진단 — 순서였다 (R34)

`section_writer` 진입 **22:29:07** / `Outline saved` **22:29:08** — **1초 차**.
`path=(none)` 은 `core/paths.py:313 return "", None` — 그때 파일이 없었다.
**연결 문제가 아니라 순서 문제**임이 양방향으로 증명됐다(같은 state 재호출 시 `chars=195`,
없는 슬러그는 R32 와 동일한 실패 모양).

### 세 겹 잠금 (R34)

`:218 pending.done = True`(Task 사망) · `:214` 재예약 가드 · `graph.py:125` 고정 엣지 → END.
**목차를 만든 뒤 writer 로 돌아오는 경로가 없다.** `content_strategist` 는 writer Task 를 만들지도,
`pending_write_title` 을 세우지도 않는다.

### 해법 (E) — writer 안에서 노드를 통째로 호출 (R35·R36)

**나가지 않으면 돌아올 필요가 없다.** 후보 6건 중 유일한 단독 해결.
- 아웃라인 전용 헬퍼는 없다 — 생성 사슬이 노드 본문에 인라인
- 시그니처가 `content_strategist(state)` 단일 인자이고 writer `:201` 에서 이미 손에 있다
- 반환 dict 는 **버린다** — 얕은 복사(`:39-40 list(...)`)라 유실은 communicator Task 1건
  (writer `:495` 가 어차피 만듦) + AIMessage 1건뿐
- 🔴 **예약 제거는 비용 열쇠가 아니었다** — 라우터는 Task 를 안 보고 `outline_missing`(`:374`)만
  본다. 아웃라인이 있으면 그 길이 안 열린다. 제거는 **원장 위생**이다
- 실측 — `content_strategist` **1회**. (E)의 비용 목표 달성

### 🔴 실패 경로 브레이크 — 챗 결함이었다

챗 단서가 *"`:218 pending.done = True` 는 유지 불요(`:490-493` 이 종료 처리)"* 라고 적었으나
**그것은 본문 경로의 사실**이고 실패 경로는 `:490-493` 에 닿지 않는다.
→ 제거하자 루프가 성립했다: 실패 → Task 미완료 → `tail:362` True → `:366` refs 통과 →
`:370` writer 재진입 → 아웃라인 여전히 없음 → LLM 반복.
**8케이스 실측으로 유료 실행 전에 잡았고**, 실패 경로에만 복원했다(성공 경로 무접촉).

## 🔴 `_resolve_title` 첫 평가 — 그리고 제목 오염

`R21`~`R32` 7회차 내내 **0회**였다(`R34 §2-c-3`). R36 이 첫 평가다.

**2단에서 났으나 값이 오염됐다.**

```
제목 = "-event -exhibition -tickets -행사 -티켓 -광고', '체험 효과 측정 관리 고객 여정 맵"
```

`get_last_write_target` 이 `reversed(tasks)` 를 도는데 `vector_search_agent` Task 의
`queries=[…]` 문자열이 `section_writer` 의 `write:` Task 보다 **먼저 걸렸다.**
`_RE_WRITE_KO_DEFAULT` 가 쿼리 안의 *"…고객 여정 맵 작성법…"* 을 작성 요청으로 읽었다.

🔴 **우리가 만든 결함이 아니다.** 원래 있던 함수이고, 이 트랙에서 **처음 실행되어 드러났다.**
→ 🆕 **규율: 죽은 경로를 살리면 그 안의 결함도 함께 깨어난다.** 다른 죽은 경로를 살릴 때 예상한다.

## 남은 것 (완주까지 2건)

| # | 항목 | 근거 |
|---|---|---|
| 1 | **제목 오염** | 위 |
| 2 | 🔴 **섹션 1/7 · 본보고서 부재** | writer 는 자기를 재예약하지 않는다. 배너 1회, `진행률: 1/7`,<br>이후 tail → communicator → END. `R35 §4-4` 코드 근거가 **실측으로 확인**됐고<br>*"관측된 적 없다"* 단서가 해소됐다 |

⚠️ 2번은 `R24 §3-b`(*"조사 후 writer 예약 지점이 없다"*)와 같은 뿌리다.

## 실적 (앵커 `Request options` / DEBUG / 전수)

| | R32 | R36 |
|---|---|---|
| LLM 전체 | 73 | **92** |
| ├ 수집 구간 | 59 | 75 |
| └ 나머지 | 14 | 17 |
| `content_strategist` | 1 | **1** |
| `section_writer` 본문 | — | 1 |
| 임베딩 | 130 | 162 |
| 새 NS `-web` | 335 | 476 |

⚠️ **두 실행은 웹 수집 결과가 다르다. 총량 대조를 인과로 읽지 않는다.** 수치만.

## 규율 신설 3건

| 규율 | 발 | 내용 |
|---|---|---|
| **인과를 적기 전에 반례를 찾아라** | R35 | `R34 §6` 이 *"return 에 flags 가 없어서"* 로 인과를 적었으나 **똑같이 안 넘기는데 살아남는 값**이 있었다(`vector_search` 2건). 상관을 인과로 적지 마라 |
| **검증 픽스처는 실물 규모를 반영하라** | R36 | Phase 1 픽스처 `task_history` **2건** / 실물 **8건**. 리스트를 순회하는 함수는 **길이와 순서**가 결과를 바꾼다. 제목 예측이 이것 때문에 빗나갔다 |
| **구·신 대조 시 「구」의 커밋 해시를 명시하라** | R36 | Phase 1 커밋으로 HEAD 가 이동해 「구」가 이미 신 코드였다. **HEAD 상대 지정 금지** |

## 「실험 설계 무효」 8회 — 변종 누적

R22 `.env` 회귀값 / R28 BOM / R29 오프셋 가정 / R31 픽스처가 신호 생성 /
R32 채점표 반대 / R32 탐침 키 이름 / **R36 기준선 이동** / **R36 픽스처 규모**.
→ 측정 대상보다 **자와 기준선**을 먼저 의심한다.

## flags 갈림점 — 미확정 유지 (R35)

살아남은 값은 `setdefault`(in-place, `vector_search.py:846`) 산출,
유실된 값은 `dict()` 복사(`content_strategist.py:218`) 산출.
5건이 이 기준으로 완전히 갈렸으나 **LangGraph 채널 병합 규칙을 코드로 확인하지 않았다.**
🔴 **상관이며 인과 미확정.** 어느 쪽으로도 밀지 않는다.
⚠️ (E) 경로에서는 무관하다 — `outline_text` 는 지역 변수, 저장은 디스크 경유.

## 🔴 정정 — `§R33 반영분` 챗 오기 2건

| 위치 | 오기 | 실물 |
|---|---|---|
| §R33 「수정 4건」 헤딩 | 4건 | **6건** (표도 6행) |
| §R33 「프리셋 (through2~7)」 | through2 포함 | 이 범위는 **through3~7 5건**. through2 는 `1c200d65` 로 **기준선 이전** |

⚠️ `R33` 시점에 CC 가 *"지시서와 실물이 어긋난다"* 로 정확히 보고했고 문안을 고치지 않았다.
**이 절이 정정본이다.**

## 상태 (⑧ 본문 성립 시점)

- 브랜치 `research1/r28-writer-fix` · `main` = `0eb3d37e` **무접촉**
- 커밋 = 🔴 **15건 · 전건 단일 파일 커밋(섞인 커밋 0)**. 코드 8 · 프리셋 6 · 문서 1.
  오래된 순 — `2c62bd55`(through3) · **`c9f29dcd`**(config 로그 보호키) · **`4e43d236`**(supervisor δ-1) ·
  **`2b040400`**(δ-1 이동) · `24f8fa23`(through4) · **`6453bfd4`**(after_planner) · `d90d10d9`(through5) ·
  **`909aba54`**(after_web has_pending) · `17ae300f`(through6) · **`f3dcd161`**(after_synth) ·
  `e9dcbd05`(through7) · `7d973123`(§R33 워크보드) · **`d26f4512`**((E)안) · **`e2c53165`**(R36-b 복원) ·
  `7a5c2b2e`(through8).
  `main` 대비 **11파일 · +419 / −36** (수정 5 · 신설 6). 코드 수정분만 **4파일 · +68 / −36**
- 색인 = 전 NS 실측(`sqlite3 -readonly`, 1차축 = 청크 수) — `through2` **829/302** · `through3` **309/302** ·
  `through4` **0/0** · `through5` **332/302** · `through6` **378/302** · `through7` **335/302** ·
  **`through8` 476/302**(8회차 신설) · `venfobel-vitamin` **47/810** · `_empty_ns_20260802.bak` **0** ·
  `base` sqlite 없음
- 🔴 **기존 NS `experiential-marketing-media` web 416 / local 302 — 8회 관통 전부 불변**
- 병합 재료 = `R37_BASELINE2.md` §3
---

# §R45 반영분 (2026-08-13) — 미배치 · 🔴 근거 사슬 성립 기준선

## 🔴 관통 10회차 — `.refs.json` 사이드카가 처음 생성됐다

**43차수 만에 `catch BC`·`BO`·`BR` 을 수치로 잴 수 있게 됐다.**
그동안은 *"refs 0인데 그럴듯한 본문"* 을 **현상으로만** 관측했다.

| 관측 | R42 (9회차) | **R44 (10회차)** |
|---|---|---|
| 본문 `[[N]]` | 0건 | 🔴 **3건** (`[1]` 형식 0) |
| `.refs.json` | 미생성 | 🔴 **생성 · markers=3** |
| 각주 대응 | 2건, `[3]` 고아 | 🔴 **3/3, 고아 0** |
| legacy 각주 경로 | 독립 실행 | 🔴 **skip 걸림** (배선 수정 없이 해소) |
| *"참고자료 N건에 근거"* | 0건 | 🔴 **발화, N=3** |

사이드카에 **URL + 청크 풀텍스트**가 들어간다 → **마커 → URL → 원문 역추적이 디스크에서 성립.**
산출물 실측 = `outlines/research1-through10/outline_report.md` **214 B**(H2 5) · `sections/research1-through10/경험적-소비와-경험경제-이론-비교.md` **3,564 B**(본문 853자+각주 3) · 동명 `.refs.json` **9,628 B** — 키 **3**(`1`·`2`·`3`), **URL 3/3 보유**, 청크 풀텍스트 **324 / 265 / 1,920자 (3/3 비지 않음)**, `summary` 필드도 3/3 채워짐(백그라운드 요약 스레드 완료, 84/55/81자). 로그 `R44_run_20260815-210523.applog` 3.77 MB·1,089행

⚠️ **`N=3 ≠ refs 5` 는 어긋남이 아니다.** 문안이 요구한 것은 **「근거한 건수」**(사용량)이며
인용 마커 수와 정확히 일치한다. **공급 5 중 2건을 안 썼다는 사실이 본문에 적혔다.**
→ 공급량이 아니라 **사용량**이 기록되므로, 그 격차 자체가 계측값이 된다.

## 리서치 프롬프트 트랙 (R41~R44)

**광고 프롬프트와 완전 분리.** 기존 `report`·`book` 분기 **무접촉**.

| 항목 | 실물 |
|---|---|
| 신설 파일 | `prompts_research.py` (LF) |
| 분기 키 | `RESEARCH1_PROMPT_TRACK` — **프리셋에만.** `.env` 무접촉 |
| 갈리는 곳 | 🔴 **노드**(`content_strategist`·`section_writer`). `prompts.py` 아님 —<br>`_get_cfg_attr` 에 **ENV 폴백이 없다**(`R41 Phase 0` 실측) |
| truthy | 두 노드 **일원화** — `config.truthy`. `catch 71` 계열 선제 차단 |
| `DOC_MODE` | 🔴 **값을 늘리지 않았다.** `R39 §2` — 접힘 방향 불균일(4곳 book) + `.env:59` 전 트랙 공유 |

### 성격 전환 (챗 문안 · 사용자 도메인 판단)

| 축 | 광고 기획서 | **리서치 리포트** |
|---|---|---|
| 성격 | 설득 | 사실 중심 · 발견 정리 + 시사점 |
| 자료 | 없어도 *"반드시 수치 포함"* | **인용 강제. 없으면 「없다」고 명시** |
| 분량 | 800~1,500 고정 | **자료량 가변** |
| 결론 | 권고 3~5개 | **시사점** (권고 아님) |
| 어조 | 설득 | 서술 중심 · 판단 유보 |

## 🔴 `catch BC` 의 뿌리가 프롬프트에 있었다 (R41 Phase 0)

| | 실물 |
|---|---|
| 조장 | `prompts.py:521` 분량 800~1,500 · `:535` *"시장 규모·성장률 반드시 포함"* · `:536` *"경쟁 브랜드명 반드시 명시"* |
| 부재 | 🔴 **자료 부족 시 지침 0건.** 다른 프롬프트 4곳엔 있다(`chapter_writer:350-351`·`synthesizer:665`·`paper:455`) |
| refs 비었을 때 | 프롬프트에 `'Queries:\n(none)'` **15자**만. **「자료 없음」이 통보되지 않는다** |

→ 자료 없이 *"수치를 반드시 넣어라"* 를 요구받고, 없을 때의 지침은 없었다.
**배선 문제도 검사망 사각도 아니었다.** 40차수를 배선으로 다뤘다.

## H2 5 = objective 5 (R42) — 인과 실측 확인

`R39 §5-d` 가 *"`prompts.py:135` 한 줄이 `Executive Summary` + `실행 로드맵/KPI` 를 강제 → 7 = 5 + 2"*
로 지목했고, **배선을 한 줄도 안 바꾼 채 문안만 교체하니 5 = 5 가 됐다.** 상관이 아니라 인과였다.
신설 문안 규칙 5개 전건 준수(최장 17자·기호 0 — 조립기 slug 매칭용).

## 🔴 R40 δ 가 실전에서 필요했다 — 2회 연속 (R42·R44)

```
stage-2 title not in outline → stage-3
```
🔴 **2단이 낸 값이 오염이 아니라 정상 제목이었는데 아웃라인에 없어 버려졌다.**
→ `R40 §2-c` 의 *"α·β는 오염은 없애지만 매칭이 성립하지 않는다"* 판정이 **실물로 증명**됐다.
조립기 규칙 매칭 **5/5**(`R39 §6-4` 의 0/7 해소). 실재 매칭 1/5 — **섹션 반복 미구현**.

## 남은 것 (완주까지 2건)

| # | 항목 | 근거 |
|---|---|---|
| 1 | **섹션 반복** — 현재 1/5 | `R38` — 자기 엣지는 있으나 `need_outline_display` 가 먼저 걸려 미진입.<br>🔴 **이 레포에 반복 전례 0. 신설이다**(`chapter_writer` 도 반복문 0) |
| 2 | **본보고서 배선** | `R38 §4` — `report_builder.py:279 build_final_report` **코드는 있다. LLM 0.**<br>🔴 **사용자 명령 전용**이고 그래프·라우터가 부르는 곳 **0건. 배선 부재다** |

⚠️ `R2c_NORTH_STAR.md:13-18` 정본 = **"한 번의 명령으로 완주"**. 별도 명령을 요구하는 현 구조는 그 정의와 어긋난다.

## 미확정 2건

| # | 항목 | 왜 미확정 |
|---|---|---|
| 1 | **본문 길이 ↔ refs 연동** | 섹션이 1개뿐이라 상관을 잴 수 없다. 표본 1 대 1(`R42` 1,100자 → `R44` 853자)을 경향 주장에 쓰지 않는다. **섹션 반복 후 처음 잴 수 있다** |
| 2 | flags 갈림점(`setdefault` ↔ `dict()`) | LangGraph 채널 병합 규칙 미확인 (`R35`) |

## 🔴 objective 1 미충족 — 수집 결함이며 지침은 옳게 작동했다 (R44)

objective 1이 요구한 **홀브룩·파인&길모어·슈미트 3계보가 본문에 없다.**
🔴 **코퍼스에 원 논문이 없다.** *"확인되는 것만 쓴다"* 를 지켜 **없는 이론을 지어내지 않았다.**

⚠️ 이것이 이 트랙의 실질 성과다 — **미달이 드러나는 것이 정상이다.**
조작으로 메워지면 미달인지도 알 수 없다. → 처분은 **코퍼스 보강**이지 프롬프트가 아니다.

## 🔴 실적 계수 — 앵커 없는 수치는 기재하지 않는다

`R42` 보고의 「LLM 54 · 임베딩 90」이 **다른 기준**이었다.
같은 로그를 `Sending HTTP Request: POST` 기준으로 재측정하면 **9 · 45**. `R44` 는 **12 · 64**.
🔴 **54 → 9. 6배 차이다.**

⚠️ `R32` 의 「LLM 5건」(배너 기준)에 이어 **두 번째**이며, 두 번 다 **챗이 그대로 받았다.**
→ 규율: **실적 수치는 앵커 문자열을 병기하지 않으면 기재하지 않는다.**
앵커 없는 수치는 회차 간 비교에 쓸 수 없다.
⚠️ 웹 수집량이 회차마다 다르므로(`R44` 130→321 청크) **증가분을 프롬프트에 귀속할 수 없다.**

## 🔴 `ahead` — 기준선이 둘이다 (R44 상태확인)

`git status` 의 *"ahead by N"* = **원격 대비**. `main..HEAD` = **main 대비**. 실측 시점 **6 vs 22**.
→ 이것이 `ahead` 가 15↔16↔17↔22 로 계속 어긋난 이유다. 절대값이 아니라 **기준선이 둘**이었다.
→ 규율: **`ahead` 는 기준선을 명시하지 않으면 쓰지 않는다. 원장에는 커밋 목록만 남긴다.**

## 정정 — CRLF 실측 (R41)

`§R37` 이 *"CRLF 4파일"* 로 적었으나 **전수는 CRLF 61 / LF 71 / 혼재 0.**
⚠️ `R40` 이 3건만 실측하고 넘겼고, **챗이 그 표본을 「4건」으로 확정형으로 옮겼다** — 표본을 전수처럼 씀.
⚠️ `agent/rag_expression.py` 는 없다. **루트에 있다.**
**신설 파일 관행 = LF**(전례 8건, `paper_section_writer.py` 포함).

## 정정 — objective 는 이름이 넷이다 (R33·R40)

| 층 | 이름 |
|---|---|
| `.env`/프리셋 | `BLOCKAGI_OBJECTIVE_<n>` |
| 변환 | `load_research_objectives_from_env()` (`config.py:182-224`) |
| CFG 정본 | `RESEARCH_OBJECTIVES` (선언 `:313` / 대입 `:555`) |
| state | `research_objectives` |
| 별개(유사품) | `CFG.BLOCKAGI_OBJECTIVES` (복수형·str, 현행 `''`) |

## 규율 신설 3건

| 규율 | 발 | 내용 |
|---|---|---|
| **선언은 실물이 아니다** | R39 | `paper_section_writer` docstring 의 *"LangGraph entry point"* 는 **한 번도 등록된 적이 없었다.** 주석·docstring·타입 힌트를 근거로 쓰지 마라 |
| **하네스도 위치를 세라** | R43 | 검증 코드가 **첫 일치·추측 인덱스**를 쓰면 가짜 실패/가짜 성공이 난다. 한 차수에 3건(정의행을 호출부로 오인 · `__pycache__` 미제외 · `L[86:90]` 추측) |
| **부작용 차단은 스텁 + 트리 스냅샷 양쪽으로** | R41 | 스텁은 **설계자가 아는 경로만** 막는다. `_record_llm_call` 누락으로 `metrics.ndjson` 5행이 샜고, **트리 스냅샷이 잡았다** |

## 세션 기록 — `/compact` · auto-mode (2026-08-13)

CC 세션이 `--resume` → `/compact` 를 거쳤다. 직후 `/auto-mode-setup` 확인 화면이 떴고 **`Discard and exit`** 했다.
⚠️ auto-mode 는 **STOP 게이트를 없앤다.** 이 트랙이 43차수를 버틴 구조와 정면 충돌하며,
집행 전에 잡은 것이 최소 3건이다(`R30` 무효 순서 교정 · `R36` 루프 위험 · `R41` 문안 이모지).
🔴 **유료 실행이 자동화되면 오발이 곧 과금이다.** 잔재 확인 = **잔재 0.** `writer_project/.claude/` · 레포 루트 `.claude/` **디렉터리 자체가 부재**(양성 대조 = `~/.claude/` 는 존재). `~/.claude/settings.json` 은 **23 B `{"theme":"light"}` 뿐** — `autoMode`·`permissions`·`soft_deny` **전건 0건**
⚠️ 제안 중 soft block 2건(`git add -A` · `rm -rf scripts/output/*`)은 **타당하다.** auto-mode 와 별개로 검토.

## 상태 (근거 사슬 성립 시점)

- 브랜치 `research1/r28-writer-fix` · `main` = `0eb3d37e` **무접촉**
- 커밋 = **25건**(`0eb3d37e..HEAD`, R45 커밋 전 기준). 🔴 **전건 단일 파일 커밋**(`--name-only` 전수, 섞인 커밋 0 — `catch AS`). 구성 = 프리셋 8 · `section_writer.py` 5 · `prompts_research.py` 4 · `routers.py` 3 · `supervisor.py` 2 · `WORKBOARD.md` 2 · `config.py` 1
- 색인 = 전 NS 실측(`sqlite3 -readonly`) — `experiential-marketing-media` **web 416 / local 302** · `through2` 829/302 · `through3` 309/302 · `through4` **0/0** · `through5` 332/302 · `through6` 378/302 · `through7` 335/302 · `through8` 476/302 · `through9` **130/302** · **`through10` 321/302**(10회차 신설) · `venfobel-vitamin` 47/810 · `_empty_ns_20260802.bak` 0 · `base` sqlite 없음
- 🔴 **기존 NS `experiential-marketing-media` web 416 / local 302 — 10회 관통 전부 불변**
- 백업 `_archive_research1/r17_pre_20260813/` · 스크래치 `r18_scratch_20260813/` 유지
- `.gitignore` — `!topics/research1-*.env` 예외 = **추가함.** `.gitignore:81 !topics/research1-*.env` (`:75 topics/*.env` 아래). 판정 = `check-ignore -v` 의 **매치 규칙 줄**로 했다 — `research1-*` → `:81` 예외 / `experiential-marketing-media`·`venfobel-vitamin`·`pet-food-premium` → `:75` 차단 유지 / `academic-*` → `:78` 불변. `git status` 신규 출현 **0**(9건 전부 이미 tracked)
---

# §R45-a 정정 (append)

## 반영분 헤딩 날짜 — 챗 문안 결함

`§R33`·`§R37`·`§R45` 반영분 헤딩의 `(2026-08-13)` 은 **챗이 지시서를 쓴 날**이며
**집행일이 아니다.** `§R45` 실집행일은 **2026-08-15**(`R45` deviation D-1 자진 보고).
`§R33`·`§R37` 도 같은 성질이며, 정확한 시점은 각 반영분의 커밋 해시로 확인한다.

🔴 **규율: 원장 문안에 날짜를 박지 않는다.**
챗은 지시서 작성 시점의 날짜를 쓰는데 CC 의 집행 시점은 알 수 없다(세션 중단·재개가 있었다).
→ 시점 표시는 **차수 번호 + 근거 커밋 해시**로 한다. 커밋 해시는 흔들리지 않고
`git log` 로 날짜를 언제든 뽑을 수 있다.

## 🔴 `git check-ignore` 종료 코드 함정 (`catch BB` 재현, R45)

- **부정 규칙(`!`) 매치에도 `rc=0`** 을 낸다 → 「차단됨」과 「예외라 통과」가 rc 로 **구분되지 않는다**
- 🔴 **이미 tracked 인 실존 파일**에 물으면 `rc=1` — 추적 경로를 건너뛴다.
  **같은 도구가 대상 상태에 따라 다른 답을 낸다**
→ 판정은 **가상 경로**로 하고, **매치된 규칙 줄을 읽어** 갈라야 한다. **rc 만 보면 정반대로 읽는다.**
⚠️ 지시서가 *"`check-ignore -v` 로 양방향 확인"* 이라고만 적었다 — 챗 지시의 불충분이다.

## 정정 — 프리셋 수 (R45 D-2)

지시서 §3 의 *"프리셋 10개"* → 실물 **9개**(`through2~10`). 규칙 동작에는 무영향.
⚠️ 챗이 수치를 확정형으로 쓰다 틀린 사례가 누적된다 —
`ahead` 15↔16↔17↔22(기준선 둘) · CRLF 4↔61(표본을 전수로) · 프리셋 10↔9(셈).

## 🆕 사이드카 `summary` 필드 (R45 §2-c)

`R44` 사이드카는 마커→URL→원문**+요약**까지 갖췄다(`summary` 3/3, 84/55/81자).
`section_writer.py:415` 가 띄운 **백그라운드 데몬** 산출이며, 종료 전에 완주했다.
⚠️ 🔴 **섹션이 늘면 데몬이 종료 타이밍과 경합할 수 있다.** 섹션 반복 후 **전건 충족 여부를 관측한다.**

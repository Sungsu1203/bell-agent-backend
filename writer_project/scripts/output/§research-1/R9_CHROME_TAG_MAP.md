# R9 — 크롬 문구 HTML 꼬리표 판별 (§research-1 선분 3 / 작업 3)

- 작성 2026-08-09 / 채널 = 기술 정밀 / **유료 API 호출 0 · 파이프라인 `.py` 수정 0**
- 근거 지시문 `CC_HANDOFF_20260808_segment3_S0-S2.md` 작업 3 (**2026-08-08 개정표 적용**)
- 입력 = `R8_CHROME_LEXICON.md` (커밋 `40931d6f`)
- 기준 HEAD = `40931d6f`

---

## 0. 🔴 이 재료는 원장 밖이다

작업 3에서 받아온 HTML은 **색인 X·Y 어디에도 들어가지 않는다.** 규칙 초안용 별도 재료다.

| 항목 | 값 |
|---|---|
| 저장 위치 | `scripts/output/§research-1/_s2_probe_html/` (`resources/` **아님**) |
| git 차단 | `writer_project/.gitignore` `scripts/output/**/_s2_probe_html/*.html` |
| 차단 방식 | **확장자 패턴.** 디렉터리 통째 차단(`_s2_probe_html/`)은 쓰지 않았다 — 하위를 `!`로 되살릴 수 없다(`CLAUDE.md §4`) |
| 전 레포 `*.html` | **쓰지 않았다** — `writer_project/User Guide/USER_GUIDE.html`이 추적 중이라 오염된다 |

신규 수집 1회로 만든 재료가 색인 X·Y의 유일한 입력이어야 "재료 물리적 동일"이 성립한다.
여기서 받은 HTML이 섞이면 그 전제가 흐려진다.

---

## 0-a. 선행 1건 — 원자료 박제 (완료)

R8의 유일한 뿌리가 `.gitignore:84`로 커밋에서 빠진 로컬 파일이라 지문을 박고 레포 밖으로 복사했다.

| 항목 | 값 |
|---|---|
| 파일 | `scripts/output/§research-1/q1_arms_20260806-161606.json` (16,029,426 B) |
| sha256 | `6bf6e039cd458edae0f12676638120f906fea228838da60185e494c73af81627` |
| 레포 밖 사본 | `/Users/ohsungsu/dev/bell-agent/_archive_research1/` — sha256 **동일** 확인 |

박제 위치 = `R8_CHROME_LEXICON.md` §0-0 (같은 커밋에 포함).
⚠️ 사본은 **같은 디스크의 다른 경로**다. 디스크 손실에는 대응하지 못한다.

---

## 1. 탐침 방법 — 파이프라인과 일치시켰다 (catch CI)

| 항목 | 파이프라인 실물 | 탐침 |
|---|---|---|
| HTTP 메서드 | `tools/web_rag/ingest_net.py:151` `sess.get` | **GET** |
| User-Agent | `tools/web_rag/ingest_config.py:26` `_UA` | **동일 문자열** |
| 파서 | `tools/web_rag/ingest.py:675` `BeautifulSoup(html, "lxml")` | **동일** |
| 텍스트화 | `tools/web_rag/ingest.py:679` `soup.get_text(separator="\n")` | **동일** |
| 디코딩 | `ingest_net.py:154~` `r.encoding` → 없으면 `chardet` | **동일** |

✅ **지시문 행번호는 옳다** (2026-08-09 실물 대조). 지시문·대장의 `ingest_docs.py:386` · `ingest.py:677`은
둘 다 **`for tag in soup(["script","style","noscript"]): tag.decompose()`** — **태그 제거 행**을 가리킨다.
수리 자리로 정확한 행이며, 두 파일에서 같은 의미의 행을 짚고 있다.
`ad/WORKBOARD.md:75`의 *"파서 행번호 2건도 실물 일치 확인"*이 맞다.

> ⚠️ 이 문서 1차판은 이 행을 *"실물과 2행 어긋난다"*고 적었다. **오기가 아니라 내 오판이었다** —
> 제거 행이 아니라 `soup` 생성행(`385`/`675`)·`get_text` 행(`388`/`679`)과 대조했다.
> `CLAUDE.md §9` *"판정 전에 열어본다"*를 어긴 사례로 남긴다. 파일을 열어보고서 뒤집혔다.

지킨 금지 3건 — 파이프라인 코드 **import 0**(`catch CK` 부작용 회피) ·
`web_search.py:848` **무수정** · seed 경로 **미사용**(`catch CJ`).
`trafilatura`·`readability`·`html2text`는 탐침에도 쓰지 않았다.

**탐침 스크립트** = `probe_s2_tagmap.py` · `probe_s2_body.py` · `probe_s2_agg.py` · `probe_s2_occ.py`
(레포 루트, `.gitignore:106 probe_*`로 차단 — 커밋 대상 아님).

---

## 2. 수집 결과 — 실패 0건

| 항목 | 값 |
|---|---|
| 대상 청크 | **65** (개정표: 19 → 65 전건) |
| 고유 URL | **43** (한 URL이 최대 6청크를 낳는다) |
| GET 성공 | **43/43 = HTTP 200** |
| 실패 | **0건** |

⚠️ `illustkorea.or.kr` 1건은 **PDF**다(청크 #08·#46·#48·#49). 이 4건은 R8에서 크롬 0이라
판별 대상 span이 없다. HTML 파싱 결과는 무의미한 바이트열이며 어떤 집계에도 쓰지 않았다.

---

## 3. 판별 방법 — 매칭 단위와 판정 단위

### 3-a. 매칭 단위 = 크롬 문구 덩어리(span)

개정표대로 청크 전문이 아니라 **그 안의 크롬 문구**를 HTML에서 찾았다.
청크 65건 중 45건이 크롬·본문 혼재라 전문은 여러 DOM 노드에 걸쳐 원리적으로 매칭되지 않는다.

span 출처는 둘이고 **성질이 다르다.**

| 출처 | 청크 수 | span 수 | 내용 |
|---|---|---|---|
| R8 §6 발췌 | 27 | 580 | 크롬 문구 **구간만** 원문 그대로 |
| R8 §5 전문(행 단위) | 18 | 784 | 청크 **전문**의 각 행 |
| 합계 | 45 | **1,364** | |

### 3-b. 판정 단위 = 문구가 아니라 **출현**, 값은 최악값

한 문구가 3곳에 나오고 그중 1곳만 `<footer>` 안이면 구조 규칙은 **그 1곳만** 지운다.
나머지 출현은 살아서 청크에 들어간다. 청크는 페이지 전체 텍스트에서 만들어지므로
판정 단위는 출현이고, 값은 최선값이 아니라 **최악값**이어야 한다.

따라서 출현 탐색에 **상한을 두지 않았다.** 상한을 3→10으로 올려도 11번째가 남고,
메뉴·헤더는 문서 상단 편중이라 상한은 ①②로 잡히기 쉬운 출현만 표집한다 —
누락 방향이 STOP 기준과 반대다. 출현별 **범주 분포**도 함께 기록했다
("5개 중 4개가 footer"와 "5개 다 본문"은 최악값이 같아도 처방이 다르다).

### 3-c. ④ 판정은 같은 페이지 본문 체인과 대조한 뒤에 내렸다

지시 — *"대조 없이 '단서가 없어 보인다'로 ④를 매기지 않는다"*.

- 대조군 = 같은 페이지의 긴 텍스트 노드 8건. 크롬 문구를 포함하는 노드는 제외.
  임계는 **200 → 120 → 80 → 40** 폴백(§4-a)
- **④ 확정 규칙** = ④ span의 조상 토큰 집합에서 본문 조상 토큰 집합을 뺀 차집합이 **공집합**
  → 본문과 구조 동일 → **④ 확정**
- 차집합이 있으면 크롬만 가진 컨테이너가 존재해 단서가 될 수 있다 → **④-구분가능**으로 분리
  (지시문 ③ *"class/id 문자열로만 식별 가능"*에 해당. STOP 합계에서 뺀다)

---

## 4. 🔴 대조군 진단 — 집계보다 먼저 (오염 가능 페이지)

`④` 판정의 근거가 대조군이므로 집계에 앞서 대조군 자체를 셌다.

### 4-a. 임계 200에서 대조군 0건 = 13 URL

| 원인 | URL 수 |
|---|---|
| 200자 이상 텍스트 노드가 페이지에 **애초에 없다** | **12** |
| 긴 노드는 있는데 크롬 열쇠가 전부 걷어냈다 | 1 (`mobiinside.co.kr`) |

→ 임계를 **200 → 120 → 80 → 40**으로 낮춰 살렸다.

🔴 **폴백은 "≥1건"이 아니라 "≥3건"에서 멈춘다** (2026-08-09 2차 정정).
1차 설계는 1건만 나와도 멈췄다. `#45`가 임계 200에서 **1건**이 잡혀 거기서 멈췄는데,
120으로 내리면 **14건**이었다. 대조군이 얇으면 본문 토큰 집합이 작아져 차집합이 남기 쉽고,
판정이 `④확정 → ④-구분가능` 쪽으로 기운다 — **④를 과소 계상**한다. 방향이 STOP 기준과 반대다.

✅ **최소 두께 3을 걸고 전량 재측정한 결과 수치가 하나도 움직이지 않았다** —
`④확정 72` · `④-구분가능 162` · 같은 11청크. 얇은 대조군 우려는 실측으로 해소됐다.

최종 대조군 분포(청크 기준):

| 임계 | 청크 수 | | 대조군 건수 | 청크 수 |
|---|---|---|---|---|
| 200 | 19 | | 8건(상한) | 22 |
| 120 | 14 | | 5~7건 | 9 |
| 80 | 8 | | 3~4건 | 12 |
| 40 | 3 | | 2건 | 1 |
| **없음** | **1** | | **0건** | **1** |

### 4-b. 🔴 임계 80의 0건은 "본문 없음"이 아니라 **탐침 실패**였다

1차 측정에서 `④-미확정`으로 뺐던 #13 `2ebd52` (`brunch.co.kr/@emforce/720`)를 **사람이 직접 열어
확인했다.** 글 플랫폼에 본문 노드가 하나도 없다는 것이 이상했기 때문이다.

| 관측 | 값 |
|---|---|
| HTML | 344,656 B |
| `soup.get_text()` 자수 | **2,623자 — 본문은 멀쩡히 있다** |
| 비어있지 않은 행 | 136 |
| **최대 행 길이** | **67자** |

브런치는 문장을 `<br>`·`<span>`으로 **잘게 쪼개 렌더링**한다. 그래서 80자 임계에서도 0건이 났다.
**"본문 노드가 없다"가 아니라 "본문 노드가 전부 짧다"** — 페이지 탓이 아니라 **임계 설계 탓**이다.

→ 임계 40을 폴백에 추가해 대조군 2건을 만들고 재판정했다. 결과:

| span | 재판정 | 본문에 없는 컨테이너(단서) |
|---|---|---|
| `keyword` | ④-구분가능 | `wrap_keyword` · `info_keyword` |
| `전시` (occ 25) | ④-구분가능 | `cover_title` · `cover_item` · `h1` |
| `브랜드` (occ 22) | ④-구분가능 | `cover_title` · `cover_item` · `h1` |
| `마케팅전략` | ④-구분가능 | `link_keyword` · `info_keyword` |
| `매거진의 이전글` | ④-구분가능 | `wrap_page_article` · `link_page` |

🔴 **`④-미확정`은 0이 됐다.** 5건 전량이 **구조로 구분 가능**했다. 1차 보고에서
*"STOP 발동 여부가 이 1건에 달려 있다"*고 적었던 불확실성은 **소멸했다.**

**대조군을 끝내 못 만든 URL은 1건**(#36 `6a799b` `brandb.net`)만 남았고, 이 청크는 ④ span이 0이라
집계에 개입하지 않는다.

### 4-b-2. #36 `brandb.net` — #13과 성질이 다르다 (확인만, 재판정 불요)

#13이 임계 탓이었으므로 #36도 같은 성질인지 확인했다. **아니다.**

| 항목 | #13 `brunch/@emforce` | #36 `brandb.net` |
|---|---|---|
| HTML | 344,656 B | 55,905 B |
| `get_text` 자수 | **2,623** | **906** |
| 텍스트 노드 | 136 | 58 |
| 최대 노드 길이 | 67자 | **112자** |
| 크롬 열쇠(≥10자) | **0개** | **23개** |
| 임계 200/120/80/40/25 대조군 | 0/0/0/**2**/2 | 0/0/0/**0**/**0** |

#13은 본문이 있는데 노드가 잘게 쪼개진 경우였다. #36은 **페이지 전체 텍스트가 906자**이고
그중 대부분이 R8이 크롬으로 지목한 문구(사업자등록번호·저작권 고지·에이전시 디렉터리 등 23개)다.
임계를 25까지 내려도 0건이다 — **임계 문제가 아니라 본문이 실제로 희박한 페이지**다.
④ span이 0이라 집계에 개입하지 않으므로 재판정하지 않는다. 사실만 기록한다.

### 4-c. ⚠️ 대조군 오염 가능 2 URL (`keys=0` + 대조군 있음)

크롬 문구가 전부 10자 미만이면 제외 열쇠가 0개가 되어, 대조군이 **아무것도 안 거르고** 채워진다.

| 청크 | URL | 대조군 | 비고 |
|---|---|---|---|
| #21 `e7dd10` | `brunch.co.kr/@ghidesigner/219` | 13건 | ④확정 0 → STOP 영향 없음 |
| #16 `76956e` | `openads.co.kr/...contsId=16137` | 2건 | ④확정 0 → STOP 영향 없음 |

`keys=0`인 나머지 3 URL(#58 · #13 · #17)은 대조군 자체가 0이라 이 항목과 겹치지 않는다.
**두 건 모두 ④확정이 0이라 STOP 판정에 개입하지 않는다.**

---

## 5. 🔴 오염 정정 — R8 §5 전문에는 본문 행이 섞여 있다

**이 정정이 STOP 결과를 바꾼다. 먼저 읽을 것.**

R8 §5는 `🔴 지배`(크롬이 절반 이상) 청크도 **전문**을 싣는데, **어느 행이 크롬이고 어느 행이
본문인지 표시가 없다.** 1차 추출은 §5 전문의 **모든 행**을 크롬 span으로 취급했다.
본문 산문은 당연히 구조 단서가 없으므로 ④로 떨어지고, **오염 방향이 STOP 쪽으로 부풀린다.**

실물 예 (#4 `4fca47`, `🔴 지배`) — ④확정으로 나온 12 span 중 11건이 본문이었다.

```
④확정  occ=14  디자인                                    ← 크롬 (jaenung nav 카테고리)
④확정  occ= 1  안녕하세요, 여러분! 오늘은 정말 흥미진진한 주제로 …   ← 본문
④확정  occ= 1  우리 귀는 24시간 열려 있어요. 자는 동안에도 …        ← 본문
```

`🔴 전량` 8건은 정의상 전 행이 크롬이라 오염이 없고, §6 발췌 27건도 크롬 구간만 뽑은 것이라
오염이 없다. **오염은 `🔴 지배` 10건(#4 #20 #22 #28 #36 #40 #41 #43 #54 #56)에 한정된다.**

**처리** — STOP을 좌우하는 `④확정` span 전량(72건)을 덤프해 **육안으로 크롬/본문을 갈랐다.**
경계 2건은 R8 §5 실물을 열어 확인했다.

| 청크 | span | 판정 | 근거 |
|---|---|---|---|
| #28 | `릴스` · `쇼츠` | **크롬** | R8:1290~1308 티스토리 **태그 목록** 한가운데(`MZ세대마케팅, ugc마케팅, …, 릴스, …, 쇼츠, …`) |
| #41 | `숏폼 콘텐츠 전략` | **본문** | R8:2082~ 결론 문단의 강조어. 앞뒤가 `…로 수렴할 것이다` · `은 2025년 디지털 마케팅의 핵심 축이다` |
| #56 | 14건 전량 | **본문** | `하며,` · `을 만들고,` 등 문장 조각. 크롬 0 |

⚠️ `④-구분가능`(157건)과 `①②③`(1,079건)에는 이 육안 정정을 적용하지 않았다.
STOP은 ④로만 결정되고, 본문 행이 ②·③으로 분류되는 것은 STOP 값을 바꾸지 않기 때문이다.
**따라서 아래 §6 분포표의 ②·③ 수치는 지배 10건분만큼 본문이 섞인 값이다.**

---

## 6. 판별 결과 — 범주 분포

### 6-a. span 1,364건 전량

| 범주 | 정의 | span 수 |
|---|---|---|
| ① 자기 태그 | 문구를 **직접 담은** 태그가 `nav`·`footer`·`aside`·`header` | **0** |
| ② 조상 태그 | 체인 **위쪽에** 위 태그가 있다 | 627 |
| ③ class·id만 | 위 태그 없이 `class`/`id` 문자열로 식별 | 452 |
| ④-구분가능 | 힌트 단어는 없으나 본문에 없는 컨테이너를 가짐 | 162 |
| **④ 확정** | **본문 체인과 구조 동일 — 못 가름** | **72** |
| ④-미확정 | 대조군 0 → 판정 보류 | **0** (§4-b에서 해소) |
| MISS·SPLIT | 노드 단위 미탐, 페이지 전문에는 있음(탐침 한계) | 9 |
| MISS·DRIFT | 페이지 전문에도 없음(07-31 이후 사이트 변경) | 42 |

🔴 **① = 0건.** 텍스트 노드의 직접 부모가 `<nav>`·`<footer>`인 경우는 1,364건 중 **하나도 없다.**
문구는 `<a>`·`<span>`·`<li>` 안에 있고 구조 태그는 항상 그 위에 있다.
→ 지시문의 *"직접 태그만 보면 본문 문단과 구별되지 않는다"*가 실측으로 확인됐다.
**직접 태그만 보는 규칙은 0건을 잡는다.**

### 6-b. MISS 51건의 원인 2분할 (self-check #7)

| 구분 | 건수 | 뜻 |
|---|---|---|
| SPLIT | **9** | 텍스트 노드가 쪼개져 노드 단위로 못 찾음. `soup.get_text()` 전문에는 있다. **탐침 한계, 개선 가능** |
| DRIFT | **42** | 페이지 전문에도 없음. 07-31 이후 사이트가 바뀐 것. **불가피** |

⚠️ **미매칭을 실패로 읽지 않는다.** 51/1,364 = 3.7%. DRIFT 42건 중 19건이 `blog.theprconsulting.com`
1개 호스트(#40)에 몰려 있다 — `catch CI`의 "실패가 한 호스트에 몰리면 그것부터 의심한다"에 해당하나,
GET·200 정상이고 문구만 사라진 것이라 **사이트 개편으로 읽는다.** 판정하지 않는다.

---

## 7. 🔴 결론 — 구조 규칙으로 거의 다 잘린다. 진짜 못 자르는 건 1건이다

작업 3의 원 물음은 **"다섯 껍데기가 HTML 꼬리표로 갈리는가"**, 넓게는
**"구조 규칙으로 크롬을 자를 수 있는가"**였다. 답은 **"거의 다 된다"**이다.

| 층 | span | 뜻 |
|---|---|---|
| ② 조상 태그 | 627 | `<nav>`·`<footer>` 등이 체인 위에 있다 → **자른다** |
| ③ class·id | 452 | `gnb`·`comment` 등 이름으로 식별 → **자른다** |
| ④-구분가능 | 162 | 본문에 없는 컨테이너를 가짐 → **자를 수 있다** |
| **④ 확정** | **72** | 본문 체인과 구조 동일 → **못 자른다** |

그런데 ④확정 72건도 대부분은 **구조 문제가 아니다.**

🔴 **④가 덮는 청크 9건 중 8건은 10자 미만 짧은 단어의 본문 어휘 충돌이 원인이고,
10자 이상 고유 문구로 ④가 난 청크는 `#45` 1건뿐이다.**

| 청크 | ④확정 span | 자수 | 그 페이지 출현 수 |
|---|---|---|---|
| #4 | `디자인` | 3 | 14 |
| #20 | `측정` `검색` `파트너` `공유` | 2~3 | 4~12 |
| #22 | `로그인` `디자인` `과학` | 2~3 | 4~16 |
| #28 | `릴스` `쇼츠` | 2 | 3 |
| #35 | `엠포스` `추천 콘텐츠` `공유하기` | 3~6 | 2~5 |
| #43 | `추천 콘텐츠` `공유하기` | 4~6 | 2 |
| #58 | `브랜드캠페인` | 6 | 2 |
| #59 | `Menu` | 4 | 4 |
| **#45** | **`진익준/ 브랜드경험디자인연구소`** | **15** | **1** |

`디자인`은 nav에도 있고 본문 문장에도 있다. 최악값 규칙이 **본문 쪽 출현**을 근거로 ④를 매기지만,
**nav 쪽 출현은 구조 규칙으로 정상 제거된다.** 청크에 남는 것은 크롬이 아니라 원래 본문이다.

→ **9는 상한값이다.** 제거 방식(격리 표시 아님)을 재검토해야 할 근거는 이 실측에서 나오지 않았다.

🔴 **#45도 통째로 미해결은 아니다** — 출현 전수로 갈라보니 ④확정 4건 중 **순수형은
`진익준/ 브랜드경험디자인연구소` 1건**이고, 태그 3단어는 크롬 쪽 출현이 `wrap_keyword`로 구분된다.
그 1건은 **저자가 글 본문에 직접 써 넣은 서명**의 형태다(출현 1회, 본문 체인).
같은 기준을 9청크 전체에 적용하면 순수형 보유는 **6청크**로 더 내려간다.
전량 덤프와 근거는 **§7-c-1 · §7-c-2**. ⚠️ 6은 관측이며 결론 문장은 이 문서에서 쓰지 않는다.

⚠️ 최악값 규칙의 한계다. 규칙 자체는 "같은 크롬 문구가 여러 크롬 위치에 있고 그중 하나가 제거 불가"인
경우에 맞다. 짧은 문구가 본문 어휘와 겹치는 경우에는 과대 계상한다.
**짧은 라벨을 어떻게 다룰지 정하는 것은 챗 소관 — 여기서는 관측만 적는다.**

---

## 7-A. STOP 판정 — 🟡 경계 (기준이 답을 못 내는 구간)

**기준(2026-08-08 개정, 확정값)** — ④로 판정된 유형들이 덮는 **청크 합계 10건(65의 약 15%) 이상**이면 STOP.
링크팜 제외, 한 청크가 여러 ④ 유형을 포함하면 **1건**으로 계산.

### 7-a. ④ 확정 유형과 덮는 청크

| ④ 유형 | 5범주/미분류 | 덮는 청크 | 청크 수 |
|---|---|---|---|
| nav 메뉴 라벨 | nav | #4 · #20 · #22 · #59 | 4 |
| 태그·키워드 목록 | 미분류 | #28 · #45 · #58 | 3 |
| 공유 버튼 | 미분류 | #20 · #35 · #43 | 3 |
| 관련글·추천 콘텐츠 위젯 | 미분류 | #35 · #43 | 2 |
| 작성자·연구소명 | 직원명단 | #45 | 1 |
| **합집합 (중복 제거)** | | **#4 #20 #22 #28 #35 #43 #45 #58 #59** | **9** |

### 7-b. 판정 — 🟡 경계

| 항목 | 값 |
|---|---|
| ④가 덮는 청크 (링크팜 제외, 중복 제거) | **9** |
| STOP 기준 | **10** |
| **판정** | 🟡 **경계 — 기준이 답을 못 내는 구간** |

🔴 **"9 < 10 이므로 미발동"으로 쓰지 않는다.**

기준 10은 **65의 약 15%**에서 잡은 값이다. 15%라는 어림수에서 나온 자에는
**1건 차이를 가를 정밀도가 없다.** 그 자로 9와 10을 가르면 **자의 정밀도를 넘어선 판정**이 된다.

이것은 기준을 옮기는 것이 아니다. 기준은 **10 그대로**다.
기록하는 것은 *"측정값이 기준의 분해능 안에 들어와 판정이 성립하지 않는다"*는 사실이다.

**따라서 발동/미발동을 이 문서에서 확정하지 않는다.** 판단 재료는 §7 결론이다 —
9건 중 8건이 짧은 단어 충돌이고 구조 미해결은 1건뿐이라는 것. **결정은 챗 소관.**

**링크팜 별도 집계** — #14 `0f3783` (`accio.com`).
`proxim sensor npn` · `wedding chargers 120 set` · `weld machin for rebar` · `관련 페이지` ·
`팝업 스토어 알바` 5 span **전량 `④-구분가능`**, **④확정 0건**.
→ 합산에서 빼도 결과가 같다. 성질 판정(사이트 구조물인가 본문에 심긴 SEO 문자열인가)은 **챗 소관** — 쓰지 않는다.

### 7-c-1. 🔴 #45 전량 덤프 (챗 요청 2026-08-09)

**"실제 구조 미해결 1건"으로 지목된 청크다. 판정 근거를 통째로 싣는다.**

| 항목 | 값 |
|---|---|
| 청크 | `#45` `fde7c4` |
| URL | `https://brunch.co.kr/@jinikjun/31` |
| dist | 1.0176 · 1.0566 / 자수 931 |
| span 출처 | **R8 §6 발췌** (혼재 27건 — 크롬 구간만, 원문 글자 보존) |
| R8 기준 크롬 유형 | **직원명단** + **미분류(태그 꼬리 · 이전글/다음글)** |
| 본문 대조군 | **8건 @ 임계 120** (1차 1건@200 → 최소 두께 3 적용 후 갱신) |

**본문 대조군 대표 체인** (임계 120)

```
astro-slot → div.article_contents → div.wrap_article.disable_selection
  → div#ArticleView.wrap_body.text_align_left → astro-island
  → p.wrap_item.item_type_text → span → span
본문 예: "한식이나 일식 레스토랑의 벽에 자연의 이미지나 미디어-월을 설치하고 스피커로…"
```

**④ 판정 span 4건 — 출현 전수와 조상 체인**

| span | R8 유형 | 출현 | 범주 분포 | 성질 |
|---|---|---|---|---|
| `진익준/ 브랜드경험디자인연구소` | 직원명단 | **1** | ④×1 | **순수형** |
| `브랜드경험디자인` | 태그 꼬리 | 4 | ④×4 | 혼재형 |
| `브랜드경험` | 태그 꼬리 | 13 | ④×13 | 혼재형 |
| `브랜드공간` | 태그 꼬리 | 8 | ④×8 | 혼재형 |

```
[순수형] 진익준/ 브랜드경험디자인연구소
  ×1  div#ArticleView.wrap_body → astro-island → p.wrap_item.item_type_text → span → span
        → 본문과 구조 동일

[혼재형] 브랜드경험디자인
  ×3  div#ArticleView.wrap_body → astro-island → p.wrap_item.item_type_text → span → span
        → 본문과 구조 동일
  ×1  div.info_keyword → div.info_article.wrap_keyword → ul.list_keyword → li → a.link_keyword
        → 구분단서 info_keyword · wrap_keyword · link_keyword

[혼재형] 브랜드경험
  ×8  div#ArticleView.wrap_body → … → p.wrap_item.item_type_text → span → span   (본문과 동일)
  ×2  p.wrap_item.item_type_text → span → span → b → span                        (구분단서 b)
  ×2  div.info_keyword → div.info_article.wrap_keyword → ul.list_keyword → li → a.link_keyword
  ×1  astro-slot → div.article_contents → div.wrap_page_article → a.link_page → strong.tit_page

[혼재형] 브랜드공간
  ×7  div#ArticleView.wrap_body → … → p.wrap_item.item_type_text → span → span   (본문과 동일)
  ×1  div.info_keyword → div.info_article.wrap_keyword → ul.list_keyword → li → a.link_keyword
```

**참고 — 같은 청크의 ④ 아닌 2건** (대조용)

| span | 판정 | 체인 |
|---|---|---|
| `keyword` | ④-구분가능 | `div.info_keyword → div.info_article.wrap_keyword → strong.screen_out` |
| `매거진의 이전글` | ④-구분가능 | `div.article_contents → div.wrap_page_article → a.link_page → span.txt_page` |
| `매거진의 다음글` | ③ | `… → a.link_page.link_next → span.txt_page.next_page` |

🔴 **읽히는 것 2가지 (판정 아님).**

1. **태그 꼬리 3단어는 크롬 쪽 출현이 구분된다.** `div.wrap_keyword → a.link_keyword` 는
   본문 체인에 없는 컨테이너다. ④가 붙은 이유는 **같은 단어가 본문에도 나오기 때문**이다 —
   이 글의 주제가 브랜드경험디자인이라 본문에 8~13회 등장한다.
2. **`진익준/ 브랜드경험디자인연구소` 만 출현이 1회이고, 그 1회가 본문 안이다.**
   `div#ArticleView.wrap_body` 밑 본문 문단과 같은 체인이다. 사이트가 만든 바이라인이 아니라
   **저자가 글 본문에 직접 써 넣은 서명**의 형태다.

### 7-c-2. 🔴 출현 전수 재판정 — 순수형 / 혼재형

7-c-1을 만들다 **집계 코드의 한계**를 찾았다. `classify`는 범주별 대표 체인을 **1건만** 저장하고
(`reps`), 차집합을 그 대표로 계산한다. 한 문구가 본문과 크롬 **양쪽에** 나오면 어느 쪽이 대표가
되느냐로 판정이 갈린다. #45 `브랜드경험디자인`이 정확히 그 경우였다.

→ `④확정` 72건 전량을 **출현 전수** 기준으로 다시 갈랐다.

| 성질 | 정의 | span | 청크 |
|---|---|---|---|
| **순수형** | ④ 출현이 **전부** 본문과 구조 동일 | **61** | 10 |
| 혼재형 | 일부 출현이 구분 가능한 컨테이너를 가짐 | 11 | — |

§5 육안 정정을 거친 **크롬 9청크**에 적용하면:

| 청크 | ④확정 크롬 span → 성질 | 순수형 보유 |
|---|---|---|
| #20 | 측정·검색·파트너·공유 = 전부 순수형 | ✅ |
| #22 | 로그인·디자인 혼재형 / **과학 순수형** | ✅ |
| #35 | 엠포스·추천 콘텐츠·공유하기 = 전부 순수형 | ✅ |
| #43 | 추천 콘텐츠·공유하기 = 전부 순수형 | ✅ |
| #45 | **진익준/… 순수형** / 태그 3단어 혼재형 | ✅ |
| #59 | Menu = 순수형 | ✅ |
| #4 | 디자인 = 혼재형 | — |
| #28 | 릴스·쇼츠 = 혼재형 | — |
| #58 | 브랜드캠페인 = 혼재형 | — |

**9청크 중 순수형 보유 = 6청크.** 나머지 3청크(#4 · #28 · #58)는 크롬 쪽 출현이 구분 가능하다.

⚠️ **이 6은 판정이 아니라 관측이다.** 세는 방식을 또 하나 늘린 것이므로 결론 문장을 여기서 쓰지 않는다.
결정은 챗 소관.

### 7-c-3. 🔴 순수형 61건 분류 — **기준을 먼저 쓴다**

> ⚠️ **이 절의 기준은 분류를 시작하기 전에 확정해 커밋했다.**
> 분류하면서 기준을 만들면 자기 채점이 된다. 아래 규칙을 먼저 고정하고, 그 다음 61건을 통과시켰다.
> 결과를 보고 기준을 고치지 않았다.

**갈래는 셋뿐이다. 다른 범주를 만들지 않는다.**

| 갈래 | 뜻 |
|---|---|
| **(a) 크롬 아님** | 사이트가 만든 구조물이 아니다. 애초에 제거 대상이 아니었다 |
| **(b) 크롬인데 사이트가 구조를 안 줌** | 진짜 미해결. 규칙 R이 다뤄야 할 것 |
| **(c) 탐침 한계** | 계측 문제. 판정에 필요한 대조가 성립하지 않는다 |

**판정 규칙 (적용 순서 고정)**

1. **a-1 — §5 본문 행이면 (a).**
   R8 §5 `🔴 지배` 청크의 전문에서 뽑힌 행 중 §5 육안 정정으로 **본문**으로 판정된 것.
   R8이 크롬으로 지목한 적이 없는 문구다. 이 판정은 §5에서 이미 끝났고 여기서 다시 하지 않는다.

2. **a-2 / b — 같은 호스트 다른 URL 대조.**
   기준 = *"사이트가 모든 글에 자동으로 붙이는 것인가, 저자가 이 글에만 쓴 것인가."*
   - 같은 호스트의 **다른 URL 페이지에도 같은 문구가 있다** → 자동 생성 → **(b)**
   - **없다** → 저자가 이 글에만 쓴 것 → **(a)**
   대조 대상은 이번에 받아둔 43개 HTML 중 같은 호스트의 다른 URL 전부.

3. **c — 위 대조가 성립하지 않으면 (c).**
   같은 호스트의 다른 URL이 **0개**여서 자동/저자를 가릴 수 없는 경우.

**미리 밝히는 한계** — 3번은 "크롬이 아니다"는 뜻이 아니라 **"이 자료로는 못 가른다"**는 뜻이다.
(c)를 (b)로 읽으면 미해결을 부풀리고, (a)로 읽으면 축소한다. 어느 쪽으로도 옮기지 않는다.

### 7-c. 세는 방식별 민감도

| 세는 방식 | 청크 수 | 기준 10 대비 |
|---|---|---|
| **④확정만 (지시문 정의 그대로)** | **9** | 🟡 경계 |
| ④확정 + ④-구분가능 | 24 | 초과 |
| ④확정 + ④-미확정 | **9** | 🟡 경계 (④-미확정 = 0) |
| ④확정 중 **순수형만** (출현 전수 기준, §7-c-2) | **6** | 미달 |

- `④-구분가능`을 ④로 세면 24가 되어 크게 넘는다. 그러나 지시문 ④ 정의는
  *"본문 문구의 체인과 구조가 동일"*이고, 구분가능은 **본문에 없는 컨테이너를 가진** 건이라
  정의상 ③이다. 정의대로 뺐다.
- `④-미확정`은 §4-b에서 **0으로 해소**됐다. 1차 보고의
  *"발동 여부가 #13 1건에 달려 있다"*는 **더 이상 성립하지 않는다.**
  세 방식 중 둘이 같은 값(9)을 낸다.

---

## 8. 다음 단계로 넘기는 관측 (판정 아님)

1. **① 0건 / ② 627건** — 구조 태그는 항상 조상에 있다. 수리 규칙은 **조상 체인을 봐야** 성립하고,
   직접 태그 검사만으로는 0건을 잡는다.
2. **③ 452건이 두 번째로 크다.** `class`/`id` 문자열이 유일한 단서인 구간이 3분의 1이다.
   한국 사이트의 `<div class="gnb">` 관행이 실측으로 확인됐다.
3. **대조군 임계 200이 13/43 URL에서 성립하지 않았다.** 한국 블로그·뉴스는 문단이 잘게 쪼개져
   200자 텍스트 노드가 없는 경우가 흔하다 — 브런치는 **최대 행이 67자**다(§4-b).
   후속 측정에서 임계를 상수로 박으면 같은 함정에 걸린다.
   🔴 **더 중요한 교훈** — 임계 0건을 "그 페이지엔 본문이 없다"로 읽으면 틀린다.
   `CLAUDE.md §9` *"양성 대조 없는 0건은 근거가 아니다"*의 실사례가 하나 더 나왔다.
   실제로 사람이 페이지를 열어보고서야 탐침 실패임이 드러났다.
4. **DRIFT 42건은 07-31 이후 사이트 변경분이다.** 신규 수집 1회 전제가 유효한 근거이기도 하다 —
   현존 원장으로는 되돌릴 수 없는 변화가 이미 일어났다.

⚠️ 위 4건은 **관측이지 규칙이 아니다.**

---

## 9. Self-check

| # | 항목 | 결과 |
|---|---|---|
| 1 | 유료 API 호출 0건 | ✅ **0** — GET 43회는 공개 웹페이지 조회. 검색 API·임베딩·LLM 호출 0 |
| 2 | `.py` 코드 수정 0건 | ✅ 파이프라인 `.py` **0**. 신규 탐침 3개는 `.gitignore:106 probe_*`로 차단, 커밋 대상 아님 |
| 3 | 커밋 대상이 지정 범위뿐 | ✅ `git diff --staged --name-status`로 §10에 증명 |
| 4 | HTML이 `resources/` 밖 + `.gitignore` 차단 | ✅ `_s2_probe_html/` · `scripts/output/**/_s2_probe_html/*.html` |
| 5 | `command grep` 병행 결과 기재 | **— 미수행.** 이번 작업은 `.env*`·`probe_*` 등 ignore 대상 검색이 없었다. 파이프라인 행 확인은 `command grep`만 사용(§10-a) |
| 6 | 작업 2 건수 검증 (19 vs 실측) | **— 미해당 (작업 2 항목).** R8 §1에서 이미 처리됨 |
| 7 | 작업 3 미매칭 건수 | ✅ **51건** = SPLIT 9 + DRIFT 42 (§6-b) |
| 8 | STOP 발동 여부 | 🟡 **경계 — 확정하지 않음.** ④ = **9청크**, 기준 **10**. 기준이 65의 15% 어림수라 1건 차이를 가를 정밀도가 없다(§7-b). 결정은 챗 소관. 1-b·1-e·작업2게이트는 **— 미해당(작업 1·2 항목)** |
| 9 | deviation·초과 작업 자진 보고 | ✅ §10-b에 4건 |
| 10 | 한 작업만 수행하고 정지 | ✅ 작업 3만 수행. 다음 단계(신규 수집)는 유료·챗 소관 — 진행하지 않음 |

---

## 10. 부록

### 10-a. 산출물

| 파일 | 커밋 | 내용 |
|---|---|---|
| `R9_CHROME_TAG_MAP.md` | **O** | 이 문서 |
| `.gitignore` | **O** | HTML 차단 1행 추가 |
| `_s2_probe_html/*.html` | X | 원본 HTML 43건 (차단) |
| `_s2_fetch_log.json` · `_s2_tagmap_raw.json` · `_s2_body_diag.json` · `_s2_final.json` · `_s2_occ.json` | X | `.gitignore:84 scripts/output/**/*.json` |
| `probe_s2_tagmap.py` · `probe_s2_body.py` · `probe_s2_agg.py` · `probe_s2_occ.py` | X | `.gitignore:106 probe_*` |

### 10-b. deviation (자진 보고)

1. **`.gitignore` 1행 추가 → 커밋 대상에 포함.** 지시문 작업 3 마감은 `R9…md` 1건만 add 하라고 했으나,
   같은 지시문이 *"`.gitignore`로 차단할 것"*을 요구한다. 차단이 유지되려면 커밋돼야 한다.
   `CLAUDE.md §4`도 `.gitignore`를 커밋 대상 운영 문서로 규정한다.
2. **span 육안 정정(§5).** 지시문에 없는 단계다. R8 §5 전문에 본문 행이 섞여 ④가 부풀려지는 것을
   확인해 `④확정` 72건을 육안으로 갈랐다. 이 단계가 없으면 STOP이 잘못 발동한다.
3. **④를 3분(확정/구분가능/미확정)했다.** 지시문은 ④ 단일 범주다. 대조군이 없는 건과
   본문에 없는 컨테이너를 가진 건을 같은 칸에 넣으면 판정 근거가 흐려진다. §7-c에 세 방식을 모두 적었다.
4. **대조군 임계 폴백(200→120→80→40).** 지시문에 임계 규정이 없다. 200 고정 시 13/43 URL에서
   대조가 불가능해 ④를 확정할 수 없었다. **40은 2차 추가분** — #13을 육안 확인한 결과
   80의 0건이 탐침 실패였음이 드러나 폴백을 한 칸 더 뒀다(§4-b).

### 10-b-2. 1차판에서 뒤집힌 판정 2건 (자진 정정)

| # | 1차판 기술 | 2차 실측 | 뒤집힌 이유 |
|---|---|---|---|
| 1 | *"지시문 행번호가 실물과 2행 어긋난다"* | **옳다.** `:386`·`:677` = 태그 제거 행 (§1) | 제거 행이 아니라 `soup` 생성행·`get_text` 행과 대조했다 |
| 2 | *"#13은 대조군 0 → ④-미확정. STOP 발동 여부가 이 1건에 달림"* | **탐침 실패.** 본문 2,623자 존재, 최대 행 67자. 임계 40에서 대조군 확보 → 5건 전량 `④-구분가능` (§4-b) | 임계 0건을 "본문 없음"으로 읽었다 |
| 3 | *"#45 = 실제 구조 미해결 1건"* (통째로) | **④확정 4건 중 순수형 1건뿐.** 태그 3단어는 크롬 쪽 출현이 `wrap_keyword`로 구분된다 (§7-c-1) | 집계가 범주별 **대표 체인 1건**만 보고 차집합을 냈다. 출현 전수를 안 봤다 |

셋 다 **파일·페이지·출현을 직접 열어보고서** 뒤집혔다. `CLAUDE.md §9` — 판정 전에 실행하거나 열어본다.

⚠️ **3건 모두 같은 뿌리다** — *요약된 대표값을 전수 대신 썼다.*
행번호 하나, 임계 0건 하나, 대표 체인 하나. 전수를 보면 셋 다 답이 달랐다.

### 10-c. 미수행 (지시 범위 밖으로 판단)

- **`TOPIC_SLUG` assert 미삽입** — `CLAUDE.md §1`은 측정/fetch 스크립트에 assert를 요구하나,
  이 탐침은 토픽 프리셋을 읽지 않는다. 입력은 R8의 URL 목록이고 색인·임베딩·retrieval을 타지 않는다.
  잘못된 토픽이 로드돼도 결과가 달라지지 않는다.
- **`④-구분가능`·`①②③`의 본문 행 육안 정정** — §5 경고 참조. STOP 값을 바꾸지 않는다.

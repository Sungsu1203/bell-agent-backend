# R10b — S4 산출물 재판독: 검정력·대조군·T 전량 (§research-1 / 작업 S4-b)

- 작성 2026-08-09 · 채널 = 기술 정밀
- 지시 `CC_S4b_20260809_census_power.md`
- **성격 = 읽기 전용.** `_s4_census.json` · `_s4_bilateral.json` 판독만. **탐침 재실행 0 · 탐침 수정 0 · 비용 $0**
- 🔴 **유효/무효 판정을 하지 않는다.** 값과 축별 내역만 낸다 (self-check #31)

---

## 🔴 0. 먼저 — §2-1 의 4축 중 **3축은 이 두 파일만으로 산출 불가**다

| 축 | 상태 | 사유 (필드 실측) |
|---|---|---|
| **1. 자수** (10자 미만/이상) | ✅ **전량 산출** | `phrase_detail[]` 에 `len` + `chrome`/`body`/`mid` 존재 |
| **2. verdict** (②/③/④-구분가능) | ❌ **불가** | `phrase_detail[]` 에 **verdict 필드가 없다.** 문구 단위로 접혀 있어 레코드 verdict 와 연결할 키가 없다 |
| **3. 대조군** (th=200/120/80/40/0) | ⚠️ **2분할만** | `phrase_detail[]` 에 **URL 식별자가 없다**(`urls` 는 개수뿐). `body_meta` 는 URL별 `th` 를 갖지만 문구↔URL 연결 키가 없다. `in_clean` 불리언으로 **"대조군 0건 URL 전용 문구" vs 나머지** 2분할까지만 가능 |
| **4. 참고 모집단** (POP_REF, ④확정 포함) | ❌ **불가** | `ref_T10` 에 담긴 것은 `{n, strict, loose, loose_pct, base_loose_pct, note}` **뿐이다.** ④확정분의 `chrome`/`body`/`mid` 는 dump 되지 않았다 |

🔴 **축 4 가 지시문이 "결정적"이라 지목한 바로 그 축이다.** ④확정 72(그중 (a)크롬아님 51)에서 `body` 가 나오는지가 (d) 의 양성 대조인데, **그 값은 저장되지 않았다.**

**닫는 데 필요한 최소 조치** — 축 2·3·4 전부 `probe_s4_census.py` 의 출현 재계산(`occ` 루프)을 다시 돌려 dump 필드를 늘려야 한다. 지시 §6-1·§6-2 가 금지한 **재실행**에 해당한다.
→ **여기서 정지하고 보고한다. 새 계산은 별건 지시로 받는다.**

아래 §1~§7 은 **재실행 없이 산출 가능한 전부**다.

---

## 1. §2-1 — 검정력 3분할 (산출 가능분)

### 1-a. 총량 (모집단 = 잔여 POP, 문구 922)

```
[검정력] 출현 총량 N = 2,642
         chrome 2,184 (82.66%) · body 169 (6.40%) · mid 289 (10.94%)
```

⚠️ **N 2,642 의 정의를 명시한다** — `phrase_detail[]` 의 문구별 출현 합계다. 이는 (문구,URL) 쌍이 걸린 페이지에서 그 문구의 **DOM 출현 전수**이며, 잔여 레코드 1,241 이나 쌍 1,240 과는 다른 단위다. (§5 의 `<T` 3값 중 "페이지출현"과 같은 계통)

**챗 사전 확정 기준표에 대응하는 값 = `body` 6.40%** → 기준표의 `2~10%` 구간(**회색 — 참고 모집단 축 결과로 판단**)에 든다.
🔴 **그런데 참고 모집단 축이 산출 불가다**(§0). **CC 는 여기서 판정하지 않는다.**

### 1-b. 축 1 — 자수

| 구분 | 문구 | 출현 N | chrome | body | mid |
|---|---|---|---|---|---|
| **10자 미만** | 617 | 2,125 | 1,741 (81.93%) | **168 (7.91%)** | 216 (10.16%) |
| **10자 이상** | 305 | 517 | 443 (85.69%) | **1 (0.19%)** | 73 (14.12%) |
| 합계 | 922 | 2,642 | 2,184 (82.66%) | 169 (6.40%) | 289 (10.94%) |

- `body` **169건 중 168건(99.4%)이 10자 미만 문구**에서 나왔다.
- 10자 이상의 `body` 는 **1건** — `'시그니처 사운드 마케팅'` 단 하나이며, (d) 의 T=10 loose 분자 1 과 동일 개체다(§5 표 1행).

### 1-c. 축 3 — 부분판 (`in_clean` 2분할)

| 구분 | 문구 | 출현 N | chrome | body | mid |
|---|---|---|---|---|---|
| `in_clean=True` (대조군 있는 URL 보유) | 878 | 2,586 | 2,128 | **169** | 289 |
| `in_clean=False` (대조군 0건 URL 전용) | 44 | 56 | **56** | **0** | **0** |

- 대조군 0건 URL(`brandb.net`) 전용 문구 44건은 출현 56 **전부가 chrome**이고 `mid` 도 0이다.
  → 이 URL 에는 **cat 4 출현 자체가 0**이라 "대조군 0 때문에 body 가 깎였다"는 경로로는 작동하지 않았다.
- `body` 169 는 **전량 `in_clean=True` 쪽**에 있다.

⚠️ 이 2분할은 `th=200/120/80/40` 4단 분해가 **아니다.** 지시 §2-2 의 `th` 분포(§2)와는 별개 표이며, 문구↔URL 연결이 없어 **두 표를 교차시킬 수 없다.**

### 1-d. 축 2 · 축 4 — 산출 불가 (사유는 §0)

**추정치를 넣지 않는다.** 빈칸으로 둔다.

---

## 2. §2-2 — 대조군 품질

```
[대조군] body_meta 등재 URL 43개
         th=200 → 19장 · 120 → 12장 · 80 → 8장 · 40 → 3장 · 0건(th=None) → 1장
         0건 URL 전량: https://brandb.net/archive/the-sound-of-leffe-2024-06
```

대조군 **건수(n) 분포** — 상한 8:

| n (대조군 건수) | URL 수 |
|---|---|
| 0 | 1 |
| 2 | 1 |
| 3 | 5 |
| 4 | 5 |
| 5 | 4 |
| 6 | 2 |
| 7 | 3 |
| **8 (상한)** | **22** |

- **43장 중 22장(51.2%)이 대조군 상한 8건**을 채웠고, **31장(72.1%)이 임계 200 또는 120** 에서 확보됐다.
- `th=40` 까지 내려간 것은 **3장**, 0건은 **1장**뿐이다.

**관측 1건 (판정 아님)** — `_s2_probe_html/` 에는 HTML 이 **45장**인데 `body_meta` 는 **43 URL**이고 `_s2_fetch_log.json` 도 **43항목**이다. 인계문·R9 의 "45장" 기술과 3자리 수치가 어긋난다. 원인은 확인하지 않았다(재실행 금지 범위).

---

## 3. §2-3 — `t_rows` 전량 10행 (T 5개 × 2판)

**키 부재 없음** — `t_rows` 에 10개 키 전부 존재(`T8/T10/T12/T15/T20` × `전체/결손제외`).

| T | 판 | ≥T 문구 | strict | strict% | loose | loose% | 판정 | `<T` 쌍 | `<T` 레코드 | `<T` 출현 | 역산 라벨 [쌍 \| 레코드 \| 출현] |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 8 | 전체 | 396 | 0 | 0.00% | 1 | **0.25%** | 미발동 | 754 | 755 | 1,920 | [1,150 \| 1,151 \| 2,316] |
| 8 | 결손제외 | 369 | 0 | 0.00% | 1 | 0.27% | 미발동 | 754 | 755 | 1,920 | [1,123 \| 1,124 \| 2,289] |
| **10** | **전체** | **305** | **0** | **0.00%** | **1** | **0.33%** | **미발동** | **883** | **884** | **2,125** | **[1,188 \| 1,189 \| 2,430]** |
| 10 | 결손제외 | 282 | 0 | 0.00% | 1 | 0.35% | 미발동 | 883 | 884 | 2,125 | [1,165 \| 1,166 \| 2,407] |
| 12 | 전체 | 245 | 0 | 0.00% | 1 | 0.41% | 미발동 | 952 | 953 | 2,255 | [1,197 \| 1,198 \| 2,500] |
| 12 | 결손제외 | 225 | 0 | 0.00% | 1 | 0.44% | 미발동 | 952 | 953 | 2,255 | [1,177 \| 1,178 \| 2,480] |
| 15 | 전체 | 195 | 0 | 0.00% | 0 | **0.00%** | 미발동 | 1,013 | 1,014 | 2,341 | [1,208 \| 1,209 \| 2,536] |
| 15 | 결손제외 | 178 | 0 | 0.00% | 0 | 0.00% | 미발동 | 1,013 | 1,014 | 2,341 | [1,191 \| 1,192 \| 2,519] |
| 20 | 전체 | 148 | 0 | 0.00% | 0 | 0.00% | 미발동 | 1,065 | 1,066 | 2,416 | [1,213 \| 1,214 \| 2,564] |
| 20 | 결손제외 | 136 | 0 | 0.00% | 0 | 0.00% | 미발동 | 1,065 | 1,066 | 2,416 | [1,201 \| 1,202 \| 2,552] |

**부기 (값 판독만, T 확정 아님)**

- 전 10행에서 **strict = 0**이다. loose 분자는 T=8·10·12 에서 **동일 문구 1건**(`'시그니처 사운드 마케팅'`, 14자)이고 T=15 이상에서 그 문구가 `<T` 로 빠지지 않으므로 0 이 되는 것이 아니라 — 14자라 T=15 에서 `≥T` 집합에서 빠진다.
- 지시 §2-3 이 지목한 비용 구조: T=10 에서 `<T` 출현 2,125 / 총 2,430 = **87.4%**. T=8 로 내리면 1,920 / 2,316 = **82.9%**.
- T 를 8→20 으로 올릴수록 역산 라벨 3값이 **모두 증가**한다(쌍 기준 1,150→1,213, 출현 기준 2,316→2,564).

---

## 4. §2-4 — ② 최근접 구조 태그

```
② 627 — header 포함 334 (53.3%) · header 단독 79 (12.6%) · 최근접 header 98 (15.6%)
```

| 세는 법 | `header` | `nav` | `footer` | `aside` |
|---|---|---|---|---|
| 중복허용(포함) | **334** | 297 | 264 | 45 |
| 조합배타(단독) | **79** | 34 | 214 | 16 |
| **최근접** | **98** | 285 | 227 | 17 |

- **최근접 98 > 단독 79.** 지시 §3-a 의 조건(*"최근접 값이 79 보다 크면 그 값을 전수 대상 모집단으로 쓴다"*)에 해당하는 대소 관계다. **적용 여부는 챗 소관** — 값만 싣는다.
- 차 19건 = 체인에 `header` 가 최근접이면서 다른 구조 태그도 함께 있는 레코드다(조합배타에서는 `header+nav` 등으로 빠진 것).

---

## 5. §2-5 — 양측 13문구 전건 육안 판정표

`Q2 §6.1 A-1-a` 형식. **전건 13건.** 매칭 실물 502행(body측 169행)은 `_s4_bilateral.json`.

| # | 문구 | 자수 | URL | chrome | body | mid | strict | loose | body측 대표 발췌 |
|---|---|---|---|---|---|---|---|---|---|
| 1 | `'시그니처 사운드 마케팅'` | **14** | 1 | **0** | 1 | 1 | **✗** | ✓ | *…이게 바로 '시그니처 사운드 마케팅'이에요!* |
| 2 | `AI` | 2 | 4 | 21 | 7 | 18 | ✓ | ✓ | *…실제 사람이 아닌 AI 기반의 가상 모델들이…* |
| 3 | `기획` | 2 | 1 | 1 | 1 | 1 | ✓ | ✓ | *…내년 팝업 기획의 방향을 구체화…* |
| 4 | `로그인` | 3 | 9 | 18 | 1 | 10 | ✓ | ✓ | *더보기 클릭 시: 로그인 필요 안내 모달…* |
| 5 | `마케팅` | 3 | 2 | 18 | 10 | 21 | ✓ | ✓ | *…가상 인간을 활용한 마케팅이 큰 주목을…* |
| 6 | `브랜드` | 3 | 3 | 4 | **46** | 13 | ✓ | ✓ | *똑똑한 브랜드들은 요즘 시대의 방식에 맞게…* |
| 7 | `역사` | 2 | 3 | 3 | 1 | 1 | ✓ | ✓ | *…소리로 영화의 역사를 쓴 거죠!* |
| 8 | `재능넷` | 3 | 2 | 28 | 6 | 17 | ✓ | ✓ | *…재능넷에서 음악이나 사운드…* |
| 9 | `전시` | 2 | 1 | 1 | 19 | 5 | ✓ | ✓ | *…확실한 브랜딩 효과를 낼 수 있는 전시 마케팅…* |
| 10 | `캠페인` | 3 | 1 | 1 | 35 | 3 | ✓ | ✓ | *…막대한 비용을 들여 캠페인을 진행한다…* |
| 11 | `콘텐츠` | 3 | 4 | 17 | 7 | 15 | ✓ | ✓ | *…최신 기술을 활용한 콘텐츠로 현실의 개념을…* |
| 12 | `팝업` | 2 | 2 | 6 | 22 | 19 | ✓ | ✓ | *…접점을 늘리기 위해 팝업스토어를 적극 활용…* |
| 13 | `팝업스토어` | 5 | 4 | 83 | 13 | 8 | ✓ | ✓ | *…국내에서 열린 팝업스토어는 1월부터 8…* |

### 5-a. 🔴 substring 오탐 판정 (self-check #33) — **1문구 확정, 2문구 부분**

body측 매칭 전수(169행 → 문자열 매칭 218회)의 **직후 문자**를 전량 집계했다. 대표값이 아니라 전수다.

| 문구 | body 매칭 | 직후가 한글 | 오탐 판정 |
|---|---|---|---|
| **`팝업`** | 28 | 28 | 🔴 **오탐 확정** — **26건(92.9%)이 `팝업스토어`의 앞부분**이다. 독립 `팝업` 은 2건(`팝업을`·`팝업에서`) |
| `전시` | 20 | 14 | ⚠️ **1건 부분 오탐** — `전시장에` 1건(복합어). 나머지 13건은 조사(`를`×7 등) |
| `브랜드` | 54 | 31 | ⚠️ **1건 부분 오탐** — `브랜드캠페인` 1건(1.9%). 나머지 30건은 조사(`의`×11 `가`×8 `들은`×2 …) |
| `AI`·`기획`·`로그인`·`마케팅`·`역사`·`재능넷`·`캠페인`·`콘텐츠`·`팝업스토어`·`'시그니처 사운드 마케팅'` | 계 116 | 계 68 | **오탐 없음** — 직후 한글은 전부 조사/어미(`의`·`를`·`은`·`에서`·`이에`·`로` 등) |

- **10자 이상 유일 건인 `'시그니처 사운드 마케팅'` 은 오탐이 아니다.** 매칭 1회, 직후 `이에요` = 조사·어미. 따옴표까지 포함한 14자 완전 일치다.
- 오탐이 확정된 `팝업` 은 **2자 문구**이므로 T=10 판정에는 개입하지 않는다.

---

## 6. §4 — `catch CN` 비교 대상 확인 (self-check #34)

지시 §4 의 3분류 중 **어느 것인지 확정한다.**

| 비교 대상 | 이번 사례 | 판정 |
|---|---|---|
| R9 시점 박제 해시와 대조 | ❌ **박제가 없다.** `R9 §0-a` 가 sha256 을 박제한 파일은 `q1_arms_20260806-161606.json`(`6bf6e039…`) **1건뿐**이고, `_s2_final.json` 은 `R9 §10-a` 산출물 표에 **이름만** 있다 | 불가 |
| 덮어쓰기 **전 백업**과 대조 | ❌ **백업을 뜨기 전에 import 가 실행됐다** | 불가 |
| 덮어쓴 뒤 **재차 생성해 서로 비교** | ✅ 이것이 실제로 한 것 — 16:42(사고분) → 16:45(리팩터 후) → 이후 재실행 전부 `5c8045…` 동일 | **결정론만 증명** |

🔴 **결론 — 원본 byte 일치는 미증명이다.** 정직하게 기재한다.

**확보된 정황 증거 2건 (증명 아님)**

1. 덮어쓰기 **전** 디렉터리 목록의 크기 **681,045 B** = 덮어쓰기 **후** **681,045 B**.
   ⚠️ **크기 일치를 동일성으로 읽지 않는다.** `STANDARDS §3.2` 가 기록한 전례 3회(빈 Chroma 188,416 B · 7508 B 3파일 · `find -size -10c`)와 같은 함정이다.
2. `R9 §6-a` 집계값 전항 일치(②627/③452/④구분가능162/④확정72/SPLIT 9/DRIFT 42, span 1,364, ④확정 보유 청크 11건).
   → **문서에 박제된 집계 수준의 일치**이지 byte 일치가 아니다.

**부기 (지시 §4 요구) — 이 사고의 양성 정보**
재생성분이 서로 byte 동일이라는 것은 **R9 집계 파이프라인이 결정론적**이라는 직접 증거다. 사고 기술만 남기지 않고 이 사실을 함께 박제한다.

**`catch CN` 확정 문안**
> `catch CN` — `__main__` 가드 없는 probe 모듈을 import 하면 산출물이 덮어써진다(`probe_s2_agg` → `_s2_final.json`). 부수 관측: 재생성분 sha256 동일 = R9 파이프라인 결정론 확인. ⚠️ 단 **원본 byte 일치는 미증명**(사전 해시·백업 부재, 크기 일치만 확보).

---

## 7. §7 — `R10_SPAN_CENSUS.md` 정정 반영

| 항목 | 내용 |
|---|---|
| 대상 | `R10_SPAN_CENSUS.md:33` (§0 본문) |
| 종전 | *"역산 라벨(아래 §5)은 T=10 에서 1,188~2,430건 으로, 전파를 안 쓰고 레코드 전량(1,241)을 라벨하는 것과 **차이가 없거나 오히려 많다.**"* |
| 정정 | *"…3값 중 둘(1,188·1,189)은 레코드 전량 1,241 보다 **적으나 절감률이 4.3%·4.2%에 그치고**, 세 번째(2,430)는 오히려 약 2배다. **전파의 실익이 없다.**"* + 정정 각주 |
| 그 외 | **손대지 않았다.** `R10:244` 의 *"1,241 과 같은 자릿수이거나 크다"* 는 1,188 이 같은 자릿수이므로 **틀린 기술이 아니다** → 정정 대상 아님 |

---

## 8. Self-check (#29~34)

| # | 항목 | 결과 |
|---|---|---|
| **29** | 재실행 0 | ✅ **`python probe_*.py` 실행 0건.** 이번 차수에 실행한 명령 전량 = ① `_s4_census.json`·`_s4_bilateral.json` 판독 python 4회(전부 `json.load` + 집계, 파일 쓰기 0) ② `R9_CHROME_TAG_MAP.md` 의 sha256 박제 유무 확인 `sed`/`command grep` ③ `R10` 정정 대상 줄 `command grep`. **탐침 3종 전부 미실행 · 탐침 수정 0** |
| **30** | §2-1 4축 전부 | ⚠️ **축1 전량 · 축3 2분할 · 축2·축4 산출 불가.** 사유 = §0 (필드 부재). **추정치를 넣지 않았다** |
| **31** | §2-1 판정 미실시 | ✅ `body` 6.40% 가 챗 기준표의 **회색(2~10%)** 구간에 든다는 **대응 관계만** 기재. `유효/무효` 선언 **하지 않음**. 회색 구간의 판단 근거인 참고 모집단 축이 산출 불가임을 함께 기재 |
| **32** | §2-3 10행 전량 | ✅ 10행 전량 수록. **키 부재 0** (`t_rows` 10키 전부 존재) |
| **33** | §2-5 육안 13건 | ✅ 전건. substring 오탐 = **`팝업` 1문구 확정(26/28)** · `전시`·`브랜드` 각 1건 부분 · 나머지 10문구 없음. **10자 이상 1건은 오탐 아님** |
| **34** | `catch CN` 비교 대상 | ✅ **3분류 중 세 번째(결정론만 증명).** **원본 byte 일치 = 미증명**으로 기재. 정황 2건은 증명이 아님을 명시 |

### 8-a. deviation

| # | 내용 |
|---|---|
| 1 | §2-1 축2·축4 **미산출**. 재실행이 필요해 지시 §6-1·§6-2 에 따라 정지 |
| 2 | §2-5 오탐 판정을 위해 body측 매칭 **직후 문자 전수 집계**를 추가로 돌렸다(기존 JSON 판독 범위 내, 파일 쓰기 0). self-check #33 이 "오탐 유무 판정 결과"를 요구하므로 근거 없이 선언하지 않기 위함 |
| 3 | §2-2 에서 **HTML 45장 vs `body_meta` 43 URL** 불일치를 관측·기재했다. 지시에 없는 항목이나 대조군 분모에 걸리는 값이라 기록만 했다(원인 미확인) |

---

## 9. 🔴 정지 — 챗 결정이 필요한 것

| # | 항목 | 상태 |
|---|---|---|
| **1** | **§2-1 축2(verdict)·축3(th 4단)·축4(참고 모집단) 산출 여부** | **정지 중.** 셋 다 `probe_s4_census.py` 재실행 + dump 필드 확장이 필요하다. 지시 §6-2 대로 **별건 지시를 기다린다** |
| 2 | (d) 유효/무효 | **CC 판정 안 함.** `body` 총량 6.40%(회색), 10자 이상 0.19%. 회색 판정의 근거인 축4 부재 |
| 3 | T 확정값 | 미확정. §3 표만 제출 |
| 4 | 표본 규모·계층 배분 | 미확정 |
| 5 | 조건 A 의 N 값 | 미확정 (Y 색인 실측 전) |
| 6 | ② 전수 대상 모집단 | 값만 제출 — 단독 **79** / 최근접 **98** |
| 7 | 커밋 | **하지 않았다.** 지시 §6-7 |

**S5·S6 미착수 유지.**

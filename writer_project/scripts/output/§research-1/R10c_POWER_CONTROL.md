# R10c — (d) 검정력 확정: 양성대조 2종 · 축 복구 · 민감도 (§research-1 / 작업 S4-c)

- 작성 2026-08-09 · 채널 = 기술 정밀
- 지시 `CC_S4c_20260809_power_control.md` (+ 사용자 추가지시 5건: cwd·venv·버전·R9 환경·소급 보고)
- **재실행 1회 승인분 집행** — `probe_s4_census.py` 수정 후 **1회 실행**. 비용 **$0** (네트워크 0 · API 0 · 색인 0)
- 🔴 **(가)/(나) 판정을 하지 않는다.** 3분할 수치와 축별 내역까지가 이 문서의 범위다 (지시 §9-3)

---

## 0. 실행 환경 (사용자 추가지시 1~5)

### 0-a. cwd · venv · 버전 캡처

| 항목 | 값 |
|---|---|
| **cwd (S4-c 실행)** | `/Users/ohsungsu/dev/bell-agent/bell-agent-backend/writer_project` |
| **venv** | `../.venv_openai/bin/python` (= `/Users/ohsungsu/dev/bell-agent/bell-agent-backend/.venv_openai/bin/python`) |
| python | **3.11.6** |
| bs4 | **4.14.2** |
| lxml | **6.0.2** |
| libxml2 | **2.14.6** |
| soupsieve | **2.8** |
| platform | macOS (Darwin 25.5.0) |

**이 문서의 모든 상대경로는 `writer_project/` 기준이다.** 산출물 경로는 `scripts/output/§research-1/…`.
⚠️ 아래 §7-b 의 일부 판독 명령은 cwd 가 `scripts/output/§research-1` 이었다 — 해당 항목에 개별 표기했다.

**venv 선택 근거** — `STANDARDS.md:215` *"`.venv_openai` = ad/openai 트랙"*. §research-1 은 ad 트랙 토픽(`experiential-marketing-media`)을 공유하는 활성 트랙이므로 `.venv_openai` 가 정본이다.

### 0-b. 🔴 두 venv 파서 스택 실측 대조 — **완전 동일**

착수 전 `.venv_openai` 와 `.venv_vertex` 를 실측했다.

| | `.venv_openai` | `.venv_vertex` |
|---|---|---|
| python | 3.11.6 | 3.11.6 |
| bs4 | 4.14.2 | 4.14.2 |
| lxml | 6.0.2 | 6.0.2 |
| libxml2 | 2.14.6 | 2.14.6 |
| soupsieve | 2.8 | 2.8 |

→ **파싱 거동이 갈릴 요소가 없다.** venv 전환에 따른 비교 불능 위험 0.

### 0-c. 🔴 실측 크로스-venv 대조 (버전 일치보다 강한 증거)

버전 표만으로 끝내지 않았다. **S4(`.venv_vertex` 산출) 의 `body_meta` 43 URL 전량**을 이번 `.venv_openai` 실행 결과와 대조했다.

```
[대조군 동일성] S4(.venv_vertex) body_meta == 현재 = True
[S4 재현] phrases 922==922 · pairs 1240==1240
```

→ 두 venv 가 **같은 HTML 45장에서 같은 대조군(URL별 `n`·`th`)을 골랐다.** 실측 동일성이다.

### 0-d. 🔴 R9 환경 = **미기록**. 이번 캡처를 기준 환경으로 박제한다

`R9_CHROME_TAG_MAP.md` · `R8_CHROME_LEXICON.md` 전문 검색 결과:

| 찾은 것 | 위치 |
|---|---|
| 파서 **종류** `BeautifulSoup(html, "lxml")` | `R9:47` · `R9:700` |
| python / bs4 / lxml / libxml2 **버전** | **0건** |
| venv 경로 | **0건** |

→ **"R9 환경 미기록"으로 명시한다.** 지시대로 **§0-a 캡처를 이 트랙의 기준 환경으로 박제**한다.
간접 근거 1건 — R9 집계(②627/③452/④구분가능162/④확정72/SPLIT 9/DRIFT 42)가 현재 환경에서 **전항 재현**된다(§1-a). 환경이 R9 와 달랐다면 파싱 차이가 여기서 드러났을 개연성이 높으나, **버전 기록이 없으므로 동일성 증명은 아니다.**

### 0-e. 🔴 소급 보고 — S4 · S4-b 는 `.venv_vertex` 로 돌렸다

| 차수 | venv | 판단 |
|---|---|---|
| **S4** (`probe_s4_census.py` 최초 실행, `probe_s2_agg.py` 재현 실행 3회) | **`.venv_vertex`** | 🔴 **트랙 규약 위반.** ad 트랙이므로 `.venv_openai` 였어야 한다 |
| **S4-b** (판독 python 4회) | **`.venv_vertex`** | 동일 위반. 단 `json.load` 집계뿐이라 파서 무관 |
| **S4-c** (이번) | `.venv_openai` | 규약 준수 |

**영향 평가** — §0-b(버전 동일) + §0-c(`body_meta` 실측 동일) + §1-a(R9 전항 재현)로 **산출값에 미친 영향은 0으로 관측된다.** 다만 이는 사후 확인이며, **착수 전 venv 확인 절차가 없었다는 사실 자체는 결함**이다. catch 등재 여부는 챗 판정(§8 `catch CO 제안`).

---

## 1. 실행 전후 보호 조치 (self-check #42 · #38)

### 1-a. `_s2_final.json` 백업 + sha256 (실행 **전**)

```
실행 전  5c804500524314158ea1a930dc59fedf2347a8e3a9077591f3beff089d1c6d28  _s2_final.json
백업 2부  (1) scratchpad/_s2_final.json.bak_before_s4c        → 동일 해시
          (2) /Users/ohsungsu/dev/bell-agent/_archive_research1/_s2_final.json.20260809_pre_s4c → 동일 해시
```
레포 밖 사본은 `R8 §0-0` 방식을 따랐다.

### 1-b. 실행 **후** 대조 — 보호 대상 5개 전부 불변

| 파일 | sha256 | 변화 |
|---|---|---|
| `_s2_final.json` | `5c8045…` | **불변** |
| `_s4_census.json` | `1591e2…` | **불변** |
| `_s4_bilateral.json` | `be206b…` | **불변** |
| `probe_s2_agg.py` | `9db064…` | **불변** (지시 §9-1 준수) |
| `probe_s2_tagmap.py` | `1530a6…` | **불변** |

수정한 파일은 **`probe_s4_census.py` 1개뿐**이다.
🔴 **S4-c 산출물은 새 파일명으로 냈다** — `_s4c_census.json` · `_s4c_bilateral.json` · `_s4c_control.json` · `_s4c_bodyaudit.json`. S4-b 판정의 증거 기반인 `_s4_*.json` 을 덮지 않기 위함이다(catch CN 규율).

### 1-c. R9 재현 (self-check #39)

```
[R9 재현] {'2': 627, '3': 452, '4-구분가능': 162, '4확정': 72, 'DRIFT': 42, 'SPLIT': 9} · 전항 일치 = True
```
- ②627 / ③452 / ④구분가능162 / ④확정72 / MISS 51(SPLIT 9 + DRIFT 42) / span 1,364 — **전항 일치.**
- `body_meta` 43 URL 도 S4 와 동일(§0-c) → **dump 확장이 대조군 산출에 영향 주지 않았음**의 증거.
- ⚠️ **`probe_s2_agg.py` 를 재실행하지는 않았다.** 사용자 승인이 *"재실행 1회(`probe_s4_census.py`만)"* 였기 때문이다. 위 두 겹(집계 전항 대조 + `body_meta` 동일성)으로 대체했고, `probe_s2_agg.py` sha256 불변으로 코드 무변경을 함께 보였다. **deviation 으로 기재한다**(§8-a-2).

---

## 2. 🔴 작업 1 — 양성대조 ① 대조군 되먹임

### 2-a. 어구 추출 규칙 (코드 고정 — self-check #35)

`probe_s4_census.py` `extract_probe_phrases()`:

1. 재료 = 각 URL 의 **`body_ctx` 가 고른 본문 노드 텍스트**(원본판 상한 8)
2. 공백으로 어절 분할 (텍스트는 이미 `P.norm` 적용 = 공백 1개 정규화)
3. 연속 **2어절·3어절**을 공백 1개로 결합
4. 길이 **10자 이상 60자 이하**만 채택
5. 등장 순서 유지 중복 제거
6. **URL 당 상한 20건** (앞에서부터)

**임의 선별이 아니다.** 노드 순서·어절 순서에만 의존하는 결정적 규칙이다.

🔴 **선택 재현 잠금** — `body_ctx` 는 텍스트를 버리므로 같은 술어로 다시 골랐다. 복제가 갈라지지 않도록 URL 42개 전량에 **assert 2종**을 걸었다(전건 통과):
```python
assert len(sel) == body_ctx[u]["n"]
assert [c[:6] for _t, c in sel[:3]] == body_ctx[u]["chains"]
```

### 2-b. 판정 경로 동일성 (🔴 self-check #36 — 이번 차수의 핵심)

**판정 분기를 복제하지 않았다.** `classify_occ(chain, ctx_tok, ctx_n)` 함수 **하나**를 만들고 네 곳이 전부 그것을 호출한다.

| 호출부 | 위치 |
|---|---|
| 본선 모집단 집계 | `probe_s4_census.py` 모집단 루프 |
| **양성대조 ①** | 작업1 블록 |
| **양성대조 ②** | `occ[]` 재사용 = 본선 루프 산출물 |
| 상한 민감도판 | 저장된 `cat`·`chain[:8]` 로 `extra` 만 재계산 |

함수 본문 = S4 원본 분기를 **그대로 옮긴 것**이다:
```python
c = P.cat_of_one(chain)
if c in ("1","2","3"): return "chrome", c
if not ctx_n:          return "mid", c
extra = A.tokens(chain[:8]) - ctx_tok
return ("body" if not extra else "mid"), c
```
**증명** — 이 리팩터 후 실행에서 본선 값이 S4 와 완전 동일했다: `phrases 922==922 · pairs 1240==1240`, (d) 표·STOP 3건 전부 S4 와 같은 값. 분기를 함수로 접었는데 결과가 안 변했다는 것이 경로 동일성의 실측 근거다.

### 2-c. 결과 — 3분할

```
[작업1 · 양성대조 ①] 되먹임 어구 출현 N = 965
    chrome    405   42.0%
    body      534   55.3%
    mid        26    2.7%
```

**챗 사전 기준표 대응** — `30~80%` = *"부분 결함. 축4 결과와 함께 챗 판단"* 구간. ⚠️ 지시 §2-a 부기대로 **80% 는 최소선**이며 90%대가 정상이다. **CC 는 판정하지 않는다.**

### 2-d. 🔴 비-body 431행의 내역 — 두 기전이 섞여 있다

지시가 상정한 실패 기전은 *"대조군이 얇아 `extra` 가 남는다"* 였다. **실측은 그것이 소수다.**

| 비-body 사유 | 행 | 비-body 중 | 기전 |
|---|---|---|---|
| `chrome` · cat **3** (class/id 이름 사전) | **390** | **90.5%** | `cat_of_one` 이 본문 컨테이너 이름을 크롬으로 읽음 |
| `chrome` · cat **2** (조상 구조태그) | 15 | 3.5% | 본문 노드가 `header`/`nav` 체인 아래 |
| `mid` · cat 4 (`extra` 잔존) | **26** | **6.0%** | **대조군 기전** |

🔴 **`extra` 잔존은 965건 중 26건(2.7%)뿐이다.** 나머지는 `extra` 계산에 도달하기도 전에 `cat_of_one` 에서 크롬으로 분기됐다.

cat 3 을 유발한 `CLASSID_HINTS` 토큰 (중복 허용):

| 토큰 | 건 | 토큰 | 건 |
|---|---|---|---|
| `top` | 137 | `widget` | 20 |
| `side` | 129 | `sns` | 10 |
| `comment` | 126 | `tag` | 3 |
| `head` | 80 | | |

cat 2 대표 체인 — `div.nav__menu → div.nav__container → div.nav → header#header.header → div#container`

### 2-e. URL별 분포 (42 URL, 대조군 0건 1 URL 제외)

| body 비율 | URL 수 |
|---|---|
| **100%** | **21** |
| 20~90% | 6 (sedaily 89.1% · benews 52.4% · illustkorea 50.0% · marketing-leadership 35.0% · sweetspot ×2 20.9%) |
| **0%** | **15** |

0% 15 URL = `alien711` · `batcrew` · `benefitview` · `blog.theprconsulting` · `inside.ampm` · `ko.creativosonline` · `ko.wikipedia` · `mobiinside` · `sweetspot` ×4 · `theawesomestyle` · `volute` · `wedidit`

→ **이분화돼 있다.** 절반은 100%, 3분의 1은 0%다. 중간값이 드물다. (판정은 챗)

---

## 3. 🔴 작업 2 — 양성대조 ② 축4 (④확정)

### 3-a. (a)51 특정 — **가능** (self-check #37)

`_s2_pure.json`(R9 §7-c-4 산출, 순수형 61)에 `cat` 필드가 있다. **추정 재구성 없이 특정했다.**

```
_s2_pure.json cat 분포 = {'a': 51, 'c': 5, 'b': 5}   → (a) 51건 특정 가능
_s2_c_resolved.json 5건은 _s2_pure.json 의 부분집합 (검증: True) — (c)→(b) 해소분
```
→ 최종 (a)51 / (b)10 / (c)0 과 정합.

### 3-b. 3분할

| 구분 | 쌍 | 출현 N | chrome | body | mid |
|---|---|---|---|---|---|
| ④확정 전량 | 72 | 198 | 56 (28.3%) | **110 (55.6%)** | 32 (16.2%) |
| **(a) 크롬 아님 51** | 51 | 58 | 3 (5.2%) | **55 (94.8%)** | **0 (0.0%)** |
| ④확정 **<10자** | 25 | 151 | 56 (37.1%) | 63 (41.7%) | 32 (21.2%) |
| ④확정 **≥10자** | 47 | 47 | **0 (0.0%)** | **47 (100.0%)** | **0 (0.0%)** |

**부기** — ④확정 쌍 72 는 POP 쌍 1,240 과 **겹치지 않는다**(교집합 0). 두 모집단이 서로소이므로 축4 는 독립 대조로 성립한다.

---

## 4. 작업 3 — 축2 · 축3 3분할 (dump 확장으로 복구)

추가한 필드 = `phrase_detail[].verdict` · `.url_list` · `.th` (지시 §3).

### 4-a. 축2 — verdict (POP 쌍 기준)

| verdict | 쌍 | 출현 N | chrome | **body** | mid |
|---|---|---|---|---|---|
| ② 조상태그 | 627 | 941 | 941 | **0 (0.0%)** | 0 |
| ③ class·id | 451 | 1,102 | 1,102 | **0 (0.0%)** | 0 |
| **④-구분가능** | 162 | 599 | 141 | **169 (28.2%)** | 289 |

🔴 **②·③ 의 body 0 은 구조상 필연이다. 결함이 아니다.**
R9 의 verdict 는 **최악값**(`max(cats)`)이다. verdict 가 `2` 면 그 문구의 출현이 전부 cat 1·2 라는 뜻이고, `3` 이면 cat 4 출현이 하나도 없다는 뜻이다. **cat 4 가 없으면 `body` 판정이 발생할 자리가 없다.**
→ **`body` 169 전량이 ④-구분가능 층에서 나온다**(총계 169 와 일치). 이 층 안에서의 비율이 **28.2%** 다.

⚠️ 지시 §3 이 *"④-구분가능 층의 body 분포를 본 적이 없다"*고 지목한 값이 **28.2%** 다.

### 4-b. 축3 — 대조군 임계 `th`

| th | 쌍 | 출현 N | chrome | **body** | mid |
|---|---|---|---|---|---|
| 200 | 579 | 1,416 | 1,296 | 51 (**3.6%**) | 69 |
| 120 | 312 | 520 | 422 | 10 (**1.9%**) | 88 |
| 80 | 285 | 518 | 388 | 19 (**3.7%**) | 111 |
| **40** | 18 | 129 | 19 | **89 (69.0%)** | 21 |
| None(0건) | 46 | 59 | 59 | 0 (0.0%) | 0 |

**관측** — `th` 가 낮을수록 `body` 가 낮아지는 단조 관계는 **없다.** `th=40`(대조군이 가장 부실한 층)이 오히려 **69.0%** 로 가장 높다. `th=40` 3 URL 은 전부 brunch 계열(문장을 잘게 쪼개 렌더링, `R9 §4-b`)이다. 판정은 챗.

> 🔴 **2026-08-09 정정 확정 (S4-e 실측 → S4-f §3-c 문안 확정).**
> **축3 역전 — 인공물 혐의는 해소됐다.** `th=40` 에서 `body` 를 만든 2 URL(`brunch.co.kr/@360company/25` ·
> `brunch.co.kr/@emforce/720`)은 견본 순도가 **0% = 전량 본문**이었다. 견본 오염의 산물이 아니다.
> **단 그 2 URL 이 전체 `body` 169 의 52.7%(89건)를 차지하는 소표본이므로 (d) 근거 목록에는 불채택한다.**
>
> **(d) 유효 판정 자체는 정정 대상이 아니다** — 남은 3갈래로 성립한다:
> ① `extra` 잔존 26건 **전량 타 위치**(출처 노드 실패 0) ② 대조② `(a)51` body 94.8% · ≥10자 100.0%
> ③ 민감도 8→32 ±0.
> 🔴 **오히려 강화됐다** — 챗이 예측한 "견본 오염이 body 판정을 뒤집는다"가 실측 기각됐고(견본 100% 크롬
> 16 URL 의 body 기여 **0**), 이는 ②의 94.8% 가 **오염된 견본 위에서 나온 값이 아님**을 뜻한다.
> (상세 `R10e §4·§5` · `R10f §5`)

---

## 5. 작업 3(민감도) — 대조군 상한 8 → 32

🔴 **원본판(상한 8)이 표제값.** 민감도판은 별도 계상뿐이며 `probe_s2_agg.py` 는 건드리지 않았다(§1-b sha256 불변).

| 항목 | 상한 8 (원본·표제) | 상한 32 |
|---|---|---|
| 선택 노드 합계 | 266 | **391** (+125) |
| POP 전체 `body` | **169** | **169** (±0) |
| **≥10자 `body`** | **1 / 517** | **1 / 517** (±0) |

**챗 사전 기준 대응** — *"상한 해제 시 ≥10자 body 가 거의 안 늘어남"* 조건에 해당한다(정확히는 **전혀 안 늘었다**). 대조군 노드를 47% 더 넣어도 판정이 1건도 바뀌지 않았다. **귀결 판정은 챗.**

---

## 6. 작업 4 — 45장 vs 43 URL 차집합 (self-check #40)

```
HTML 45장 · R8 65청크 고유 URL 43 · 차집합 2 · rows 에 있는데 HTML 없는 URL = 0
```

**HTML 에만 있는 2건 특정:**

| # | 파일명 |
|---|---|
| 1 | `https_business_google_com_kr_think_search_and_video_2025_youtube_works_awards_korea_grand_prix.html` |
| 2 | `https_magazine_contenta_co_2016_05_9537_.html` |

**가설 검증 결과 = 챗 가설대로 정상.**
- 두 파일의 URL 은 `rows`(R8 65청크)에 **부재**다. `expect_files` 는 `rows` URL 에서 생성한 집합이고 두 파일이 그 밖에 있다는 것이 곧 부재의 증명이다.
- 🔴 **정지 조건은 반대 방향**(`rows` 에 있는데 HTML 이 없는 경우)이며 그 값은 **0**이다.
- → **판정에서 누락된 span 0건.** 43 이 정상이고, HTML 45장은 그보다 넓은 집합이다. 기록만 하고 종료.

---

## 7. 작업 5 — `body` 전건 오탐 감사 (self-check #41)

### 7-a. 집계 (전수, 대표값 아님)

`body` 판정 행 **169건**의 노드 안 **모든 매칭 위치**(앞뒤 30자)를 남겨 감사했다.

| 항목 | 값 |
|---|---|
| body 행 | **169** |
| 문자열 매칭 총수 | 196 |
| 기계 오탐 **후보** 매칭 | 30 |
| **전 매칭이 오탐 후보인 행** | **24** |
| 일부 매칭만 오탐인 행 | 1 |
| 기계 기준 오탐 제외 후 body 행 | **145** |

기계 기준 = *매칭 직후 글자가 한글이면서 조사·어미 집합 밖*. 🔴 **이것은 후보 추출이며 판정이 아니다.** 확정은 24행 전량 육안으로 했다.

### 7-b. 육안 판정 — 24행 전량 (cwd = `scripts/output/§research-1`)

| 문구 | 행 | 실제 형태 | 육안 판정 |
|---|---|---|---|
| `팝업` (2자) | **20** | 전부 **`팝업스토어`** 의 앞부분 (`팝업스토어를`·`팝업스토어는`·`팝업스토어에서`…) | 🔴 **오탐 확정** — `팝업스토어` 는 별개 낱말 |
| `전시` (2자) | 2 | **`전시장에`** · **`전시장을`** | 🔴 **오탐** — `전시장`(장소)은 별개 낱말 |
| `브랜드` (3자) | 1 | `브랜드캠페인을` | ⚠️ **경계** — `브랜드 캠페인` 을 붙여 쓴 복합어. `브랜드` 가 구성 형태소 |
| `캠페인` (3자) | 1 | `캠페인별로` | ⚠️ **경계** — `캠페인` + 접미 `별로`. 형태소로는 정상 출현 |

**육안 확정 오탐 = 22행** (`팝업` 20 + `전시` 2) · **경계 2행**

| 기준 | 오탐 제외 후 body 행 |
|---|---|
| 기계 후보 전부 제외 | **145** |
| **육안 확정만 제외** | **147** |
| 제외 없음(원값) | 169 |

🔴 **24행 전부 10자 미만이다.** ≥10자 `body` 1건(`'시그니처 사운드 마케팅'`, 직후 `이에요` = 어미)은 **오탐이 아니다.**
→ 지시 §6 부기대로 오탐 보정은 `<10자` 쪽만 낮추므로 ≥10자와의 격차는 **더 벌어진다.** (수치 진술이며 T 확정 아님)

---

## 8. Self-check (#35~42)

| # | 항목 | 결과 |
|---|---|---|
| **35** | 양성대조 ① 추출 규칙 | ✅ `extract_probe_phrases()` 로 코드 고정 · §2-a 명시. 2·3어절 n-gram, 10~60자, URL당 20건, 순서 결정적. **임의 선별 아님** |
| **36** | **① 판정 경로 동일성** | ✅ `classify_occ()` **단일 함수**를 본선·①·②·민감도가 전부 호출. 분기 복제 0. 리팩터 후 본선 값이 S4 와 완전 동일(922/1240, (d)표, STOP 3건)한 것이 실측 증거 (§2-b) |
| **37** | 축4 (a)51 특정 가부 | ✅ **가능.** `_s2_pure.json` `cat` 필드로 특정(a=51). 추정 재구성 0. 72 전량도 병기 (§3) |
| **38** | 상한 민감도 2판 | ✅ 원본판(8)이 표제. `probe_s2_agg.py` sha256 **`9db064…` 불변** (§1-b) |
| **39** | R9 재현 재확인 | ✅ **전항 일치**(②627/③452/④구분가능162/④확정72/MISS51). ⚠️ `probe_s2_agg.py` 재실행은 **안 함**(승인 범위 밖) — 집계 전항 대조 + `body_meta` 동일성 + 코드 sha256 불변 3겹으로 대체. deviation 기재 |
| **40** | 45 vs 43 차집합 | ✅ 2 URL 특정. `rows` 에 **부재** 확인. 역방향(정지 조건) **0건** → 누락 span 0 (§6) |
| **41** | body 오탐 감사 | ✅ **169행 전건.** 기계 후보 24행 → 육안 확정 오탐 **22행**, 경계 2행. 제외 후 body **147**(육안) / **145**(기계) (§7) |
| **42** | `_s2_final.json` 보호 | ✅ 실행 전 sha256 `5c8045…` + 백업 2부(레포 밖 포함). 실행 후 **불변** 확인 (§1-a·1-b) |

### 8-a. deviation (자진 보고)

| # | 내용 |
|---|---|
| 1 | 🔴 **S4·S4-b 를 `.venv_vertex` 로 실행했다** (트랙 규약상 `.venv_openai`). 영향은 §0-b·0-c·1-c 로 **0 관측**이나 절차 결함이다. → **`catch CO` 제안**: *"착수 전 venv 를 트랙 규약과 대조한다. 버전이 같아도 확인 절차 자체가 기록이다."* 등재는 챗 판정 |
| 2 | 🔴 **`probe_s2_agg.py` 를 재실행하지 않았다**(지시 §8 은 "원본판으로 1회 재수행" 요구). 사용자 승인이 *"재실행 1회(`probe_s4_census.py`만)"* 라 충돌했고, **좁은 쪽(사용자 지시)을 따랐다.** 대체 증거 3겹은 §1-c |
| 3 | 🔴 **`probe_s4_census.py` diff 전문을 싣지 못했다.** 수정 전 파일을 백업하지 않고 sha256(`780bec…`)만 박았다. 역산 복원을 시도했으나 **검증 실패**(복원본 `df7192…` ≠ `780bec…`) → **검증 안 된 복원본은 폐기**하고 §9 에 **편집 전·후 전문**을 실었다. (이 실수는 §8-a-1 과 같은 뿌리 — 착수 전 보호 절차 누락) |
| 4 | S4-c 산출물을 `_s4_*.json` 이 아닌 **`_s4c_*.json`** 새 파일명으로 냈다. S4-b 증거 기반 보존 목적(catch CN 규율). 지시에 명시는 없었다 |
| 5 | 지시에 없는 **비-body 431행 기전 분해**(§2-d)를 추가했다. ① 결과를 "대조군 기전 실패"로 읽으면 틀리기 때문 — `extra` 잔존은 2.7%뿐이다. **판정이 아니라 내역이다** |

---

## 9. 부록 — `probe_s4_census.py` 변경 전·후 전문 (diff 대체, §8-a-3 참조)

수정은 **8곳**이다. 각 항목의 "전" 은 sha256 `780bec…` 판, "후" 는 현재 판(실행에 쓰인 판)이다.

### 9-1. `import re` 추가
```diff
 import json
+import re
 import sys
```

### 9-2. `PARTICLE` 상수 신설 (`BUCKETS` 아래)
```python
# S4-c 작업 5 — 오탐 판별용 조사·어미 첫 글자 집합.
# 매칭 직후 글자가 한글이면서 이 집합 **밖**이면 "더 긴 낱말의 앞부분"(오탐 후보).
# ⚠️ 이것은 **후보 추출**이다. 확정은 `_s4c_bodyaudit.json` 전량 육안으로 한다.
PARTICLE = set("이가을를은는의에도와과로으들만부터까지라고나며인입었했하한할함")
```

### 9-3. 🔴 함수 3종 신설 (`stop_verdict` 앞) — `classify_occ` · `select_body_nodes` · `extract_probe_phrases`
`classify_occ` 본문은 §2-b 에 전문 인용. `select_body_nodes` 는 `A.build_body_ctx` 의 선정 술어를 같은 순서로 재현하되 텍스트를 함께 반환하고, `extract_probe_phrases` 는 §2-a 규칙 6단계를 구현한다.

### 9-4. 모집단 루프 → `classify_occ` 호출로 교체
```diff
-            c = P.cat_of_one(h["chain"])
-            if c in ("1", "2", "3"):
-                side = "chrome"
-                rec["chrome"] += 1
-                if c in ("1", "2"):
-                    ...
-            else:
-                extra = A.tokens(h["chain"][:8]) - body_ctx.get(u, {}).get("tok", set())
-                if not body_ctx.get(u, {}).get("n"):  side = "mid";  rec["mid"] += 1
-                elif not extra:                       side = "body"; rec["body"] += 1
-                else:                                 side = "mid";  rec["mid"] += 1
+            side, c = classify_occ(h["chain"], ctx.get("tok", set()), ctx.get("n"))
+            rec[side] += 1
+            if side == "chrome" and c in ("1", "2"):
+                ...
```
**동작 무변경** — 분기 조건·순서·토큰 범위가 1:1 대응한다. 실행 결과가 S4 와 동일함으로 확인했다.

### 9-5. `body` 행에 `ctxs` 추가 (작업 5 재료)
```diff
+            if side == "body":
+                row["ctxs"] = [t[max(0, m.start() - 30):m.end() + 30]
+                               for m in re.finditer(re.escape(key), t)]
```

### 9-6. S4-c 본문 블록 신설 (`# ── 10. STOP 값 산출` 앞)
환경 캡처 · R9/S4 재현 대조 · 작업4(차집합) · 작업1(양성대조 ①) · 작업2(양성대조 ②) · 축2/축3 · 민감도 · 작업5(감사) · `_s4c_control.json`·`_s4c_bodyaudit.json` 기록.

### 9-7. `phrase_detail` 3필드 추가 + `s4c` 수록
```diff
+             "verdict": sorted({v for u in p2u[p] for v in pair_verdict.get((p, u), ())}),
+             "url_list": p2u[p],
+             "th": [body_ctx.get(u, {}).get("th") for u in p2u[p]]
```

### 9-8. 출력 파일명 `_s4_*` → `_s4c_*` (S4 원본 보존)

---

## 10. 챗 발 오판 2건 기록 (지시 §12)

| # | 내용 | 이번 실측과의 관계 |
|---|---|---|
| **A** | S4-b 판정 기준표를 **전체값(6.40%)에 적용**하도록 제시했다. 구간별로 적용해야 했고 실제 ≥10자는 **0.19%** 다 | 이번 §4-a 가 한 겹 더 보탠다 — `body` 는 **④-구분가능 층에서만** 나올 수 있다(②③은 구조상 0). 분모를 어디로 잡느냐가 값을 크게 바꾼다 |
| **B** | 1차 지시 H 에서 토큰화 **범위** 비대칭만 다루고 대조군 **체인 개수 상한 8** 을 보지 않았다(51.2% 페이지가 상한에 걸려 있었다) | §5 실측 — 상한을 32 로 올려 노드를 47% 더 넣어도 `body` 가 **1건도 안 변했다**(169→169, ≥10자 1→1). **상한은 (d) 를 누르지 않았다** |

---

## 11. 🔴 정지 — 챗이 확정할 것

**CC 는 (가)/(나) 를 판정하지 않는다.** 아래 값만 넘긴다.

| # | 항목 | 값 |
|---|---|---|
| 1 | **양성대조 ①** | N=965 · chrome 42.0% · **body 55.3%** · mid 2.7%. **비-body 의 93.9% 는 `cat_of_one` 크롬 분기이고 `extra` 잔존은 2.7%뿐** (§2-d). URL 이분화: 100% 21 / 0% 15 |
| 2 | **양성대조 ②** | ④확정 전량 **body 55.6%** · **(a)51 body 94.8%** · **④확정 ≥10자 body 100.0%** |
| 3 | **상한 민감도** | 8→32 에서 `body` **169→169**, ≥10자 **1→1** (±0) |
| 4 | 축2 | ②·③ = 0%(구조상 필연) · **④-구분가능 28.2%** |
| 5 | 축3 | th=200 3.6% · 120 1.9% · 80 3.7% · **40 69.0%** · None 0% |
| 6 | body 오탐 | 169 → 육안 확정 오탐 22 → **147**. **≥10자 1건은 오탐 아님** |
| 7 | 45 vs 43 | 정상. 누락 span **0** |
| 8 | (d) 유효/무효 · T · 표본 규모 · 조건 A 의 N | **전부 미확정** |
| 9 | `catch CO`(venv 확인 절차) 등재 | 제안만 |
| 10 | 커밋 | **하지 않았다** (지시 §9-8) |

**S5·S6 미착수 유지.**

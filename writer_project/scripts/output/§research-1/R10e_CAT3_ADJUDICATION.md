# R10e — ③ 견본 노드 103건 육안 판정 (§research-1 / 작업 S4-e)

- 작성 2026-08-09 · 채널 = 기술 정밀
- 지시 `CC_S4e_20260809_cat3_adjudication.md`
- **재실행 1회 승인분 집행** — `probe_s4_census.py` 수정 후 **1회 실행**. 비용 **$0**
- 🔴 **③ 오작동 확정/기각을 선언하지 않는다.** 육안 3분류·집계·교차표까지가 범위다 (지시 §9-3)
- 🔴 `cat_of_one:195` **미수정** — 진단 중 (지시 §9-1)

---

## 0. 실행 환경 (self-check #44 준용 · 상시 절차)

| 항목 | 값 |
|---|---|
| **cwd** | `/Users/ohsungsu/dev/bell-agent/bell-agent-backend/writer_project` |
| **venv** | `../.venv_openai/bin/python` |
| python / bs4 / lxml / libxml2 / soupsieve | 3.11.6 / 4.14.2 / 6.0.2 / 2.14.6 / 2.8 |

**보호 조치** (지시 §9-10) — 탐침 수정 **전** 원본 사본 2부: `scratchpad/probe_s4_census.py.pre_s4e` · `_archive_research1/probe_s4_census.py.pre_s4e`, sha256 `f59d270130d8f1017697b664098f9cbdce7510405efa0318ad57ef4f293e3b9f` 일치. `_s2_final.json` 백업 1부.

**실행 후 대조 — 전부 불변**: `_s2_final.json` `5c8045…` · **`probe_s2_agg.py` `9db064…`** · **`probe_s2_tagmap.py` `1530a6…`**
→ `cat_of_one:195` **무접촉 증명.** R9 재현도 전항 일치(627/452/162/72/9/42).

---

## 1. 🔴 self-check #52 — 판정 순서 (이번 차수 핵심)

### 1-a. 순서를 **파일 분리로 물리적으로 강제**했다

탐침이 같은 103건을 **두 파일로 나눠** 냈다.

| 파일 | 담은 것 |
|---|---|
| `_s4e_nodes_TEXT.json` | `id` · `url` · `chars` · **노드 텍스트 전문**. **체인·class/id 없음** |
| `_s4e_nodes_NAMES.json` | `id` · `url` · **체인 전문(절단 없음)** · 발동 트리거 상세 |

**1단계** — `_s4e_nodes_TEXT.json` **만** 열어 103건을 판정했다. 필드 목록을 함께 출력해 이름 부재를 확인했다(`['id','url','chars','text']`).
**봉인** — 판정을 `s4e_verdict_stage1.json` 에 기록하고 sha256 `f93b6082f1b9b75f5db2efdf25386f764d67f02e3aa3a6240e2a71b11acedcd3` 를 박은 **뒤에** 이름 파일을 열었다.

> 🔴 **봉인 검증 한계** (2026-08-10 추가) — 봉인 대상 실물(`_s4e_nodes_TEXT.json` · `_s4e_nodes_NAMES.json`)과
> 봉인본(`s4e_verdict_stage1.json`)은 `.gitignore:84`(`scripts/output/**/*.json`) · `CLAUDE.md §4` 에 따라
> **레포에 포함되지 않는다.** 레포에는 sha256 만 남으며, **레포만으로는 봉인 검증이 불가**하다.
> 실물은 레포 밖 사본 `/Users/ohsungsu/dev/bell-agent/_archive_research1/s4_seal_20260810/` 에 보관한다
> (`R8 §0-0` 원자료 박제 관행 준용, 사본 sha256 원본 일치 확인).
**2단계** — `_s4e_nodes_NAMES.json` 을 열어 교차표를 만들고, 판정 변경을 셌다.

### 1-b. 판정 변경 = **1건** (0 이 아니다. 정직하게 계상한다)

| # | 텍스트 전문 | 1단계 | 2단계 | 변경 사유 |
|---|---|---|---|---|
| **25** | `UGC 광고, 소비자는 왜 더 선호할까? - 인사이트 \| 마케팅 인사이드` | **애매** | **크롬** | 체인이 **`title → head → html`**. 텍스트만으로는 "기사 제목(본문)인지 `<title>` 메타인지" 안 갈렸는데, 구조가 **`<title>` 요소**임을 확정했다 |

⚠️ **이 변경은 이름사전 앵커링이 아니다.** class/id 이름이 아니라 **요소 종류(`title` 태그)** 가 근거다. 그럼에도 **2단계에서 바뀐 것은 사실이므로 1건으로 센다.**
🔴 **`본문` 건수는 83으로 불변이다** — 챗 기준표에 들어가는 값은 영향받지 않았다.

| 집계 | 본문 | 크롬 | 애매 |
|---|---|---|---|
| 1단계 (텍스트만) | **83** | 17 | 3 |
| 2단계 후 | **83** | 18 | 2 |

**나머지 102건은 변경 0.** 2단계 체인은 1단계 판정을 전건 확증했다(§2-c).

---

## 2. 🔴 육안 103건 — 집계와 교차표 (본체)

### 2-a. 3분류 집계

| 판정 | 건 | 비율 |
|---|---|---|
| **본문** | **83** | **80.6%** |
| 크롬 | 18 | 17.5% |
| 애매 | 2 | 1.9% |
| 계 | 103 | 100% |

**챗 사전 기준표 대응** — `본문 ≥20%` 구간에 해당한다. 🔴 **CC 는 판정하지 않는다** (지시 §9-3).

### 2-b. 🔴 판정 × 발동근거 교차표

| 판정 | 완전일치 포함 | **부분일치만** | 트리거 0 | 계 |
|---|---|---|---|---|
| **본문** | 57 | **26** | 0 | **83** |
| 크롬 | 18 | 0 | 0 | 18 |
| 애매 | 2 | 0 | 0 | 2 |
| **계** | **77** | **26** | **0** | **103** |

🔴 **부분일치만 26건이 전부 `본문` 판정이다.** 크롬·애매 쪽에는 0건이다.
지시 §3-d 의 *"`본문` 판정 건 중 부분일치가 유일 근거인 비율이 `catch CP` 의 실규모"* → **26 / 83 = 31.3%**.

### 2-c. 부분일치만 26건 전량 (catch CP 실물)

| # | URL | 부분일치 | 체인(앞 4단) |
|---|---|---|---|
| 9–16 (**8건**) | `batcrew.co.kr` | `top` ⊂ **`desk`top** | `p → div.__post-content → div.blog-post-main → article.blog-post-single…` |
| 17–19 (**3건**) | `benefitview.co.kr` | `side` ⊂ **`in`side** | `p.wp-block-paragraph → div.entry-content → div.inside-article → article#post-11442…` |
| 26–32 (**7건**) | `inside.ampm.co.kr` | `top` ⊂ **`desk`top** | `p → div → div.a-board-content.q-mt-lg → main.q-page.a-board-view-container` |
| 48 (1건) | `mobiinside.co.kr` | `tag` ⊂ **`tag`div** | `p → div.td-post-content.tagdiv-type → div.td-ss-main-content → …` |
| 94–100 (**7건**) | `volute.eu` | `top` ⊂ **`desk`top** | `span → span._0nrb0 → p#viewer-… → div → div.w1b6n` |

체인이 전부 **본문 컨테이너**다 — `__post-content` · `entry-content` · `article#post-` · `a-board-content` · `td-post-content` · `p#viewer-`.
🔴 **규칙 R 의 이름사전 수정안은 쓰지 않는다** (지시 §9-2). 발동 근거 기재까지다.

### 2-d. 부수 관측 — **완전일치 쪽에도 의미 불일치가 있다** (판정 아님)

`본문` 83건의 발동 이름 상위에는 완전일치인데 크롬과 무관해 보이는 것이 섞여 있다.

| 이름 | 사전 항목 | 일치 | 건 |
|---|---|---|---|
| `div.margin-top-xxl._comment_body_m2024…` | `top` | **완전일치** (`margin`-**`top`**-`xxl` 의 토큰 `top`) | 34 |
| `div.margin-top-xxl._comment_body_m2024…` | `comment` | 완전일치 | 34 |
| `body#top.wp-singular.post-template-defau…` | `top` | 완전일치 (`id="top"`) | 7 |
| `div#s2025…​.section_wrap.pc_sec…` | `side` | 완전일치 | 34 |

`margin-top` 은 CSS 여백 유틸리티다. **토큰 경계를 지켜도 걸린다.** 기재만 하고 판정하지 않는다.

### 2-e. `크롬` 18건 내역 (체인으로 확증)

| 유형 | 건 | 대표 체인 |
|---|---|---|
| 기계 잔해 — IE 조건부 CSS/스크립트 · Yoast SEO 주석 · mask-icon | 10 | `head → html` |
| 기계 잔해 — MediaWiki 파서 리포트(NewPP · Transclusion) | 2 | `div.mw-parser-output → div#mw-content-text …` |
| 저작권 RDF(CC 라이선스 블록) | 3 | `div.container_postbtn → div.entry-content → div.inner` |
| SNS 공유 버튼 마크업(kakao · google) | 2 | `div.sns → div.sns-box2 → div.article_cont_area` |
| **`<title>` 메타** (#25, 2단계 재분류) | 1 | `title → head → html` |

⚠️ **기준표의 "회사소개·약관·저작권·댓글폼·메뉴·배너"에 정확히 대응하는 것은 저작권 3 + 공유버튼 2 = 5건**이고, 나머지 13건은 **기계 잔해/메타**다. 기준표에 그 칸이 없어 `크롬` 으로 넣었다. **`본문` 수(83)에는 영향이 없다.**

### 2-f. `애매` 2건 — 억지로 가르지 않았다 (지시 §9-5)

| # | 내용 | 왜 애매인가 |
|---|---|---|
| 42 | `Bronner, Kai / Hirt, Rainer (2007): Audio-Branding. …` | 위키백과 **참고문헌 서지**. 페이지 고유 내용이지만 **서술형 문장이 아니다.** 크롬(사이트 공통 요소)도 아니다 |
| 43 | `Langeslag, Patrick / Hirsch, Wilbert (2004): …` | 동일 |

2단계 체인(`li#mwAz0 → ul#mwAzc → section#mwAzY → div.mw-parser-output`)은 "문서 본문 영역 안"임을 보여주나 **"서술형 문장" 기준은 해소하지 못한다** → 애매 유지.

---

## 3. 작업 2 — 미판정 134 복원 (절단 해제, self-check #54)

**절단 규칙** — 이번 판은 `hint_detail()` 을 **전체 체인**에 적용하고 그 **판정 결과만** 저장한다(체인 원문은 육안 대상 103건에서만 전문 보존). 따라서 절단으로 인한 미판정이 **원리적으로 발생하지 않는다.**

| 구분 | 건 | 비율 |
|---|---|---|
| 완전일치 포함 | 272 | 77.9% |
| 🔴 **부분일치만** | **77** | **22.1%** |
| 트리거 0 | 0 | — |
| **계 (동일 노드 cat3)** | **349** | 미판정 **0** |

S4-d 는 `chain[:8]` 절단으로 `23 / 판정가능 215` 였다. **전량 판정 결과는 77 / 349 = 22.1%** 다.

**부분일치만 77건의 URL 분포:**

| URL | 건 |
|---|---|
| `batcrew.co.kr` | 20 |
| `benefitview.co.kr` | 20 |
| `volute.eu` | 19 |
| `inside.ampm.co.kr` | 15 |
| `mobiinside.co.kr` | 3 |

→ S4-d 가 "23 중 20이 단일 사이트" 라 유보했던 집중도가 **5개 사이트로 퍼졌다.**

---

## 4. 작업 3 — 견본 100% 크롬 URL 16개 (self-check #55)

전체 `body` **169** 기준.

| 구분 | URL 수 | body 기여 | 비율 |
|---|---|---|---|
| **견본 순도 100% 크롬** | **16** | **0** | **0.0%** |
| 견본 순도 0%(전량 본문) | 21 | **166** | **98.2%** |
| 나머지(중간) | 5+1 | 3 | 1.8% |

**16개 URL 전량** (th 병기):

| th | 견본 n | body | URL |
|---|---|---|---|
| 120 | 8 | 0 | `alien711.tistory.com/entry/2025-content-marketing-trends-strategy` |
| 120 | 8 | 0 | `batcrew.co.kr/ko/blog-insight-ugc-user-generated-content-marketing` |
| 200 | 3 | 0 | `benefitview.co.kr/숏폼-종류…` |
| 200 | 3 | 0 | `blog.theprconsulting.com/738` |
| 40 | 8 | 0 | `inside.ampm.co.kr/insight/9424` |
| 200 | 8 | 0 | `ko.creativosonline.org/오디오-브랜딩…` |
| 200 | 5 | 0 | `ko.wikipedia.org/wiki/소리_상표` |
| 120 | 8 | 0 | `sweetspot.co.kr/…idx=164451104` |
| 120 | 8 | 0 | `sweetspot.co.kr/…idx=164670038` |
| 200 | 8 | 0 | `sweetspot.co.kr/…idx=168670944` |
| 200 | 3 | 0 | `sweetspot.co.kr/…idx=168981535` |
| 200 | 7 | 0 | `sweetspot.co.kr/…idx=169616054` |
| 200 | 6 | 0 | `sweetspot.co.kr/…idx=169631315` |
| 80 | 4 | 0 | `theawesomestyle.com/entry/고객-리뷰…` |
| 120 | 8 | 0 | `volute.eu/single-post/브랜드아이덴티티…` |
| 200 | 3 | 0 | `wedidit.kr/bxdeep?bmode=view&idx=29723692` |

🔴 **지시 §5-a 가 예측한 판정 뒤집힘(크롬 문장 → body 판정)은 관측되지 않았다.** 16개 URL 의 `body` 기여는 **정확히 0** 이다. `body` 169 의 98.2% 는 견본이 **전량 본문**인 21개 URL 에서 나왔다.
(기전 추정 — 견본이 전량 크롬인 페이지에서는 그 페이지 span 의 출현이 대부분 cat 1·2·3 으로 먼저 걸러져 `extra` 계산에 도달하지 않는다. **추정이며 측정하지 않았다.**)

---

## 5. 작업 4 — 축3 역전 재해석 (self-check #56)

### 5-a. th=40 3 URL 특정

| URL | body | 견본 순도(크롬%) | 100%크롬 목록 포함 |
|---|---|---|---|
| `brunch.co.kr/@360company/25` | **54** | **0.0** | 아니오 |
| `brunch.co.kr/@emforce/720` | **35** | **0.0** | 아니오 |
| `inside.ampm.co.kr/insight/9424` | 0 | 100.0 | **예** |
| **합계** | **89 / 169 (52.7%)** | | |

🔴 **body 89건을 만든 2 URL 은 견본 순도가 0% — 전량 본문이었다.**
→ **축3 역전은 견본 오염의 산물이 아니다.** 지시 §6-a 가 상정한 기전은 실측으로 지지되지 않는다.
→ 그러나 **URL 2장이 전체 body 의 52.7%를 만든다**는 소표본 편중은 그대로다.

### 5-b. `R10c` 정정 반영 완료

`R10c §4-b` 축3 표 아래에 정정 블록을 넣었다 — **축3 역전을 (d) 유효 근거 목록에서 제외**하고 "인공물 혐의(소표본·사이트 편중)" 로 재기재. **(d) 유효 판정 자체는 정정하지 않았다** — 근거 3갈래(`extra` 잔존 전량 타 위치 / 대조② (a)51 94.8% · ≥10자 100.0% / 민감도 ±0)로 유지된다. 위 5-a 실측(견본 오염 아님)도 보론으로 병기했다.

---

## 6. 작업 5 — 기재 2건

### 6-a. `#45` 자수 확인 (지시 §7-a)

**같은 청크의 서로 다른 span 이다.** #45 의 ④확정 span 전량:

| 자수 | 문구 |
|---|---|
| **16자** | `진익준/ 브랜드경험디자인연구소` ← `R9 §7` 의 "10자 이상 1건" 이 이것 |
| **8자** | `브랜드경험디자인` ← S4-d §5-a 가 낸 8자 건 |
| 5자 | `브랜드공간` |
| 5자 | `브랜드경험` |

→ 두 기술은 **충돌하지 않는다.** 서로 다른 span 을 가리킨다. T=10 확정에는 영향 없음(8~9자 2건 존재는 어느 쪽이든 성립).

### 6-b. 챗 오판 D 기록 (지시 §12)

| # | 내용 | 상태 |
|---|---|---|
| A | (d) 기준표를 구간 분해 없이 전체값에 적용 | 확정 |
| B | 견본 체인 개수 상한 8 미검토 | 무혐의 종결 (민감도 ±0) |
| C | 견본 선택 체인을 "정의상 본문" 으로 전제 | 확정. 오염 39.8% 실측 |
| **D** | **동일 노드 cat3 비율로 ③ 오작동 측정 시도 — `cat_of_one` 이 체인만 보므로 논리적 항등** | **기록.** S4-d §3-d 기준표 폐기 |

`catch CP`(경계 없는 부분일치) 문안은 지시 §2 대로 수용. **실규모 = 본문 판정 26 / 83 = 31.3%**(§2-b) · **동일노드 cat3 전량 기준 77 / 349 = 22.1%**(§3).

---

## 7. Self-check (#51~57)

| # | 항목 | 결과 |
|---|---|---|
| **51** | 육안 103건 전건 | ✅ 누락 0. `본문 83 / 크롬 18 / 애매 2` (1단계 83/17/3) |
| **52** | **판정 순서** | ✅ **파일 분리로 물리적 강제.** 1단계 텍스트만 → sha256 봉인 → 2단계 이름. **변경 1건(#25 애매→크롬, `title` 요소 근거)**, `본문` 수 불변 |
| **53** | 발동 근거 표기 | ✅ 완전일치/부분일치 구분 + **어느 토큰의 어느 부분**까지 (`top` ⊂ `desktop` 등). §2-c |
| **54** | 미판정 134 복원 | ✅ 절단 해제 → **349 전량, 미판정 0.** 부분일치만 **77 (22.1%)** |
| **55** | 16 URL + th=40 3 URL | ✅ 전량 특정 + body 기여(16개 → **0**, th=40 2개 → **89**) |
| **56** | `R10c` 축3 정정 | ✅ 반영. (d) 유효 판정은 미정정 |
| **57** | 판정 미실시 | ✅ **③ 오작동 확정/기각 선언 없음.** 이름사전 수정안 0. 표본 규모·조건 A 의 N 없음. `cat_of_one:195` 무수정(sha256 증명) |

### 7-a. deviation (자진 보고)

| # | 내용 |
|---|---|
| 1 | 🔴 **#52 변경이 0 이 아니라 1건**이다(#25). 이름사전 앵커링이 아니라 요소 종류(`title`) 근거이나, **2단계에서 바뀐 사실 자체를 계상**했다. `본문` 수 불변 |
| 2 | 기준표의 `크롬` 예시(회사소개·약관·저작권·댓글폼·메뉴·배너)에 **기계 잔해/메타** 칸이 없다. 13건을 `크롬` 으로 넣고 §2-e 에 내역을 분리 기재했다. `애매` 로 밀면 `본문` 비율은 그대로이나 `크롬` 의 뜻이 흐려진다고 판단 |
| 3 | 지시에 없는 **§2-d(완전일치인데 의미 불일치)** 를 추가했다. `catch CP` 를 "부분일치만 고치면 끝"으로 읽으면 틀리기 때문. **판정이 아니라 관측이다** |
| 4 | §4 의 기전 추정("cat 1·2·3 에서 먼저 걸러져 `extra` 에 도달하지 않는다")은 **측정하지 않았다.** 추정으로 명시 |

---

## 8. 부록 — `probe_s4_census.py` diff (원본 사본 대조, 검증됨)

이번에는 수정 전 사본이 있으므로 **실제 `diff -u`** 다(S4-c deviation ③ 해소). 전체 153줄, 추가 109 / 삭제 1.

주요 추가 5블록:

1. **`hint_detail(chain)`** — 발동 근거를 `element`/`hint`/`token`/`exact`/`partial` 로 분해. `probe_s2_tagmap.py:195` 는 미수정.
2. **cat3 견본 노드 전건 수집** — `s4e_text`(텍스트만) / `s4e_name`(체인 전문 + 트리거) 2계열. **체인 절단 없음.**
3. **`same_full`** — 동일노드 cat3 349건을 **전체 체인**으로 재판정(작업 2).
4. **작업 3·4·5a 블록** — 견본 순도 극단 URL · th=40 · #45 span.
5. **`_s4e_nodes_TEXT.json` / `_s4e_nodes_NAMES.json` 분리 기록** — #52 순서 강제.

삭제 1줄 = 없음(빈 줄 정리 1). **`build_body_ctx` 호출부·판정 경로(`classify_occ`)는 무변경.**

---

## 9. 🔴 정지 — 챗이 확정할 것

**CC 는 ③ 오작동을 판정하지 않는다.** 값만 넘긴다.

| # | 항목 | 값 |
|---|---|---|
| **1** | **육안 103건** | **본문 83 (80.6%)** · 크롬 18 (17.5%) · 애매 2 (1.9%) |
| **2** | **교차표** | 부분일치만 **26건 전부 `본문`**. 완전일치 포함 77 (본문 57 / 크롬 18 / 애매 2) |
| **3** | **`catch CP` 실규모** | 본문 기준 **26 / 83 = 31.3%** · 동일노드 cat3 전량 기준 **77 / 349 = 22.1%** |
| 4 | 부분일치 실물 | `top` ⊂ `desktop`(22건) · `side` ⊂ `inside`(3) · `tag` ⊂ `tagdiv`(1) |
| 5 | 완전일치 의미불일치 | `margin-top`(34) · `id="top"`(7) 등 — 경계를 지켜도 걸림 |
| 6 | 견본 100% 크롬 16 URL | body 기여 **0**. 예측된 판정 뒤집힘 **미관측** |
| 7 | th=40 3 URL | body 89 (52.7%). 그 2 URL 견본 순도 **0% = 전량 본문** → **오염 아님**, 단 소표본 |
| 8 | #45 | 16자·8자는 **서로 다른 span**. 충돌 없음 |
| 9 | #52 | 변경 **1건**(#25), `본문` 수 불변 |
| 10 | ③ 오작동 · 계층 배분 · 표본 규모 · 조건 A 의 N | **전부 미확정** |
| 11 | 커밋 | **하지 않았다** (지시 §9-8) |

**S5·S6 미착수 유지.**

# R10h — CP-3 · 배치계열 · 전수 대상 집계 (§research-1 / 작업 S4-h)

- 작성 2026-08-10 · 채널 = 기술 정밀
- 지시 `CC_S4h_20260809_scope_final.md`
- **재실행 1회 승인분 집행.** 비용 **$0** · **육안 판정 0건**(전부 기계 조회)
- 🔴 **전수 대상 확정 선언 없음** (지시 §8-5) · **③ 재설계안·이름사전 수정안 0** · `cat_of_one:195` **무접촉**

---

## 0. 🔴 결론 먼저 — STOP-1 발동

```
전수 대상 총계 = ④구분가능 162 + ②header최근접 98 + ③위험군 245 = 505
```

| STOP | 값 | 기준 | 판정 |
|---|---|---|---|
| **1** | 전수 총계 **505** | > 450 | **🔴 발동 — 육안 한계 초과** |
| 2 | ③ 계 **452** | ≠ 452 면 중단 | 미발동 (**452 일치**) |
| 3 | CP-3 **188** | > 226(452 과반) | 미발동 |

**지시 §6-c 대로 중단·보고한다. 배분 재설계는 챗 소관이며 CC 는 집계표까지 낸다.**

---

## 1. 실행 환경

| 항목 | 값 |
|---|---|
| **cwd** | `/Users/ohsungsu/dev/bell-agent/bell-agent-backend/writer_project` |
| **venv** | `../.venv_openai/bin/python` |
| python / bs4 / lxml / libxml2 / soupsieve | 3.11.6 / 4.14.2 / 6.0.2 / 2.14.6 / 2.8 |

**보호** — 탐침 수정 전 원본 사본 2부(sha256 `b2d76687…`), `_s2_final.json` 백업 1부.
**실행 후 불변** — `_s2_final.json` `5c8045…` · `probe_s2_agg.py` `9db064…` · **`probe_s2_tagmap.py` `1530a6…`**(= `cat_of_one:195` 무접촉).
**회귀 확인** — S4-f 배타 `58/30/364`, S4-g 모집단 `307 (③291+②16)` 동일 재현.

---

## 2. 🔴 작업 1 — CP-3 전역 요소 (self-check #77)

### 2-a. 두 수치를 구분해 낸다 — 같은 것이 아니다

| 값 | 정의 | 수 |
|---|---|---|
| **표제 (지시 §3-b 문안)** | 전역 매칭 URL **들의** ③ 452 내 레코드 수 | **192** |
| **배타 집계용** | 레코드가 **자기 체인에 실제로 전역 트리거를 가진** 수 | **188** |
| 차 | | **4** |

🔴 **차 4건의 정체 — 전량 특정했다.** `<title>` 노드이며 체인이 **`title → head → html`** 이라 **`<body>` 를 거치지 않는다.** 그래서 `<body>` 의 전역 클래스가 이들에게는 걸리지 않는다(cat3 근거는 `head`).

| 레코드 | span | URL |
|---|---|---|
| #23 | `마케팅 인사이드` | `inside.ampm.co.kr/insight/9424` |
| #36 | `The Sound of Leffe 로고 \| brandB 브랜드비` | `brandb.net/archive/…` |
| #36 | `brandB` | `brandb.net/archive/…` |
| #57 | `[bxd] 브랜드 사운드 로고 … 비엑스딥` | `wedidit.kr/bxdeep?…` |

→ **§6 배타 집계에는 188 을 쓴다**(레코드가 실제로 그 트리거를 갖는가가 기준). 표제 192 는 지시 문안 그대로 병기한다.

**(참고) 잔여 1,241 내 레코드 = 322.** 🔴 **표제 합산에 넣지 않는다**(지시 §3-b·§8-6).

### 2-b. 전역 매칭 URL 9 / 파싱 43 (HTML 45장) — 실물 전량

| ③ | 전체 | URL | 전역 요소 실물 | 걸린 항목 |
|---|---|---|---|---|
| 0 | 0 | `alien711.tistory.com/entry/2025-content-mark…` | `body#tt-body-page.layout-aside-right.list-type-thumbnail.paging-number` | `aside=aside` · `side⊂aside` |
| 7 | 20 | `batcrew.co.kr/ko/blog-insight-ugc…` | `html.desktop.bat-blog` | `top⊂desktop` |
| **43** | 96 | `blog.theprconsulting.com/738` | `body#tt-body-page.layout-aside-right.list-type-vertical.paging-number` | `aside=aside` · `side⊂aside` |
| 29 | 46 | `brandb.net/archive/the-sound-of-leffe…` | `body.font-pretendard.drag-prevent.m-0` | `prev⊂prevent` |
| 7 | 7 | `inside.ampm.co.kr/insight/9424` | `body.desktop.no-touch.body--light` | `top⊂desktop` |
| 0 | 9 | `ko.creativosonline.org/오디오…` | `body#top.wp-singular.post-template-default.single` | `top=top` |
| 3 | 27 | `ko.wikipedia.org/wiki/소리_상표` | `html.client-nojs.vector-feature-language-in-header-…` | `header=header` · `head⊂header` |
| 4 | 14 | `theawesomestyle.com/entry/고객-리뷰…` | `body#tt-body-page.layout-aside-right.paging-number` | `aside=aside` · `side⊂aside` |
| **99** | 103 | `wedidit.kr/bxdeep?bmode=view&idx=29723692` | `body#doz_body.page_ready.doz_sys._body_menu_m2023…` | `menu=menu` |

**매칭 항목별(전역 요소 기준)** — `side/부분` 3 · `aside/완전` 3 · `top/부분` 2 · `prev/부분` 1 · `top/완전` 1 · `header/완전` 1 · `head/부분` 1 · `menu/완전` 1

🔴 **관측 2건 (판정 아님)**
1. **`wedidit.kr` 1개 URL 이 ③ 99건**을 낸다. 근거는 `<body>` id 의 `_body_menu_m2023…` → `menu` **완전일치**. 그 페이지 전 노드가 이 하나로 걸린다.
2. **`brandb.net` 은 `drag-prevent` 의 `prev`** 다. `prev`(이전 글 링크)를 노린 사전 항목이 **드래그 방지 클래스**에 걸렸다.

### 2-c. `catch CP-3` 문안 (지시 §2-c 그대로, 등재는 챗)

> `catch CP-3` — `<body>`·`<html>` 등 **전역 요소의 class/id 가 사전 항목을 포함**하면 그 페이지 **전 노드**가 크롬 판정을 받는다. 실물 `layout-aside-right`(aside) · `_body_menu_…`(menu) · `drag-prevent`(prev) · `desktop`(top) · `body#top`(top). 파급이 페이지 단위라 CP-1·CP-2 보다 크고, **정답이 나와도 근거가 무효**이므로 정답률이 규칙 성능을 과대평가한다.

---

## 3. 작업 2 — 배치·간격 접두어 계열 (self-check #78)

### 3-a. 검출 문자열 — **목록 무변경** (지시 §4-b)

```
layout-  grid-  flex-  col-  row-  spacer-  glue-  gap-
margin-  padding-  mt-  mb-  pt-  pb-
```
코드 상수 `LAYOUT_PREFIXES` 로 고정했다. **늘리거나 줄이지 않았다.**

**경계 적용 방식** — `desc()` 문자열 `tag#id.cls1.cls2` 를 **태그 / id / class 조각**으로 가른 뒤(`re.split(r"[#.]")`), **조각 단위 `startswith`** 로 본다.
→ `margin-` 은 `margin-top` 에 걸리고 **`submargin-x` 에는 걸리지 않는다.**

### 3-b. 결과 (③ 452 기준)

| 축 | 값 |
|---|---|
| 배치계열 이름 **보유** 레코드 | **65** |
| 🔴 **배치계열에서만 매칭**(부당 확정) | **33** |
| 정당 근거 병존 | 32 |

**접두어별** — `layout-` 138 · `margin-` 54 · `glue-` 22 (매칭 발생 횟수 기준)
**실물** — `layout-aside-right`(138) · `margin-top-xxl`(54) · `glue-headline`(7) · `glue-headline--headline`(4) · `glue-spacer-5-top`(4) · `glue-spacer-5-bottom`(4) · `glue-headline--headline-3`(3)

**[별도 관측 · 표제 미포함]** 목록 밖 접두어 후보(`u-`·`is-`·`js-`·`has-`·`el-`·`c-`·`o-`·`_`) = **0건**. 목록 변경 사유가 관측되지 않았다.

---

## 4. 작업 3 — `기타 top 17` 정체 (self-check #79)

**레코드 기준 총 17** — S4-f §2-c 의 17 과 **대조 일치**.

| 이름 | 레코드 | 3분류 |
|---|---|---|
| `entry-top` | 5 | 진짜 상단 구역 |
| **`glue-spacer-5-top`** | **4** | **배치·간격** (작업 2 와 중복) |
| `top` | 3 | **판단 보류** |
| `board-top` | 2 | 진짜 상단 구역 |
| `SCROLL_TO_TOP` | 1 | 기능·동작 |
| `top-news-wrap` | 1 | 진짜 상단 구역 |
| `ast-scroll-top` | 1 | 기능·동작 |
| `ast-scroll-top-icon` | 1 | 기능·동작 |
| `ast-scroll-to-top-right` | 1 | 기능·동작 |
| `cmt-top` | 1 | 진짜 상단 구역 |

**분류별 레코드 수**

| 분류 | 레코드 | 지시 §5 판정 |
|---|---|---|
| **진짜 상단 구역** | **9** | 정당 |
| 배치·간격 | 4 | 부당 |
| 기능·동작 | 2 | **판단 보류 → 챗** |
| **판단 보류** (`top` 단독) | **3** | 이름만으로 안 갈림 |

⚠️ 합 18 > 17 — **1건이 두 분류에 걸친다**(한 레코드가 이름 2개 보유). 중복 명시.
⚠️ `top` 단독 3건은 **억지로 밀지 않았다** — 클래스명이 `top` 뿐이라 상단 구역인지 앵커인지 이름만으로 판별 불가.

**지시 §5 함의(값만)** — *"17건이 전부 정당이면 CP-2 는 30 → 13"*. 실측은 정당 9 · 부당 4 · 보류 5 다. **CP-2 재산정은 챗 소관.**

---

## 5. 🔴 작업 4 — 배타 집계 (self-check #80·#81)

**우선순위 고정** (지시 §6-a): `CP-3 전역` > `CP-1 부분일치 유일` > `CP-2 top 완전일치` > `배치계열(부당 확정)` > `그 외`

| 계층 | 배타 건수 | 비중 |
|---|---|---|
| **③ CP-3 전역** | **188** | 41.6% |
| ③ CP-1 부분일치 유일 | 33 | 7.3% |
| ③ CP-2 top 완전일치 | 24 | 5.3% |
| ③ 배치계열(부당 확정) | **0** | 0.0% |
| ③ 그 외 | 207 | 45.8% |
| **③ 계** | **452** | **✅ 452 대조 일치** |

**[중복허용]** CP-3 **188** · CP-1 **58** · CP-2 **30** · 배치계열(부당 확정) **33**

🔴 **배치계열 배타가 0 인 이유** — 부당 확정 33건이 **전부 CP-3/CP-1/CP-2 에 이미 흡수**됐다. 배치계열이 없다는 뜻이 아니라 **독립 기여가 0** 이라는 뜻이다. 중복허용판 33 을 함께 읽어야 한다.

### 5-a. 전수 대상 총계

```
③ 위험군 = CP-3 188 + CP-1 33 + CP-2 24 + 배치계열 0 = 245
전수 총계 = ④-구분가능 162 + ② header 최근접 98 + ③ 위험군 245 = 505
```

### 5-b. STOP 판정 (self-check #82)

| # | 조건 | 실측 | 판정 |
|---|---|---|---|
| **1** | 전수 총계 > **450** | **505** | **🔴 발동** — 육안 한계 초과. 중단·보고 |
| 2 | ③ 계 ≠ 452 | 452 | 미발동 |
| 3 | CP-3 > 226 (452 과반) | 188 (41.6%) | 미발동 |

⚠️ **STOP-1 은 경계가 아니다** — 505 는 450 의 1.12배이고, ③ 위험군을 한 계층 통째로 빼야(예: CP-3 188 제외 시 317) 한계 안으로 들어온다. **어느 계층을 빼는지는 챗 판정이며 CC 는 선언하지 않는다.**
⚠️ STOP-3 은 41.6% 로 과반과 8.4%p 차이다. 경계로 보기 어렵다.

---

## 6. Self-check (#77~83)

| # | 항목 | 결과 |
|---|---|---|
| **77** | CP-3 산출 | ✅ 전역 매칭 URL **9/43** · **표제 = ③ 452 내 192**(URL 기준) · **배타용 188**(레코드 기준) · **차 4건 전량 특정**(`title→head→html`, `<body>` 미경유) · 1,241 참고값 **322** 를 표제와 분리 · URL별 실물 전량 |
| **78** | 배치계열 | ✅ 목록 **무변경**(코드 상수 `LAYOUT_PREFIXES` 14개). 경계 = 조각 분할 후 `startswith`. 목록 밖 후보 **0건**(별도 관측) |
| **79** | 기타 top 17 | ✅ 이름 **10종 전량** + 레코드 17 대조 일치. 3분류 = 정당 9 / 부당 4 / 기능·동작 2 / **보류 3**. **보류는 보류로** |
| **80** | 배타 우선순위 | ✅ §6-a 순서 준수. **중복허용판 병기** |
| **81** | ③ 계 대조 | ✅ **452 일치** |
| **82** | STOP 3건 | ✅ 각 값 + 발동 여부. **1 발동 / 2·3 미발동.** 경계 아님을 근거와 함께 기재 |
| **83** | 판정 미실시 | ✅ 전수 대상 확정 선언 **0** · ③ 재설계안·이름사전 수정안 **0** · **육안 0건** |

### 6-a. deviation (자진 보고)

| # | 내용 |
|---|---|
| 1 | 🔴 **1차 실행이 조용히 틀렸다.** CP-3 매칭 URL 이 **0** 으로 나왔는데, `hint_detail()` 에 **체인(리스트) 대신 문자열 1개**를 넘겨 글자 단위로 순회한 탓이다. **에러 없이 0건**이 나왔다 — `CLAUDE.md §9` "양성 대조 없는 0건은 근거가 아니다" 에 걸린 사례다. 작업4 의 CP-3 188 과 **모순**이라 발견했다. 수정 후 9 URL / 192 |
| 2 | 🔴 같은 실행에서 지역변수 `trigs` 를 `trig` 로 써 **S4-d 의 Counter 를 덮었고** 덤프 단계에서 `AttributeError` 로 죽었다. 이름 분리 후 재실행. **실행 3회 / 유효 산출 1회** |
| 3 | 표제(192)와 배타용(188)이 다르다. 지시 §3-b 문안은 URL 기준이므로 **표제는 192 로 두고**, 배타 집계에는 레코드 기준 188 을 썼다. **둘을 합산하지 않았다** |
| 4 | 작업 3 을 지시의 이름 단위가 아니라 **레코드 단위**로도 냈다. S4-f 의 17 과 대조하려면 단위를 맞춰야 하기 때문(S4-g §5 단위 혼동 재발 방지) |

---

## 7. 부록 — `probe_s4_census.py` diff

전체 247줄, 추가 229 / 삭제 1. 변경 3블록:

1. **상수·헬퍼** — `LAYOUT_PREFIXES`(14) · `GLOBAL_TAGS` · `segments()` · `is_layout_seg()` · `hint_segments()`.
2. **S4-h 본문 블록** — 작업1 CP-3 / 작업2 배치계열 / 작업3 기타 top / 작업4 배타 집계 + STOP + `_s4h_scope.json`.
3. 지역변수 `trig` → `trigs` 개명(deviation 2).

**`classify_occ`·`build_body_ctx`·`cat_of_one` 무변경.**

---

## 8. 🔴 정지 — 챗이 확정할 것

| # | 항목 | 값 |
|---|---|---|
| **1** | **STOP-1 발동** | 전수 총계 **505** > 450. 배분 재설계 필요 |
| **2** | ③ 배타 집계 | CP-3 **188** · CP-1 **33** · CP-2 **24** · 배치계열 **0** · 그 외 **207** = **452** |
| 3 | 중복허용 | CP-3 188 · CP-1 58 · CP-2 30 · 배치계열 33 |
| **4** | CP-3 표제 | URL 기준 **192** / 레코드 기준 **188** (차 4 = `<title>` 노드) · 참고 1,241 내 **322** |
| 5 | CP-3 집중 | `wedidit.kr` 1 URL 이 ③ **99건** (`_body_menu_…` menu 완전일치) |
| 6 | 배치계열 | 보유 65 · **부당 확정 33** · 목록 밖 후보 0 |
| 7 | 기타 top 17 | 정당 9 / 부당 4 / 기능·동작 2 / **보류 3** |
| 8 | 전수 대상 최종 · 계층별 표본 규모 · CP-2 재산정 | **전부 미확정 — 챗 소관** |
| 9 | 커밋 | **하지 않았다** |

**S5·S6 미착수 유지(별건 지시 진행 중).**

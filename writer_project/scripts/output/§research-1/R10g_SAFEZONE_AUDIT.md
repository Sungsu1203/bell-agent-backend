# R10g — 307 안전지대 표본 60건 검증 (§research-1 / 작업 S4-g)

- 작성 2026-08-10 · 채널 = 기술 정밀
- 지시 `CC_S4g_20260809_safezone_audit.md`
- **재실행 1회 승인분 집행.** 비용 **$0**
- 🔴 **§3-e 판정 선언 없음 · 전수 범위 미확정 · ③ 재설계안 0 · `cat_of_one:195` 무접촉** (지시 §6)

---

## 0. 실행 환경

| 항목 | 값 |
|---|---|
| **cwd** | `/Users/ohsungsu/dev/bell-agent/bell-agent-backend/writer_project` |
| **venv** | `../.venv_openai/bin/python` |
| python / bs4 / lxml / libxml2 / soupsieve | 3.11.6 / 4.14.2 / 6.0.2 / 2.14.6 / 2.8 |

**보호 조치** — 탐침 수정 전 원본 사본 2부, sha256 `06a90dedd3998f0ccb2011c279df3ed9e69b070e547dece962359fbb686baa9a` 일치. `_s2_final.json` 백업 1부.
**실행 후 — 전부 불변**: `_s2_final.json` `5c8045…` · `probe_s2_agg.py` `9db064…` · **`probe_s2_tagmap.py` `1530a6…`**(= `cat_of_one:195` 무접촉 증명).
기존 값 회귀 확인 — `A assert 1241`, S4-f 배타 분해 `58/30/364` 동일.

---

## 1. 🔴 표본 추출 (self-check #71 — 이번 차수 핵심)

### 1-a. 모집단 = **307** (실측 재현)

정의 = ③ 레코드 중 **부분일치 보유 ∧ 완전일치 보유**.

```
[S4g 작업1 · 307 표본] 모집단 = 307 · 배타 내역 {'③ 그 외 완전일치': 291, '② top 완전일치': 16}
```

⚠️ **지시 문안과의 차이 1건** — 지시 §3-a 는 307 을 *"배타 분류에서 ③ 으로 간 레코드"* 로 기술했으나, 실측하니 **291 만 ③ 이고 16 은 ②(top 완전일치)** 로 갔다. 307 이라는 수 자체는 일치하므로 **모집단은 지시대로 307 전량**을 썼고, 내역만 병기한다.

### 1-b. 추출 코드 (그대로 수록 — 재현 가능)

```python
SEED = 20260809          # 고정. 지시 작성일. 재현 가능.
SAMPLE_N = 60            # 고정. 늘리거나 줄이지 않는다.

# 결정적 정렬 후 추출 — dict 순서에 의존하지 않는다
pop307 = sorted(pop307, key=lambda x: (x[0], x[1], x[2]))     # (레코드번호, url, span)
picked = random.Random(SEED).sample(pop307, SAMPLE_N)
picked = sorted(picked, key=lambda x: (x[0], x[1], x[2]))
```

🔴 **층화 없음.** 사전 항목·URL·빈도 어느 것으로도 나누지 않았다. `random.Random(SEED).sample` = **단순 무작위 비복원 추출**.
🔴 **정렬 선행** — `dict` 순회 순서에 의존하면 재현이 깨진다. `(레코드번호, url, span)` 3키로 전순서를 만든 뒤 추출했다.

### 1-c. 추출된 60건 id 전량 (id : 레코드번호)

```
 1:#4    2:#11   3:#20   4:#22   5:#23   6:#25   7:#25   8:#31   9:#32  10:#32
11:#32  12:#32  13:#32  14:#32  15:#34  16:#34  17:#34  18:#34  19:#34  20:#34
21:#34  22:#35  23:#35  24:#35  25:#36  26:#36  27:#36  28:#36  29:#37  30:#40
31:#40  32:#40  33:#40  34:#40  35:#40  36:#41  37:#41  38:#41  39:#43  40:#50
41:#50  42:#51  43:#53  44:#54  45:#57  46:#57  47:#57  48:#57  49:#57  50:#57
51:#57  52:#57  53:#57  54:#57  55:#57  56:#57  57:#57  58:#57  59:#57  60:#57
```

URL 분포 — `wedidit.kr` 16 · `sweetspot.co.kr` 17 · `blog.theprconsulting.com` 6 · `openads.co.kr` 4 · `brandb.net` 4 · `marketup.co.kr` 3 · `jaenung.net` 2 · `batcrew.co.kr` 2 · 나머지 6 URL 각 1.
⚠️ **쏠림은 층화하지 않은 결과 그대로다.** 모집단 자체가 몇 사이트에 몰려 있다. 보정하지 않았다.

---

## 2. 🔴 판정 순서 (self-check #72) — S4-e 방식 재사용

| 단계 | 내용 |
|---|---|
| 1 | 탐침이 60건을 두 파일로 분리 출력 — `_s4g_TEXT.json`(`['id','rec','url','span','occ','chars','texts']`, 체인·이름 **없음**) / `_s4g_NAMES.json`(체인 전문 + 트리거 상세) |
| 2 | **TEXT 만** 열어 판정 (필드 목록 출력으로 이름 부재 확인) |
| 3 | 판정 봉인 — `s4g_verdict_stage1.json`, sha256 **`dcefd741c83bd2dab99e2ea6a00fd5268ff6726973f6ef36a6e8cf8ba342de98`** |
| 4 | 그 **뒤** `_s4g_NAMES.json` 개봉 |
| 5 | 변경 계상 |

> 🔴 **봉인 검증 한계** (2026-08-10 추가) — 봉인 대상 실물(`_s4g_TEXT.json` · `_s4g_NAMES.json`)과
> 봉인본(`s4g_verdict_stage1.json`)은 `.gitignore:84`(`scripts/output/**/*.json`) · `CLAUDE.md §4` 에 따라
> **레포에 포함되지 않는다.** 레포에는 sha256 만 남으며, **레포만으로는 봉인 검증이 불가**하다.
> 실물은 레포 밖 사본 `/Users/ohsungsu/dev/bell-agent/_archive_research1/s4_seal_20260810/` 에 보관한다
> (`R8 §0-0` 원자료 박제 관행 준용, 사본 sha256 원본 일치 확인).

### 2-a. 🔴 이름 확인이 판정을 바꾼 건수 = **9건**

기대값은 0 이었으나 강제하지 않는다는 지시(§3-b-5)대로 **그대로 낸다.**

| 집계 | 본문 | 크롬 | 애매 |
|---|---|---|---|
| 1단계 (텍스트만) | **0** | 47 | 13 |
| 2단계 (이름 확인 후) | **2** | 54 | 4 |

**변경 9건 전량:**

| id | 레코드 | span | 1단계 | 2단계 | 구조 근거 |
|---|---|---|---|---|---|
| 1 | #4 | `21` | 애매 | 크롬 | `div.card-footer.p-2.pt-0` — 카드 푸터 카운트 |
| **3** | **#20** | `2026년, 영향력 있는 브랜드가 되려면? …` | 애매 | **본문** | **`h1.simple-article-hero__headline` → … → `main#page-content.simple-article`** — 기사 h1 제목 |
| 4 | #22 | `99,000` | 애매 | 크롬 | `div.card-footer` — 카드 푸터 가격 |
| 30 | #40 | `(3)` | 애매 | 크롬 | `table → div.another_category…` — 관련글 표 |
| 31 | #40 | `2024.05.09` | 애매 | 크롬 | 〃 |
| 32 | #40 | `2025.05.02` | 애매 | 크롬 | 〃 |
| 33 | #40 | `2025.10.15` | 애매 | 크롬 | 〃 |
| **34** | **#40** | `※ 참고문헌` | 애매 | **본문** | **`p → div.tt_article_useless_p_margin.contents_style → div.entry-content → article#content`** — 티스토리 본문 문단 |
| 41 | #50 | `유통사 팝업스토어 대행사, 어떻게 골라야 할까?` | 애매 | 크롬 | `div.comment_section → div.inside → div.board_view` |

**성격** — 9건 전부 **1단계 `애매` 였던 것**이다. 이름사전 앵커링으로 `크롬`·`본문` 판정이 뒤집힌 사례는 **0건**이다. 텍스트로 갈린 47건은 하나도 바뀌지 않았다.
🔴 **단 변경 방향이 한쪽이 아니다** — 7건은 크롬으로, **2건은 본문으로** 갔다. 본문 2건이 곧 §3-e 의 분자다.

---

## 3. 🔴 60건 육안 결과 (self-check #73)

### 3-a. 3분류 집계

| 판정 | 건 | 비율 |
|---|---|---|
| **본문** | **2** | **3.3%** |
| 크롬 | 54 | 90.0% |
| 애매 | 4 | 6.7% |

**챗 사전 기준 대응** — `<5%` 구간에 해당한다. 🔴 **CC 는 판정하지 않는다** (지시 §6-6).

### 3-b. 🔴 `애매` 4건이 결론을 바꿀 크기다 (catch CM 적용)

| 가정 | 본문 비율 | 챗 기준 구간 |
|---|---|---|
| 애매를 어느 쪽으로도 안 밀 때(현행) | **2 / 60 = 3.3%** | `<5%` |
| **애매 4건이 전부 본문이라면** | **6 / 60 = 10.0%** | **`≥10%`** |

→ **두 구간이 정확히 갈린다.** `catch CM` 이 요구한 *"미확정 칸의 크기가 결론을 바꿀 만하면 닫으러 간다"* 에 해당하나, **이 4건은 추가 자료로 닫히지 않는다** — 성질이 다르다(아래).

### 3-c. `애매` 4건 — 닫히지 않는 이유 = **레코드 내부 혼재**

| id | 레코드 | span | 구조 |
|---|---|---|---|
| 7 | #25 | `인사이트` | occ 2 — (1) `ul.post-categories → div.__post-category → div.blog-post-header` **크롬** (2) `p → div.__post-content → article.blog-post-single` **본문** |
| 19 | #34 | `콘텐츠` | occ 2 — (1) `div#mobile_slide_menu…` **크롬** (2) `div.margin-top-xxl._comment_body_… → div.board_txt_area.fr-view` **본문** |
| 20 | #34 | `팝업스토어` | occ 26 — `title` + 메뉴 + 본문 다수 **혼재** |
| 27 | #36 | `Leffe` | occ 5 — `title → head` + 본문 영역 제목 **혼재** |

🔴 **같은 레코드가 크롬 위치와 본문 위치에 **동시에** 출현한다.** 하나로 못 가르는 것이 자료 부족 때문이 아니라 **레코드의 실제 성질**이다. 그래서 `애매`로 남긴다. (S4 §4-b 의 "양측 출현" 과 같은 계통이며, 여기서는 ③ 층에서 관측됐다.)

⚠️ 부기 — 4건 모두 **본문 위치 체인에도 사전 항목이 걸린다**(#19 `comment`·`top`·`side`, #7 `top⊂desktop`). 즉 ③ 규칙이 발동하면 크롬 출현뿐 아니라 본문 출현도 함께 걸린다. **관측이며 판정이 아니다.**

---

## 4. 🔴 트리거 귀속 교차표 (self-check #74)

**귀속 정의**

| 귀속 | 판별 |
|---|---|
| **완전일치 정당** | 완전일치 항목이 요소의 실제 역할과 부합 (`nav` in `navbar-nav`, `menu` in `_menu_wrap`, `comment` in `comment_section`) |
| **완전일치 부당** | 완전일치이나 의미 불일치 — **여백/앵커/레이아웃 서술자**: `margin-top`·`glue-spacer-5-top`·`glue-spacer-5-bottom`·`#top`·`layout-aside-right` |
| **부분일치 실질** | 완전일치가 **전부 부당** → 실제 근거로 남는 것이 부분일치뿐 |

배타 귀속 (합계 60 대조 ✅):

| 귀속 | 본문 | 크롬 | 애매 | 계 |
|---|---|---|---|---|
| 완전일치 정당 보유 | **0** | 50 | 4 | **54** |
| **부분일치 실질 (완전일치 전부 부당)** | **2** | 4 | 0 | **6** |
| **계** | **2** | **54** | **4** | **60** |

중복 라벨 — **완전일치 `부당` 을 하나라도 보유한 레코드 = 12** (정당도 함께 가진 6 포함).

🔴 **본문 2건은 둘 다 `부분일치 실질` 그룹에서 나왔다. `완전일치 정당 보유` 54건의 본문은 0 이다.**

### 4-a. 본문 2건의 트리거 실물

| id | 완전일치 | 판별 | 부분일치 |
|---|---|---|---|
| **3** (#20) | `div.glue-page.**glue-spacer-5-top**.**glue-spacer-5-bottom**` → `top`·`bottom` | **부당** (CSS 여백 유틸리티) | `head ⊂ headline` ×4 |
| **34** (#40) | `body#tt-body-page.**layout-aside-right**.list-type-vertical.paging-number` → `aside` | **부당** (페이지 레이아웃 서술자 — 이 노드가 aside 안이라는 뜻이 아니다) | `side ⊂ aside` |

**id 3 은 `h1` 기사 제목**이고, **id 34 는 티스토리 본문 문단**이다. 둘 다 완전일치가 여백/레이아웃 클래스에서 왔다.

⚠️ **지시 밖 관측 1건** — `layout-aside-right` 는 **`<body>` 태그의 클래스**다. 조상 최상단에 붙으므로 **그 페이지의 모든 노드**가 `aside` 완전일치를 얻는다. `margin-top` 류(요소 국소)와 달리 **페이지 전역**이다. 30·31·32·33 이 크롬으로 맞은 것도 근거는 이 `aside` 였다(정답이지만 근거가 다르다). **관측이며 판정·재설계안이 아니다.**

---

## 5. §4 — 사전 항목별 완전/부분 내역 (self-check #75)

③ **452 레코드** 보유 기준. **6항목 전부 수록, 0건도 0으로 기재.**

| 항목 | 총 레코드 | 완전일치 | 부분일치 | 대표 부분일치 실물 |
|---|---|---|---|---|
| `menu` | 190 | **180** | **36** | `slidemenu`(28) · `dropdownmenubutton`(4) · `jnmobilemenu`(2) · `menubar`(2) |
| `nav` | 133 | **129** | **121** | **`navbar`(120)** · `naver`(1) |
| `side` | 91 | **35** | **81** | `aside`(46) · **`inside`(33)** · `sidebar`(2) |
| `head` | 79 | **41** | **49** | `header`(41) · `heading`(5) · `headline`(3) · `sharemodalheader`(2) |
| `top` | 55 | **32** | **25** | **`desktop`(13)** · `topbar`(12) |
| `tag` | 44 | **27** | **17** | **`tagdiv`(13)** · `tags`(4) |

⚠️ **`menu`·`nav` 도 부분일치가 있다** — 0건이 아니다. 다만 성질이 다르다:
- `nav ⊂ navbar`(120) · `menu ⊂ slidemenu`(28) · `menubar` — **의미상 정당한 부분일치**(진짜 내비게이션)
- `nav ⊂ naver`(1) — **네이버**. 부당
- `side ⊂ inside`(33) · `top ⊂ desktop`(13) · `tag ⊂ tagdiv`(13) — 부당
- `side ⊂ aside`(46) · `head ⊂ header`(41) — 사전에 `aside`·`header` 항이 따로 있어 **중복 발동**

🔴 **총계가 S4-f §2-d 와 다르다** — S4-f 는 `menu 188 · nav 133 · side 90 · head 56 · top 49 · tag 38`. 이번 값은 `190 / 133 / 91 / 79 / 55 / 44`.
**원인 = 세는 단위가 다르다.** S4-f 는 **레코드 하나당 항목 1회**(`set` 합산), 이번 §4 는 **완전·부분을 각각 센 뒤 보유 레코드 수**를 냈다. `head` 56 → 79 차이가 큰 것은 `header`·`heading` 등 부분일치가 별도로 잡혔기 때문이다. **정정이 아니라 단위 차이이며, 두 표를 섞어 쓰지 않는다.**

---

## 6. Self-check (#71~76)

| # | 항목 | 결과 |
|---|---|---|
| **71** | 표본 추출 | ✅ **SEED=20260809 · N=60 · `random.Random(SEED).sample`** (단순 무작위 비복원, **층화 0**). 결정적 정렬 선행. **코드 수록**(§1-b). **60건 id 전량 수록**(§1-c). 모집단 **307 대조 일치** |
| **72** | 판정 순서 | ✅ TEXT/NAMES **파일 분리** + 봉인 sha256 `dcefd7…`. **변경 9건** — 전부 1단계 `애매` 였던 건이며, 텍스트로 갈린 47건은 **변경 0** |
| **73** | 60건 전건 | ✅ 누락 0. **본문 2 / 크롬 54 / 애매 4** |
| **74** | 귀속 교차표 | ✅ 합계 **60 대조 일치**. 완전일치 정당 54(본문 0) / 부분일치 실질 6(본문 2). 중복라벨 부당 보유 12 |
| **75** | §4 6항목 | ✅ `menu`·`nav` 포함 6항목 전부. **부분일치 0건 항목 없음**(전부 실측값 기재). S4-f 와의 단위 차이 명시 |
| **76** | 판정 미실시 | ✅ §3-e 판정 선언 **0** · 전수 범위 **0** · ③ 재설계안·이름사전 수정안 **0** · 표본 규모 변경 **0**(60 고정) |

### 6-a. deviation (자진 보고)

| # | 내용 |
|---|---|
| 1 | 🔴 **#72 변경이 9건**이다(기대 0). 전부 1단계 `애매` 에서 이동했고 그중 **2건이 `본문`** 이라 §3-e 분자를 만들었다. 이름 앵커링에 의한 크롬↔본문 뒤집힘은 0건이나, **애매를 닫는 데 구조를 쓴 것 자체를 변경으로 계상**했다 |
| 2 | 지시 §3-a 의 307 기술("배타 ③ 으로 간 레코드")과 실측이 다르다 — **291 은 ③, 16 은 ②(top)**. 수는 307 로 일치하므로 전량을 모집단으로 썼고 내역을 병기했다(§1-a) |
| 3 | §4 총계가 S4-f §2-d 와 다르다. **세는 단위 차이**이며 정정이 아니다. 두 표를 섞지 말라고 명시했다(§5) |
| 4 | 지시에 없는 **`layout-aside-right` 관측**(§4-a)을 추가했다. `<body>` 클래스라 **페이지 전역**에 걸린다는 점이 `margin-top` 류와 성질이 달라 기재했다. **판정·재설계안 아님** |
| 5 | 표본 URL 이 `wedidit.kr` 16 · `sweetspot.co.kr` 17 로 쏠렸다. **층화 금지 지시대로 보정하지 않았고** 사실만 기재했다 |

---

## 7. 부록 — `probe_s4_census.py` diff (원본 사본 대조)

전체 103줄, 추가 79 / 삭제 2. 변경 3블록:

1. `import random` 추가.
2. ③ 트리거 수집부에 **`texts`·`chains`** 저장 추가(육안 재료). 노드 텍스트 300자 초과 시 절단 표시.
3. **S4-g 블록** — 307 모집단 구성 · 시드 고정 무작위 추출 · `_s4g_TEXT.json`/`_s4g_NAMES.json` 분리 기록 · §4 사전 항목별 완전/부분 집계.

**`classify_occ`·`build_body_ctx`·`cat_of_one` 무변경.**

---

## 8. 🔴 정지 — S4 계열 최종. 챗이 확정할 것

| # | 항목 | 값 |
|---|---|---|
| **1** | **307 표본 60건** | **본문 2 (3.3%)** · 크롬 54 (90.0%) · 애매 4 (6.7%) |
| **2** | 🔴 **애매 4건 감도** | 어느 쪽으로도 안 밀면 **3.3%(`<5%`)**, 전부 본문이면 **10.0%(`≥10%`)** — **두 구간이 갈린다** |
| 3 | 애매 4건 성질 | **레코드 내부 혼재**(크롬·본문 동시 출현). 추가 자료로 닫히지 않는다 |
| **4** | **귀속 교차표** | 완전일치 정당 보유 54 → **본문 0** / 부분일치 실질 6 → **본문 2** |
| 5 | 완전일치 부당 보유 | **12 / 60** |
| 6 | 본문 2건의 부당 트리거 | `glue-spacer-5-top/bottom`(h1 기사 제목) · `layout-aside-right`(티스토리 본문 문단) |
| 7 | 사전 항목 완전/부분 | `menu` 180/36 · `nav` 129/121 · `side` 35/81 · `head` 41/49 · `top` 32/25 · `tag` 27/17 |
| 8 | 표본 추출 재현 | SEED **20260809** · 코드·id 60건 전량 수록 |
| 9 | 307 안전 여부 · 전수 대상 최종 · 계층별 표본 규모 | **전부 미확정 — 챗 소관** |
| 10 | 커밋 | **하지 않았다** |

**S5·S6 미착수 유지(별건 지시 진행 중).**

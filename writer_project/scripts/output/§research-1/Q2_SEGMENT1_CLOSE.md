# §research-1 Q-2 종결 — 선분 1 (쿼리 생성) 요건 확정

- 작성: 2026-08-06
- 원자료: `q2_zdouble_20260806-210440.json` (Z''), `q1_arms_20260806-161606.json` (Y·Z') — 둘 다 `.gitignore:84` 로 커밋 제외
- 드라이버: `probe_q2_zdouble.py` (`.gitignore:106 probe_*` 로 추적 제외)
- 비용: Z'' 실행 ≈ $0.015 (LLM 5콜 + 임베딩 17콜). Y·Z' 재측정 0회

---

## 🔴 0. 적용 상태 — 반드시 먼저 읽을 것

**`prompts.py` 는 수정되지 않았다.** 이 문서의 Z'' 문안은 드라이버(`probe_q2_zdouble.py`)
안의 런타임 `str.replace` 결과로만 존재한다. `get_research_planner_prompt()` 는
여전히 Z 원본(`prompts.py:589-623`)을 반환한다.

- 확인 명령: `git status --short prompts.py` → 출력 0줄이어야 정상
- 이 문서를 근거로 `prompts.py` 를 고치지 말 것. 반영 방식·시점은 §2 에 있다

---

## 1. 판정 (챗 소관) — 요건 확정

**4칸 중 1번 칸 = 술어 ↑ + 분해 ↑. 선분 1 요건 확정.**

- 축1 술어 보존 **Z'' 78% vs Z' 58% (+20%p)**, 연도 **0% → 75%**, 병기 국문만 **4쌍 → 2쌍**
- 축3 **arm X 회귀 없음** — 자수 20~51자 (X는 133~189자), 갈래 대응 obj1·2·5 정확, 새 개념어 0건
- → **규칙 1(술어 보존)과 규칙 3(갈래 분해)이 한 쌍으로 작동함이 확인됐다.**

### 🔴 CLAUDE.md §7 첫 정량 실증

> "억압형(~하지 마라)은 실패하고 요구형(~를 유지하라)이 통한다"

| | 규칙 1 슬롯 | 연도 보존 |
|---|---|---|
| Z' | 삭제만 (억압형 제거) | **0/4** |
| Z'' | 보존 요구 신설 (요구형) | **3/4** |

**대조군 있는 측정으로 확인된 최초 사례.** §7 본문에 이 수치를 근거로 추가 예정
(등재·문안은 챗 소관).

---

## 2. 코드 반영 방식 — **미정** (2026-08-06 현재)

🔴 방식 (나) "리서치 전용 함수 신설" 은 **기각됐다.**
사유: `get_research_planner_prompt()` 실호출부가 `research_planner.py:229` **1곳**이며,
그 1곳이 챗 UI 본선이다(`/api/run` → `run_once` → 컴파일 그래프 → planner 노드).
v2 를 추가해도 `:229` 가 무엇을 부르냐만 남으므로 **트랙 분리가 성립하지 않는다.**

### 확정된 사실 3건 (정찰 실측 — §3)

| 항목 | 실물 |
|---|---|
| 호출부 | `research_planner.py:229` **1곳**. import 1 · 드라이버 2 · 문서 2 외 없음 |
| 진입 경로 | **3종** — `graph.py:120` supervisor_router / `graph.py:159` after_synthesizer_router(라운드 재진입) / `core/routers.py:980`. 태스크 주입처 3곳(`supervisor.py:767`·`:789`, `research_synthesizer.py:341`) |
| 토글 | `ENABLE_RESEARCH_PLANNER` = CFG `<absent>` · env None → **기본 True**, `.env` 정의 0건. **항상 등록된다** |

⚠️ **`vector_search_agent` 와 다르다.** R3a 의 B안(노드 직접 호출로 그래프 우회)이
여기서 성립하는지는 **미확인**이며, planner 는 진입 3종 + 라운드 재진입 루프 안에 있다.
**B안을 전제로 설계하지 말 것.**

### 후보 3안 (전부 미결)

| | 방식 | 미해결 |
|---|---|---|
| **(다)** | `:229` 조건 분기 | 조건 소스가 없다. env 추가 = 4계층 ENV 를 광고 트랙과 공유하는 위험 / slug 분기 = catch AB 계열 |
| **(라)** | 리서치 전용 planner 노드 신설 | 그래프 수정. 챗 UI 그래프 공유 |
| **(마)** | state 로 프롬프트 주입 (`state.get(...) or 기본값`) | state 조립이 화이트리스트(CARRY 4키)인데 프롬프트를 나르는 게 맞는지 미확인. `research_planner.py` 수정이며 그 파일도 챗 UI 본선 |

### 🔴 결정 시점 = 아래 3건이 정해진 뒤. 지금 정하지 않는다

1. 우리 파이프라인이 planner 를 **그래프 안에서 쓸지** (R3-2 루프 설계 미착수)
   — **그래프 밖에서 쓴다면 이 문제가 통째로 소멸한다**
2. state 조립 방식이 프롬프트 주입을 수용하는가
3. Z'' 문안 확정 — 이월 3건(obj4 취약 / 영문 소실 2건 / 자수 하한 6건) 미해결 (§8)

⚠️ 현재 `prompts.py` **미수정**. Z'' 는 `probe_q2_zdouble.py` 문자열로만 존재한다.

---

## 3. 정찰 — `research_planner` 호출부 (2026-08-06, $0 읽기 전용)

§2 방식("호출부가 어느 함수를 부르는지로만 분기")의 전제를 확인한 결과다.
**판정은 하지 않는다** — 근거만 싣는다.

### 3.1 `get_research_planner_prompt()` 실호출 지점 — **전량 1곳**

| # | 파일:라인 | 성격 | 근거 |
|---|---|---|---|
| 1 | `agent/research_planner.py:229` | **본선 (챗 UI 경로)** | `planner_prompt = get_research_planner_prompt()` — 노드 본문 |
| — | `agent/research_planner.py:14` | import 문 | 호출 아님 |
| — | `probe_q1_arms.py:481` · `probe_q2_zdouble.py:523` | **드라이버 전용** | `probe_*` = `.gitignore:106` 추적 제외. 그래프 밖 |
| — | `scripts/output/§paper-writer-1/step_a_audit.md:60`, `§research-1/R2a_DESIGN_INPUTS.md:157` | 문서 인용 | 코드 아님 |

검색 명령: `git -c core.quotePath=false grep -n "get_research_planner_prompt" -- .`
및 `grep -rn --include="*.py"` (untracked 포함). **양쪽 결과 일치.**

### 3.2 호출 사슬 — 진입점까지

```
[HTTP]  @web_app.post("/api/run")            app.py:1340-1341  async def api_run()
             │
             ├─ asyncio run_in_executor(run_once, …)   app.py:1377
             ▼
        def run_once(state, user_input, recursion_limit)   app.py:1036
             │
             ├─ graph_obj = _get_graph_cached()      app.py:1169
             │       └─ _load_graph() → build_graph()  app.py:540-542 · 571-577
             ▼
        _invoke_graph(graph_obj, state, …)       app.py:1178
             │
             ▼
        build_graph()                             graph.py:60
             ├─ _add_node("research_planner", research_planner, _EN_PLANNER)   graph.py:104
             ├─ START → "supervisor"                                          graph.py:108-110
             ├─ supervisor_router → {… "research_planner": "research_planner"} graph.py:113-121
             ├─ after_planner_router (planner 이후 분기)                       graph.py:128-135
             └─ after_synthesizer_router → "research_planner" (재진입)         graph.py:157-159
             ▼
        agent/research_planner.py:27  def research_planner(state)
             ▼
        :229  planner_prompt = get_research_planner_prompt()   ← 유일 호출 지점

[콘솔] if __name__ == "__main__":   app.py:2203
             └─ run_once(state, user_input, …)   app.py:2506   → 위와 동일 사슬
```

### 3.3 판정 재료 (결론은 챗 소관)

**(a) 호출 지점 성격** — 본선 1곳. 드라이버 2곳은 `probe_*`로 추적 제외이며 그래프 밖에서 직접 부른다.

**(b) 챗 UI 도달 가능성 — 도달함.**
`/api/run` → `run_once` → `build_graph()` 컴파일 그래프 → `research_planner` 노드.
콘솔 경로(`app.py:2506`)도 같은 `run_once`를 탄다. **경로 2개, 사슬은 동일.**

**(c) 그래프 연결 — 연결돼 있고, 우회 가능한 위치가 아니다.**
- 노드 등록: `graph.py:104`
- 진입 경로 **3종**: `supervisor_router`(`:120`) / `after_synthesizer_router`(`:159`, 라운드 재진입) / `core/routers.py:980` (`after_synth` 계속 연구 시 `return "research_planner"`)
- 태스크 주입처: `agent/supervisor.py:767` `:789` (research_mode bootstrap·preemption), `agent/research_synthesizer.py:341` (`plan_next`)

**(d) 토글 실측** — `_EN_PLANNER = bool(_get_cfg_attr("ENABLE_RESEARCH_PLANNER", True))` (`graph.py:32`).
CFG·env 어디에도 정의돼 있지 않아 **기본값 True**로 항상 등록된다.
```
ENABLE_RESEARCH_PLANNER = CFG:'<absent>'  env:None    → 기본 True
```
정의처는 `graph.py:32`와 `app.py:564`(그래프 캐시 시그니처) 2곳뿐. `.env` 정의 0건.

⚠️ **`vector_search_agent` 와 다르다.** R3a 에서 vector_search 는 드라이버가 노드를 직접 불러
그래프를 우회할 수 있었다. planner 는 진입 경로가 3종이고 라운드 재진입 루프에 들어 있어
같은 방식의 우회가 성립하는지는 **이 정찰 범위 밖**이다.

---

## 4. 선분 1 기준 문안 — Z'' 전문

원자료 `q2_zdouble_20260806-210440.json` 의 `zdouble_template` 필드 직독 (1,189자).
`_tmpl()`(dedent+strip) 적용 후 형태이며 `PromptTemplate.from_template()` 에 넣는 문자열이다.

```
현재 주제: {topic_title}
역할: Research Analyst. 아래 목표를 달성하기 위해 **검색 질의 2~3개**만 작성한다.
(각 질의는 짧고 단순하게, 한 번에 조건을 많이 넣지 말 것)

- 목표: {objective}
- 기존 레퍼런스 개요: {references}

엄격한 작성 규칙:
1) 🔴 **목표에 등장한 고유 술어는 표기 그대로 옮긴다.**
   이론가명·연도·개념어·영문 술어를 임의로 번역하거나 한국어로 바꾸지 않는다.
   목표에 국문과 영문이 병기돼 있으면 **둘 다 유지한다.**
   (예: 목표에 `확증 편향(confirmation bias)` → 질의에 `확증 편향 confirmation bias`)
   (예: 목표에 `조절 초점 이론(regulatory focus theory)` → 질의에 `조절 초점 이론 regulatory focus theory`)
2) {topic_title}를 그대로 복사하지 말고, **핵심 키워드 1~2개만** 추려 포함한다.
   (예: 체험마케팅, 브랜드 경험, 경험경제)
3) 🔴 **목표를 다 담으려 하지 말고 나눈다.** 목표가 여러 갈래를 담고 있으면
   질의 하나에 한 갈래씩 배정한다. 한 질의에 갈래를 합치지 않는다.
4) **연산자 기본 금지**: 큰따옴표(", 구문검색), +, |, AND/OR, 괄호 연산은 사용하지 않는다.
   - 예외(가능하면 최소): 쇼핑/체험단 노이즈가 확실할 때만 `-체험단` 또는 `-광고` 중 1개만 허용.
5) **도메인 필터(site:)는 기본적으로 사용하지 않는다.**
   - 예외: 정부/공공 통계·규제 문서가 필요할 때만 **한 도메인 1개**(예: site:mfds.go.kr).
6) 최신성이 정말 중요할 때만 **1개 질의에 한해** 연도(예: 2025 2026)를 포함한다.
7) 정보 의도는 서로 다르게 구성한다(예: 이론 원전, 실행 사례, 측정·실증).
8) 중복 금지. 각 질의는 **25~80자**로 간결하게.
9) 출력 형식: 부호/번호/설명 없이 **한 줄에 쿼리 1개**만.

# 예시(참고용, 출력에 포함 금지):
# 페스팅거 1957 인지부조화 이론 cognitive dissonance 원전
# 넛지 선택 설계 choice architecture 공공정책 적용 사례
# 카너먼 트버스키 1979 prospect theory 실험 검증 논문

출력:
```

### 4.1 설계 불변식 3건 (문안 수정 시 깨지면 안 됨)

1. **번호 재부여 없음** — 규칙 1·3 슬롯을 채우되 2·4·5·6·7·8·9 는 원래 번호 유지.
   재번호하면 7줄의 선두 문자가 바뀌어 "나머지 문자열 전부 동일" 조건이 깨진다.
2. **예시는 타 도메인 고정** — 형식(국문+영문 병기)만 시연하고 이번 토픽 술어를 넣지 않는다.
   1회차 문안이 `분트 곡선(Wundt curve)`·`슈미트 1999 … ExPro` 를 예시로 노출해
   **측정 대상을 측정 도구 안에 넣은** 사고가 있었다 (챗 발 오판 33).
   하단 예시 1줄은 arm Y 쿼리와 byte 완전 일치였다.
3. **규칙 7 의 3축과 하단 예시 3줄은 한 쌍** — 이론 원전 / 실행 사례 / 측정·실증에 1:1 대응.
   본문만 두고 예시만 바꾸면 둘이 충돌해 새 변수가 된다.

### 4.2 잔여 유출 1건 (기록)

규칙 2 예시 `(예: 체험마케팅, 브랜드 경험, 경험경제)` 의 **`경험경제` 는 obj1 술어**다.
Q-1 D3(=Q-2 F2)에서 들어왔고 Z' 와 동일 문자열이라, 고치면 Z'↔Z'' diff 3건 설계가 깨진다.
→ 보고에서 obj1 분모를 **11 + 1(예시 노출분)** 로 분리 계상했다. 문안은 무변경.

---

## 5. diff 계보 — Z → Z' → Z''

### 5.1 Z (원본, `prompts.py:589-623`)

규칙 1 = 한국어 전용 / 규칙 3 = 국내 맥락 강제 /
규칙 2 예시 = 키성장 건기식(타 도메인 잔재) / 규칙 7 축 + 하단 예시 3줄 = 광고 축.

### 5.2 Z → Z' (Q-1, diff 4건)

| 태그 | 내용 |
|---|---|
| **D1** | 규칙 1 `**반드시 한국어**로만 작성한다. 영어 단어/라벨 금지.` — **삭제** |
| **D2** | 규칙 3 `국내 맥락 키워드(한국\|국내\|대한민국 중 1+)를 포함한다.` — **삭제** |
| **D3** | 규칙 2 예시 `(예: 키성장, 건강기능식품, 키성장 건기식)` → `(예: 체험마케팅, 브랜드 경험, 경험경제)` |
| **D4** | 규칙 7 축 `통계/보고서, 브랜드 비교, 소비자 고민/후기` → `이론 원전, 실행 사례, 측정·실증` **+ 하단 예시 3줄 교체** (본문과 예시는 한 쌍) |

Z 대비 **−7 / +5줄**. 방향 = 광고 프레임 제거(단일 방향).

### 5.3 Z' → Z'' (Q-2, diff 3건)

| 태그 | 내용 |
|---|---|
| **E1** | 규칙 1 슬롯(Z'에서 공란) → **술어 보존 요구 신설** (5줄) |
| **E2** | 규칙 3 슬롯(Z'에서 공란) → **갈래 분해 지시 신설** (2줄) |
| **E3** | 하단 예시 3줄 → **타 도메인(사회심리·행동경제) 술어 병기 시연으로 전량 교체** |

Z' 대비 **−3 / +10줄**. Z 원본 대비로는 **−7 / +12줄**
(F1~F5 5건 중 F2·F4 는 Z' 와 동일 문자열이므로 Z' 대비 diff 에 안 잡힌다).

### 5.4 검증 절차 (재현 시 그대로 쓸 것)

- 원본 문자열에 `str.replace` 적용. **손으로 옮겨 적지 않는다**
- 치환마다 `assert count(old)==1` → `assert old not in out` → `assert new in out`
- `_MUST_BE_GONE` 13건 잔존 검사 (구 문안 8 + Q-2 재승인분 5)
- `_MUST_KEEP` 6건 유지 검사 (`-체험단`·`site:mfds.go.kr`·`25~80자`·플레이스홀더 3)
- `difflib.unified_diff` 전량 출력 — **Z' 대비·Z 대비 둘 다**
- 술어 69개 전량을 프롬프트 전문에 대고 유출 스캔
- ⚠️ 집합 비교(`l not in other_lines`)는 **옛 줄이 남고 새 줄만 추가된 경우를 못 잡는다.**
  잔존 assert 가 필수다

---

## 6. 측정 결과 — 판정축 1~4

### 6.0 측정 조건 (Y·Z'·Z'' 공통)

```
model gpt-4o · temperature 0.0 (객체 실측) · N=1 · references=""
RAG_DISTANCE_THRESHOLD 1.10 (무변경) · RAG_TOP_K 6 (무변경) · RETRIEVE_WEB_RATIO 0.65 (무변경)
M1-a 창 = agent/vector_search.py:360 _split_k(6) = web 4 / local 2   ← import 호출, 하드코딩 0
n_results = 50 (드라이버 인자, C7-a 저촉 아님)
RESEARCH_PLANNER_MAX_Q 미적용 · _core_subst 미적용(국내 편향 재주입 차단)
색인 web 416 / local 302 · dim 3072 (sqlite3 -readonly 확인, Chroma 개방 전)
```
Y·Z' 는 Q-1 값을 재사용했다. 위 5개 조건이 전항 일치함을 실행 시 출력으로 확인했다.

### 6.1 판정축 1 · 술어 보존율 (1순위)

분모 = objective 원문을 **육안**으로 읽어 확정한 고유 술어. 정규식 추출 0.
연도는 분리 계상한다 — `홀브룩 허쉬만 1982` 가 신호이지 `1982` 가 아니며,
분리해야 "이름은 죽고 연도만 살아남음" 같은 병리가 보인다.

| obj | 분모 | Y | Z' | **Z''** |
|---|---|---|---|---|
| 1 | 본술어 11 (+예시노출 1) | 10 (+1) | 7 (+1) | **11 (+1)** |
| 1 | 연도 3 | 3 | **0** | **3** |
| 2 | 본술어 14 | 12 | 10 | **11** |
| 3 | 본술어 12 | 8 | 7 | **11** |
| 4 | 본술어 14 | 10 | 4 | **7** |
| 4 | 연도 1 | 1 | 0 | **0** |
| 5 | 본술어 13 | 12 | 9 | **10** |
| **본술어 계** | **64** | **52 (81%)** | **37 (58%)** | **50 (78%)** |
| **연도 계** | **4** | **4 (100%)** | **0 (0%)** | **3 (75%)** |

#### 병기 술어 5쌍의 국문/영문 생존 (요건 1 핵심)

| 쌍 | Y | Z' | Z'' |
|---|---|---|---|
| 분트 곡선 / Wundt curve | 둘 다 | **국문만** | **둘 다** |
| 감정 곡선 / emotion arc | 둘 다 | **국문만** | **둘 다** |
| 경험 격자 / experiential grid | 둘 다 | **국문만** | **국문만** |
| 고객 여정 맵 / customer journey map | 둘 다 | **국문만** | **국문만** |
| SEM / 전략적 경험모듈 | 둘 다 | 둘 다 | 둘 다 |

Z' 는 5쌍 중 4쌍에서 영문이 죽었고, Z'' 는 2쌍에서 죽었다.

#### Z'' 미보존 항목

- obj2: `Feel(감성)` · `감각적 일관성` · `감각 과부하`
- obj3: `experiential grid`
- obj4: `customer journey map` · `참여지표` · `체류시간` · `UGC 생성률` · `재방문` · `브라커스` · `자란토넬로` · 연도 `2009`
- obj5: `릴스` · `틱톡` · `페스티벌`

#### 🔴 A-1-a 육안 판정 근거 — 짧은 국문 술어 히트 전량 (Z'')

**판정 방식**: substring 은 **후보 추출 전용**. 판정은 쿼리 원문 대조로 한다.
사후 정정이 아니라 사전 회피가 이 트랙의 절차다 (Q-1 A-2 의 `자리` 오탐 20건 처리 방식 승계).

| 술어 | 표기 | 쿼리 | 원문 발췌 | 판정 |
|---|---|---|---|---|
| `Think(인지)` | 인지 | obj3-1 | `슈미트 SEM **인지** 행동 관계 모듈 실행 사례` | **일치** — 모듈명. `인지부조화` 아님 |
| `Act(행동)` | 행동 | obj3-1 | `SEM 인지 **행동** 관계 모듈` | 일치 |
| `Relate(관계)` | 관계 | obj3-1 | `인지 행동 **관계** 모듈` | 일치 |
| `Sense(감각)` | 감각 | obj2-1 | `슈미트 SEM **감각** 모듈 사운드 로고` | 일치 |
| `역U자` | 역U자 | obj2-2 | `자극 강도 선호 **역U자** 관계 연구` | 일치 |
| `팬덤` | 팬덤 | obj3-4 | `**팬덤** 커뮤니티 기반 브랜드 콜라보` | 일치 (팬덤형/팬덤화 아님) |
| `몰입(flow)` | 몰입 | obj4-2 | `칙센트미하이 **몰입** 이론 마케팅` | 일치 (몰입형/몰입감 아님) |
| `4차원` | 4차원 | obj4-3 | `브랜드 경험 척도 **4차원** 측정문항` | 일치 |
| `브랜드 충성도` | 충성도 | obj4-3 | `측정문항과 **브랜드 충성도** 연구` | 일치 (전체 표기 존재) |
| `네이티브` | 네이티브 | obj5-1 | `숏폼 **네이티브** 브랜드 콘텐츠` | 일치 (네이티브 광고 아님) |

**오탐 0건.** dry-run 에서 예상한 `인지부조화` 오탐은 발생하지 않았다 —
Z'' 는 프롬프트 예시를 베끼지 않고 objective 의 `인지(Think)` 를 그대로 옮겼다.
**타 도메인 예시로 교체한 판단이 유효했다는 관측.**

### 6.2 판정축 2 · M1-a (본선 창 web 4 / local 2)

| arm | n_q | M1a web | M1a local | 합계 | 쿼리당 |
|---|---|---|---|---|---|
| Y | 13 | 21/52 | 24/26 | 45 | 3.46 |
| Z' | 16 | 39/64 | 26/32 | 65 | **4.06** |
| **Z''** | 17 | 29/68 | 24/34 | 53 | **3.12** |

#### Z'' objective별

| obj | 쿼리 | web | local |
|---|---|---|---|
| 1 | 3 | **0/12** | 6/6 |
| 2 | 3 | 5/12 | 4/6 |
| 3 | 5 | 4/20 | 2/10 |
| 4 | 3 | 8/12 | 6/6 |
| 5 | 3 | **12/12** | 6/6 |

#### 🔴 축 2 하락에 대한 챗 해석 (⚠️ **미검증** — 이월 등재)

> Z'' M1a 3.12/q < Z' 4.06/q. 그러나 **obj1 web 0/12 · local 6/6** 이 원인의 대부분이다.
> **catch BX 재확인으로 읽는다** — web 에 이론 자료가 없으므로 정확한 이론 쿼리는
> web 에서 0 이 나오는 것이 정상이다. Q-1 arm X 는 뭉툭한 쿼리로 통과를 벌었으나
> 가져온 것이 뷰티 팝업 블로그였다 (catch BZ).
> → **M1-a 는 코퍼스에 자료가 있을 때만 쿼리 품질을 반영한다.**

⚠️ 확정하려면 Z'' obj1 web 상위 청크의 M4(입자도)가 필요하나 **지금 하지 않는다** —
크롬 29%(catch CA) 상태에서 입자도 관측은 의미가 없다. 선분 3 이후로 미룬다.

### 6.3 판정축 3 · 분해 적정성

**objective당 쿼리 수** — Z'' `[3, 3, 5, 3, 3]` = 17 (Z' `[3,3,4,3,3]`=16, Z `[3,5,4,3,3]`=18).
obj3 만 프롬프트 상한 3을 초과했다(5개).

#### 갈래 매핑 (육안)

| obj | 원문 갈래 | Z'' 쿼리 배정 |
|---|---|---|
| 1 | 홀브룩·허쉬만 / 파인·길모어 / 슈미트 = **3** | obj1-1~3 → **3:3 정확** |
| 2 | 감각·감성 실행 / 분트 곡선 / 감정 곡선 = **3** | obj2-1~3 → **3:3 정확** |
| 3 | 인지·행동·관계 모듈 / 인터랙티브 / UGC 챌린지 / 팬덤 콜라보 / 경험격자 = **5** | obj3-1~5 → **5:5** (상한 초과했으나 갈래 수와 일치) |
| 4 | 고객여정맵 / 몰입 / 참여지표 / 브랜드경험척도 = **4** | obj4-1~3 → **3:4, `참여지표` 갈래 누락** |
| 5 | 숏폼 / 팝업·phygital / AI·VR = **3** | obj5-1~3 → **3:3 정확** |

#### 어휘 중복률

정의: 쿼리를 공백으로 자른 토큰 중 objective 원문에 substring 으로 존재하는 비율.
**형태소 분석이 아니다.** 값만 보지 말고 토큰 목록과 함께 읽을 것.

| arm | 평균 | 1.00인 쿼리 | 최저 |
|---|---|---|---|
| Y | 0.97 | 10/13 | 0.82 (2-c) |
| Z' | 0.89 | 9/16 | 0.57 (obj1-2) |
| **Z''** | **0.95** | **11/17** | **0.67 (obj4-1)** |

Z'' 에서 objective 밖 토큰은 전부 **의도 표시어**였다: `논문`·`사례`·`연구`·`온오프`·`체험마케팅`.
새 개념어를 만들어 넣은 건 **0건**.

#### arm X 회귀 여부 — 회귀 없음

자수 Z'' **20~51자** vs arm X **133~189자**. 자릿수가 다르다.
중복률 0.95 는 각 쿼리가 objective 의 **한 갈래만 담고 그 갈래의 어휘를 그대로 쓴** 결과이지
objective 전체를 복사한 것이 아니다.

### 6.4 판정축 4 · min dist (보조)

| obj | Y web | Y local | Z' web | Z' local | **Z'' web** | **Z'' local** |
|---|---|---|---|---|---|---|
| 1 | 1.2059 | 0.5538 | 1.0989 | 0.6091 | **1.1787** | **0.7166** |
| 2 | 0.5450 | 0.6343 | 0.8563 | 0.9884 | **0.8506** | **0.6741** |
| 3 | 1.0458 | 0.7128 | 0.9190 | 1.0323 | **0.9094** | **0.7630** |
| 4 | 1.1577 | 0.7379 | 0.9981 | 0.5749 | **1.0529** | **0.6469** |
| 5 | 0.7883 | 0.9508 | 0.6813 | 0.8708 | **0.7290** | **0.8738** |

### 6.5 예상 실패 지점 2건 — 관측 결과

#### ① 규칙 1 ↔ 규칙 8(25~80자) 충돌 → **관측되지 않음. 반대 방향이 나왔다.**

자수 초과 **0건**. 대신 **하한 미달(25자 미만) 6건**:

| 쿼리 | 자수 | 원문 |
|---|---|---|
| obj4-1 | 20 | 고객 여정 맵 작성법 체험마케팅 사례 |
| obj3-3 | 21 | UGC 참여 챌린지 참여 설계 메커니즘 |
| obj3-4 | 21 | 팬덤 커뮤니티 기반 브랜드 콜라보 사례 |
| obj3-5 | 22 | 온오프 통합 캠페인 경험 격자 구성 사례 |
| obj4-2 | 22 | 칙센트미하이 몰입 이론 마케팅 적용 연구 |
| obj3-2 | 24 | 반전 퀴즈형 인터랙티브 콘텐츠 설계 메커니즘 |

**상한도 하한도 조정하지 않았다.** 관측만 기록.

#### ② 규칙 1 ↔ 규칙 2 당김 (X 회귀) → 6.3 참조. 회귀 없음.

---

## 7. Z'' raw_output 전문

```
--- obj1 (정규화 후 3개) ---
홀브룩 허쉬만 1982 경험적 소비 3F 정보처리 패러다임 비판 논문
파인 길모어 1998 경험경제 4E 경험 연출 개념 사례
슈미트 1999 전략적 경험모듈 SEM 5유형 경험제공수단 ExPro 비교 연구

--- obj2 (정규화 후 3개) ---
슈미트 SEM 감각 모듈 사운드 로고 비주얼 아이덴티티 사례
분트 곡선 Wundt curve 자극 강도 선호 역U자 관계 연구
감정 곡선 emotion arc 설계 브랜드 필름 공감 서사형 캠페인 2023 2026 사례

--- obj3 (정규화 후 5개) ---
슈미트 SEM 인지 행동 관계 모듈 실행 사례
반전 퀴즈형 인터랙티브 콘텐츠 설계 메커니즘
UGC 참여 챌린지 참여 설계 메커니즘
팬덤 커뮤니티 기반 브랜드 콜라보 사례
온오프 통합 캠페인 경험 격자 구성 사례

--- obj4 (정규화 후 3개) ---
고객 여정 맵 작성법 체험마케팅 사례
칙센트미하이 몰입 이론 마케팅 적용 연구
브랜드 경험 척도 4차원 측정문항과 브랜드 충성도 연구

--- obj5 (정규화 후 3개) ---
숏폼 네이티브 브랜드 콘텐츠 알고리즘 확산 사례
팝업스토어 브랜드 체험관 온오프 통합 phygital 사례
AI VR 가상인간 기반 가상경험 마케팅 2025 2026 동향
```

---

## 8. 이월 3건 — 전부 원인 가설 미부여

### ① obj4 취약

- 14개 중 7개 보존. `참여지표` 갈래에 쿼리 미배정 (4갈래인데 3개 생성)
- 챗 관측 추가: obj4 는 **유일한 4갈래**이고 프롬프트는 "2~3개"를 요구한다
- ⚠️ 단 **obj3 이 5개를 냈으므로 상한 충돌 가설은 반례가 있다.** 이월

### ② 영문 소실 2건

- `experiential grid` · `customer journey map` 이 국문만 남음
- ⚠️ `Wundt curve`(2어절)는 살고 `experiential grid`(2어절)는 죽어 **어절수 패턴이 성립하지 않는다**
- 실무 영향은 크다 — Q-1 관측 (b)에서 `experiential grid` 유무가 local min dist
  **0.713 vs 1.178 (차 0.46)** 을 만든 항목이다. 이월

### ③ 자수 하한 미달 6건

- 전부 obj3·obj4. **obj3 이 M1-a 최약(web 4/20)인 것과 겹친다**
- ⚠️ **규칙을 추가하지 말 것.** 검증 없는 보완은 이 사이클에서 하지 않는다. 이월

### (추가) M1-a 해석 한계 — 6.2 참조

**M1-a 는 코퍼스에 자료가 있을 때만 쿼리 품질을 반영한다.** 미검증. 선분 3 이후 재확인.

---

## 9. 다음

**선분 1 종결.** 다음은 **선분 3(색인 위생)** 정찰이며 범위는 챗에서 정한다.

선분 3 이 선행하는 근거 (Q-1 catch CA):
web 통과 청크의 **29%(19/65, M1-a 창 기준)** 가 사이트크롬(nav·푸터·사업자정보·직원명단)이고
최소 dist **0.669** 로 유의미 자료보다 가깝다. local 은 0건.
→ **쿼리 개선분이 색인 오염에 상쇄된다.**

⚠️ 크롬 제거 코드는 **파서 위치·현행 처리 방식 미정찰 상태**다. 지시 전 착수 금지.

# R10f — ③ 452 트리거 분해 · S4 계열 마감 (§research-1 / 작업 S4-f)

- 작성 2026-08-09 · 채널 = 기술 정밀
- 지시 `CC_S4f_20260809_trigger_scope.md`
- **재실행 1회** (§3-a 가 기존 덤프로 불가 — 근거 §1-b). 비용 **$0**
- 🔴 **③ 재설계안·이름사전 수정안을 쓰지 않는다** (지시 §7-2) · **§3-b 판정을 하지 않는다** (§7-3) · **표본 규모 확정 안 함** (§7-4)
- 🔴 `cat_of_one:195` **무접촉**

---

## 0. 실행 환경

| 항목 | 값 |
|---|---|
| **cwd** | `/Users/ohsungsu/dev/bell-agent/bell-agent-backend/writer_project` |
| **venv** | `../.venv_openai/bin/python` |
| python / bs4 / lxml / libxml2 / soupsieve | 3.11.6 / 4.14.2 / 6.0.2 / 2.14.6 / 2.8 |

**보호 조치** — 탐침 수정 전 원본 사본 2부(`scratchpad/` · `_archive_research1/`), sha256 `35f2ee5213139fa5710fcc89493c9de66376d089cd9c3d39eba2401b380f8b07` 일치. `_s2_final.json` 백업 1부.

**실행 후 대조 — 전부 불변** (self-check #61):

| 파일 | sha256 |
|---|---|
| `_s2_final.json` | `5c8045…` |
| `probe_s2_agg.py` | `9db064…` |
| **`probe_s2_tagmap.py`** | **`1530a6…`** ← `cat_of_one:195` 무접촉 증명 |

기존 값 전량 회귀 확인 — `A assert 1241`, 대조① `N=965`, 절단해제 `349`, 견본 순도 `body 169`, `STOP-1 922` 모두 이전과 동일.

---

## 1. 재실행 사유 (self-check #58)

### 1-a. 먼저 기존 덤프로 시도했다

`_s2_final.json` 의 `reps['3']`(범주별 대표 체인)로 §3-a 를 낼 수 있는지 실측했다.

### 1-b. 🔴 불가 — 두 가지 이유

| 이유 | 실측 |
|---|---|
| **절단** | `reps` 는 `probe_s2_tagmap.classify` 가 `h["chain"][:8]` 로 잘라 저장한다. ③ 452 중 **65건(14.4%)** 은 절단본 안에 트리거가 **보이지 않는다.** 체인 길이 분포에서 **385건(85.2%)이 길이 8** = 절단 의심 |
| **대표 1건** | `reps` 는 범주별 **첫 출현** 체인만 담는다(catch CL). 레코드의 다른 cat3 출현은 다른 트리거를 가질 수 있다 |

→ 지시 §3-a 단서(*"불가하면 재실행 1회를 승인"*)에 따라 재실행했다. **전체 체인**으로 산출한다.

### 1-c. 재실행하지 않고 처리한 것

**§3-b(44건 판정 분포)는 기존 덤프만으로 냈다** — `_s4e_nodes_NAMES.json` + S4-e 육안 판정 봉인본 교차.

---

## 2. 🔴 §3-a — ③ 452 트리거 분해 (본체, self-check #59)

단위 = **레코드 452** (쌍 451 아님. 지시 §3-a 의 계가 452). **조회 실패 0.**

### 2-a. 배타 분류 — **표제**

우선순위: 부분일치 유일 근거 > `top` 완전일치 보유 > 그 외 완전일치.

| 트리거 유형 | 건수 | 비중 | 성격 |
|---|---|---|---|
| **① 부분일치 유일 근거 (CP-1)** | **58** | **12.8%** | 경계 없는 부분일치가 유일 근거 |
| **② `top` 완전일치 보유 (CP-2)** | **30** | **6.6%** | 토큰 경계를 지켜도 걸림 |
| ③ 그 외 완전일치 | **364** | **80.5%** | 위 둘에 해당하지 않음 |
| (트리거 0 / 조회 실패) | 0 | 0% | — |
| **계** | **452** | **100%** | 452 대조 ✅ |

**CP-1 + CP-2 = 88건 (19.5%)**

### 2-b. 중복 허용

| 유형 | 보유 레코드 | 비중 |
|---|---|---|
| 완전일치 보유 | 394 | 87.2% |
| 부분일치 보유 | 365 | 80.8% |
| **부분일치 유일 근거** | **58** | **12.8%** |
| **`top` 완전일치 보유** | **30** | **6.6%** |

🔴 **부분일치를 하나라도 가진 레코드는 365건(80.8%)** 이다. 그중 307건은 완전일치도 함께 가져 배타 분류에서 ③으로 갔다.

### 2-c. `top` 완전일치 30건 내역 (레코드 보유 기준)

| 유형 | 레코드 |
|---|---|
| 기타 `top` 완전일치 | 17 |
| `margin-top` | 13 |
| `id=top` | 0 |

### 2-d. 사전 항목별 보유 레코드 상위 (완전·부분 합산)

| 항목 | 레코드 | 항목 | 레코드 |
|---|---|---|---|
| `menu` | **188** | `comment` | 48 |
| `nav` | **133** | `aside` | 47 |
| `side` | 90 | `widget` | 41 |
| `head` | 56 | `tag` | 38 |
| `top` | 49 | `foot` · `footer` | 34 · 34 |
| | | `header` | 29 |

⚠️ `side` 90 에는 `inside` 부분일치가, `top` 49 에는 `desktop` 부분일치가 섞여 있다(완전일치는 30).

### 2-e. 🔴 해석 제약 — **모집단이 다르다** (이 표를 육안 결과와 곱하지 말 것)

| 집단 | 정체 | URL 수 |
|---|---|---|
| **육안 103건** | `body_ctx` 가 고른 **본문 견본 노드** 중 cat3 인 것 | 18 |
| **③ 452** | 잔여 모집단의 **크롬 문구 span 레코드** | 34 |

교집합 URL 15 · ③ 전용 19 · 103 전용 3.

→ *"CP-1 은 정밀도 0%"* 는 **103 표본에서 관측된 값**이다. 452 의 CP-1 58건이 전부 본문이라는 뜻이 **아니다.** 452 에 대한 육안은 하지 않았다. **전수 대상 범위 판단은 챗 소관** (지시 §7-4).

---

## 3. §3-b — `top` 완전일치의 판정 분포 (self-check #60)

### 3-a. 🔴 건수 정정 — **41 이 아니라 44** 다

지시 §2-a 의 "41건(34+7)" 은 `R10e §2-d` **상위 20 표에서 잘린 값**이다. 전량 재집계 결과:

| 요소 | 유형 | 노드 |
|---|---|---|
| `div.margin-top-xxl._comment_body_m2024102878aa9338c7` | margin-top | 34 |
| `div.margin-top-xxl._comment_body_` | margin-top | **3** ← 상위 20 밖이라 누락됐던 것 |
| `body#top.wp-singular.post-template-default.single` | id=top | 7 |
| **계** | | **44** |

### 3-b. 판정 교차표 (육안 103건과 교차)

연결 방식 — `_s4e_nodes_NAMES.json` 의 `id` 로 S4-e 육안 판정(2단계 확정본, #25 반영)과 join. 중복허용/배타가 **동일**하다(두 유형을 겸하는 노드 0).

| 유형 | 본문 | 크롬 | 애매 | 계 | 본문% |
|---|---|---|---|---|---|
| `margin-top` | **37** | 0 | 0 | 37 | **100.0%** |
| `id=top` | **7** | 0 | 0 | 7 | **100.0%** |
| **계** | **44** | **0** | **0** | **44** | **100.0%** |

**챗 사전 기준 대응** — `≥50%` 구간에 해당한다. 🔴 **CC 는 판정하지 않는다** (지시 §7-3).

**부기** — CP-1(26)과 CP-2(44)는 **서로소**다(CP-1 은 정의상 완전일치 0). 합 70 / 103 = 68.0%. 나머지 33건은 `top` 외 완전일치로 걸린 본문 13 + 크롬 18 + 애매 2 다.

---

## 4. §3-d — `catch CP` 분할 문안

원장 등재 여부는 챗 판단. **CC 는 문안만 기재한다.**

> **`catch CP-1`** — `cat_of_one`(`probe_s2_tagmap.py:195`)의 이름사전 대조가 **경계 없는 평문 부분일치**다(`k in " ".join(chain).lower()`). `inside`←`side` · `desktop`←`top` · `tagdiv`←`tag` 로 본문 컨테이너가 크롬 판정된다. 견본 103 표본에서 **26건 전건 본문(정밀도 0%)**, ③ 452 에서 **58건(12.8%)**. `팝업스토어`←`팝업` 오탐과 동일 기전. **경계 매칭으로 해결 가능.**
>
> **`catch CP-2`** — 토큰 경계를 지켜도 남는 **의미 불일치**. `margin-top` 의 `top` 은 CSS 여백이지 페이지 상단이 아니고, `id="top"` 도 앵커일 뿐이다. 견본 103 표본에서 **44건 전건 본문(100%)**, ③ 452 에서 **30건(6.6%)**. **경계 매칭으로 해결되지 않아 사전 항목 자체의 재검토가 필요하다.**

⚠️ 두 문안의 "103 표본" 수치와 "452" 수치는 **모집단이 다르다**(§2-e). 곱하거나 합산하지 않는다.

---

## 5. §4 — 챗 오판 E 기록 + (d) 강화 함의

**예측** (S4-e 지시 §5-a) — *"견본이 전량 크롬이면 크롬 문장이 `body` 로, 본문 문장이 `mid` 로 판정이 뒤집힌다."*

**실측 기각** — 견본 순도 100% 크롬 16 URL 의 `body` 기여 = **0**. `body` 169 의 **98.2%(166건)** 가 견본 순도 **0%(전량 본문)** 인 21 URL 에서 나왔다.

🔴 **(d) 강화 함의 (지시 §4 요구, 1줄)** — 대조② `(a)51` 의 **body 94.8% 는 오염된 견본 위에서 산출된 값이 아니다.** 판정기는 깨끗한 견본에서 작동했고, 오염된 견본은 body 를 만들지 못했다.

**축3 재기재 완료** (§3-c) — `R10c` 를 지시 문안대로 확정했다: *"인공물 혐의 해소(th=40 2 URL 견본 순도 0%). 단 2 URL 이 전체 body 의 52.7% 인 소표본이므로 (d) 근거 목록에는 불채택."* **(d) 유효 판정은 미정정.**

---

## 6. 챗 오판 누적 + CC 이탈 (양쪽 기재, 지시 §10)

### 6-a. 챗 발 5건

| # | 내용 | 상태 |
|---|---|---|
| A | (d) 기준표를 구간 분해 없이 전체값에 적용 | 확정 |
| B | 견본 체인 개수 상한 8 미검토 | 무혐의 종결 (민감도 ±0) |
| C | 견본 선택 체인을 "정의상 본문" 으로 전제 | 확정 (오염 39.8%) |
| D | 동일 노드 cat3 비율로 ③ 오작동 측정 시도 — 논리적 항등 | 확정 |
| **E** | 견본 오염이 body 판정을 뒤집는다고 예측 — 실측 기여 **0** | **확정** |
| (F) | CP-2 규모를 **41**로 기재 — 실제 **44**(상위 20 절단) | 본 문서 §3-a 에서 정정 |

### 6-b. CC 발 3건 (전부 자진 보고·해소)

| # | 내용 | 해소 |
|---|---|---|
| 1 | S4·S4-b 를 `.venv_vertex` 로 실행 (ad 트랙 규약 위반) | `catch CO` 승인. 3겹 증거로 값 유지 |
| 2 | `probe_s2_agg.py` 재실행 미수행 (승인 범위 충돌) | 3겹 대체 증거 제시 |
| 3 | 수정 전 사본 미보관 → diff 미수록 | S4-d 부터 사본 2부 선행. S4-e·S4-f 는 실물 diff 수록 |

---

## 7. Self-check (#58~62)

| # | 항목 | 결과 |
|---|---|---|
| **58** | 재실행 여부 | ✅ **1회 실행.** 사유 = §3-a 가 기존 덤프로 불가(`reps` 절단으로 **65/452 트리거 불가시** + 대표 1건). §3-b 는 **재실행 없이** 기존 덤프로 산출 |
| **59** | **③ 452 분해** | ✅ **중복허용/배타 2벌.** 표제 = **배타**. 계 **452 대조 일치**, 조회 실패 0. 배타 = CP-1 **58** / CP-2 **30** / 그 외 **364** |
| **60** | 44건 교차표 | ✅ 합계 대조 — 지시의 41 은 상위 20 절단값이며 **실제 44**(37+7). 연결 방식 = `_s4e_nodes_NAMES.json` `id` ↔ S4-e 육안 봉인본 join, #25 2단계 확정본 반영 |
| **61** | `cat_of_one:195` 무접촉 | ✅ `probe_s2_tagmap.py` sha256 **`1530a6…` 실행 전후 동일** |
| **62** | 판정 미실시 | ✅ ③ 재설계안 **0** · 이름사전 수정안 **0** · 표본 규모 **0** · §3-b 판정 선언 **0** · 전수 대상 범위 **미확정** |

### 7-a. deviation (자진 보고)

| # | 내용 |
|---|---|
| 1 | 지시가 "41건" 으로 준 CP-2 규모를 **44** 로 정정했다(§3-a). 근거 = 전량 재집계. 상위 20 표를 전수로 읽은 것이 원인 |
| 2 | 1차 실행이 `UnboundLocalError` 로 실패했다(`tri3` 선언 위치). **선언을 루프 앞으로 옮겨 재실행**했다. 실패 실행은 산출물을 쓰기 전에 죽었고 보호 대상 sha256 불변을 확인했다. **실행 횟수는 2회**이나 유효 산출은 1회 |
| 3 | 지시에 없는 **§2-e(모집단 구분)** 를 추가했다. 103 표본의 "정밀도 0%" 를 452 에 그대로 곱하면 틀리기 때문. **판정이 아니라 제약 기재다** |

---

## 8. 부록 — `probe_s4_census.py` diff (원본 사본 대조)

전체 121줄, 추가 91 / 삭제 1. 변경 3블록:

1. **`top_family(element)`** 신설 — `#top` / `margin-top` / 기타로 가른다.
2. **모집단 루프에 `cat == "3"` 분기 추가** — `hint_detail(h["chain"])` 을 **전체 체인**에 적용해 쌍별 `exact`/`partial`/`topfam` 집계. `classify_occ` 경로 무변경.
3. **§3-a 집계·출력 블록** — 배타/중복허용 2벌 + `top` 내역 + 사전 항목별.

삭제 1줄 = `tri3` 선언 위치 이동(deviation 2).
**`build_body_ctx`·`classify_occ`·`cat_of_one` 무변경.**

---

## 9. 🔴 정지 — S4 계열 마감. 챗이 확정할 것

| # | 항목 | 값 |
|---|---|---|
| **1** | **③ 452 배타 분해** | CP-1 **58 (12.8%)** · CP-2 **30 (6.6%)** · 그 외 완전일치 **364 (80.5%)** |
| 2 | ③ 452 중복허용 | 부분일치 보유 **365 (80.8%)** · 완전일치 보유 394 |
| 3 | `top` 완전일치 30 내역 | 기타 17 · `margin-top` 13 · `id=top` 0 |
| 4 | 사전 항목 상위 | `menu` 188 · `nav` 133 · `side` 90 · `head` 56 · `top` 49 |
| **5** | **CP-2 판정 분포** | **44건 전건 본문 (100%)** — `margin-top` 37 · `id=top` 7 |
| 6 | CP-1 ∩ CP-2 | **서로소.** 103 표본에서 합 70 / 103 = 68.0% |
| 7 | 🔴 모집단 제약 | 육안 103(견본 노드, 18 URL) ≠ ③ 452(span 레코드, 34 URL). **곱하지 말 것** |
| 8 | 전수 대상 최종 범위 · 계층별 표본 규모 · 라벨 봉인 착수 | **전부 미확정 — 챗 소관** |
| 9 | 커밋 | **하지 않았다** |

**S5·S6 미착수 유지.**

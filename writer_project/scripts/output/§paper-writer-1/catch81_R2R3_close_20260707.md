# catch 81 — R2 배선 + R3 유료 통제런 종결 박제

**작성일**: 2026-07-07
**성격**: 수정 배선(R2) + 인과 확정 유료 통제 생성런(R3) 결과 박제. R1 규명(`catch81_numbering_feedback_loop_report_20260707.md`)의 후속·종결.
**결론**: 한 런 내부 numbering feedback loop(shift→leak→재사용→재shift) = **종결**. 필요조건 절단 + 인과 실증 완료.

---

## 계보 한 줄
- **catch 80** (`dcbb7f12`) = 섹션-로컬 `[[N]]` → 글로벌 승격 (섹션↔footer 정합).
- **catch 81 R1** = catch 80의 글로벌 shift 산물이 `previous_sections`로 다음 섹션 writer 프롬프트를 오염 = 한 런 내부 feedback loop 규명.
- **catch 81 R2/R3 (본 박제)** = leak 채널 절단 배선 + 유료 통제런으로 인과 확정·종결.

---

## 1. R2 배선 요약 (working tree → 커밋)

**전략**: writer가 이전 섹션 글로벌 `[[N]]`을 프롬프트에서 보고 복사→재shift 하는 고리③을,
`paper_section_writer.py:58` 단일 chokepoint에서 strip으로 절단. 최종 조립은 무손상.

| 파일 | 변경 | 내용 |
|---|---|---|
| `agent/paper_section_writer.py` | `import re` 추가 + `:58` | `prev_text`에서 `\[\[\d+\]\]` strip + 이중공백 정규화. **프롬프트로 나가는 로컬 복사본만** 정제 |
| `prompts.py` | `:439` [citation 규칙] +3줄 | "[[N]]은 본 section [참고 자료 요약] K개 로컬 번호만; previous_sections prose의 인용번호 재사용·도입 금지" (defense-in-depth) |

**무손상 보장 (recon T3 지뢰3 근거)**: `measure_paper`의 `section_bodies`(글로벌 마커 상태)는
① `:273` previous_sections(leak) **와** ② `:290` 최종 `paper_body`/③ `:299` 반환 이 **동일 리스트 공유**.
→ driver-side strip은 최종 마커 mutate. **writer-side(:58) 로컬 복사본 strip만 안전** → `section_bodies`·footer 정합 불변.

**`prompts.py:450`("표기 일관성") = 불변** (recon T1: "표기"=용어/변수 ≠ [[N]]. strip과 무충돌).

---

## 2. R3 유료 통제 생성런 — 인과 확정

**통제 조건 (매우 깨끗)**: fetch가 결정론적이라 R3가 baseline과 **references 89개 byte-identical**
(offsets 0/15/33/53/72, grade_dist complete43/partial41/missing5, backends openalex60/ss29 전부 동일).
→ **참조는 상수로 고정, writer 본문만 변수(=fix)**. 단일변수 통제 실험 성립.

- 산출물: `paper_..._20260707_020918.md` (vertexai/gemini-2.5-flash, `--topic="consumer perceived trademark similarity and likelihood of confusion"`, SKIP_VERTEX_SEARCH=true).
- 런 1회 (task 규정: 1회 성공기준 충족 = 확정).

### 성공기준 4종 판정 (전/후)

| 판정 | baseline (004833) | R3 (020918) | 결과 |
|---|---|---|---|
| **1 out-of-range** | 9 occurrence / **8 distinct** | **0 / 0** | ✅ PASS |
| **2 2-hop 사슬** | 1 (`[[112]]` = 26→59→112) | **0** | ✅ PASS |
| **3 footer 정합** | max marker 112 **> 89 (FAIL)** | max 82 **≤ 89**, 89 chunk 동일 | ✅ PASS |
| **4 측정축 무이동** | axis1 0.944/WARN, axis3 PASS/1.0 | **동일** (grade·backends까지 identical) | ✅ PASS |

### 가짜-PASS 반증 (후반-섹션-제로 은폐 배제)
5개 섹션 **전부 인용 존재 & 전량 in-range**: Intro 8 / TheoBg 7 / PropFw 3 / ResDes 2 / Expected 1.
baseline 오매칭 집중부가 전부 정상화 — PropFw 5건→3건(raws 3·7·12≤20), ResDes 2-hop→2건(raws 1·2),
Expected 2건→1건(raw 10). **후반 섹션이 인용을 회피해 문제를 숨긴 게 아니라 실제로 로컬-스코프로 착지.**

**인과 결론**: 참조 상수·offset 상수 하에 fix만으로 out-of-range 8→0, 2-hop 소멸.
R1 §2가 유료 통제런 몫으로 남긴 인과(writer 실제 복사) = **확정**. writer는 leaked 글로벌 마커를 복사하고 있었고, 채널을 끊자 오매칭이 사라졌다.

---

## 3. ⚠️ 정찰 관찰 (헛발질 방지 — R1 §5 묘비명과 같은 예방)

1. **구두점 앞 단일 공백 잔재**: `"...multidimensional [[26]]."` → strip 후 `"...multidimensional ."`.
   `[ \t]{2,}`만 정규화하는 스펙 그대로의 결과. 이는 **leak 채널 입력(프롬프트 컨텍스트)일 뿐 최종 출력 무관·prose 무손상 → 무해**. 스펙 밖이라 **미수정**. (나중 "이 공백 버그냐" 재질문 방지)

2. **본문 마커 수 52→21 감소**: 생성 비결정성 + fix로 복사-마커 소거의 복합. **4종 판정 어디도 위반 안 함**
   (axis1 참조품질 불변, 전 섹션 인용 유지·in-range). 단일 런 변동이라 fix에 인과귀속 불가 = 관찰 기록만.

---

## 4. Landmine / DRIFT 기록 (재질문 방지)

- **LANDMINE — `_CITE_MARKER_RE` 공유 불가**: 정규식은 `measure_paper.py:225`에 있고 `measure_paper:155`가
  이미 `agent.paper_section_writer`를 import(script→lib 방향). 역import는 **순환**. → `agent/`에서 inline
  `\[\[\d+\]\]` 사용이 **유일 비순환 옵션** (self-check "import 재사용"은 위상상 불가). 동일 매치, 캡처그룹만 불요.
- **DRIFT 1 — `:58` fallback 보존 필수**: 실제 `:58` = `... .strip() or "(없음 — 첫 section)"`. task 원본 replacement가
  이 fallback을 누락 → 그대로 적용 시 첫 섹션 previous_sections가 `""` 렌더(동작 변화). **fallback 보존함** (dry T2 PASS로 검증).

---

## 5. 변경 파일 (종결 커밋)
- `agent/paper_section_writer.py` (+`import re`, `:58` strip)
- `prompts.py` (`:439` 로컬-스코프 명확화)
- `scripts/output/§paper-writer-1/catch81_R2R3_close_20260707.md` (본 박제)
- `README-dev-§14.md` (catch 81 close 엔트리)
- **제외**: `c_paper_measurement.json`(R3로 갱신됐으나 관행상 종결 커밋 제외 — working tree uncommitted 잔존), output `paper_..._020918.md/.docx`(untracked, 미add). baseline JSON은 scratchpad 백업 보유.

---

## 6. 전제 유지 (안 건드림)
- `[[58]]` Beebe = faithfulness 표적 제외 (R1 §4). R3에서 faithfulness 재개 안 함.
- dedup(89 chunk / 53 고유) = 별 트랙 잔존 (R1 §6). 이번 종결 무관.
- catch 78/79/axis3 = 이미 landed, 무접촉. axis1 0.944 WARN(결손 5) = catch 79 종결 상태 그대로 재현.

---

## status: closed (2026-07-07)
한 런 내부 numbering feedback loop = leak 채널(`:58`) strip + `:439` 명확화로 절단.
유료 통제런(references 상수) 4종 PASS로 인과 확정 (out-of-range 8→0, 2-hop 소멸).
**re-entry 조건**: (1) 마커 수 변동이 딜리버러블 인용밀도에 유의미하게 걸리면 재검토, (2) 다른 토픽/섹션수에서
out-of-range 재출현 시 leak 잔여 경로 재진단, (3) faithfulness 트랙 재개는 43 aligned 마커 중 재선정(R1 §4).

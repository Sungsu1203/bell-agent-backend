# §academic-5 entry sketch (2026-05-22)

> 박제 chain reference
> - 직전 cycle: §academic-4 cycle close (commit 4 영역, verdict PARTIAL)
> - 본 sketch 위치: §academic-4 Phase 4 commit 4 영역 박제 (Claude Code 자율 영역, 다음 cycle entry 시점 사용자 컨펌 후 영역 확정)
> - 본 sketch 영역 처분: §academic-5 entry pre-brainstorm 시점 사용자 컨펌 후 Step A audit 진입

---

## 1. 진입 사유

§academic-4 verdict PARTIAL — ratio 임계 미달 (mean 0.3915 < 0.60 threshold) 영역.

root cause: **vertex 비학술 dilution** (per-backend ratio 0.15~0.29 영역, forbes / medium / marketing blog 비중 ↑ 본질 영역).

§academic-4 cycle scope (architecture 영역, catch 51 fix S1) 안 처분 불가 — §academic-5 vertex academic mode 정합 보강 + 산식 layer 변경 영역 이전 정합.

---

## 2. 핵심 영역 (4 영역)

### 2-1. vertex academic mode 정합 보강 (HIGH 영역)

| 영역 | 값 |
|---|---|
| 현재 영역 | vertex per-backend ratio 0.15~0.29 (academic-en 5 runs 평균 0.17) |
| 목표 영역 | vertex per-backend ratio ~0.50+ |
| 효과 영역 | ratio mean 0.39 → ~0.60+ 영역 도달 (catch 51 임계 충족 영역) |

#### 검토 영역
- (a) **vertex allowed_domains 영역 강화** — gatekeep 영역 vertex layer 영역에도 적용 (현재 gatekeep 은 legacy 영역 만)
- (b) **vertex prompt template academic 영역 강화** — 학술 source 우선 영역 prompt 박제 (academic mode q_lang!=ko 시점)
- (c) **vertex search filter 영역** — 학술 keyword 강제 영역 (`site:*.edu` / `site:*.ac.*` 영역 또는 academic domain whitelist)
- (d) **vertex grounding tool 옵션 영역** — `Tool(google_search=GoogleSearch())` 빈 객체 영역 (catch 51 A1-a 영역) 의 학술 reach toggle 영역 재검토 (§academic-4 Step A 시점 DEAD 판정 — 정책 변경 영역 또는 별 옵션 영역 검토)

### 2-2. 산식 layer 변경 (catch 66 영역, MID 영역)

| 영역 | 값 |
|---|---|
| 현재 산식 | `academic_set ∩ all_uniq / all_uniq` |
| 함정 | 분모 (`all_uniq`) 가 분자 보다 빠르게 증가 — multi-backend 합류 시 자연 dilution |
| Phase 2 commit 3 부분 처분 | `academic_hit_count` primary metric + `academic_ratio_per_backend` secondary metric 추가 (compat 영역 보존) |
| §academic-5 영역 | 산식 layer 변경 영역 (all_uniq 정의 영역) |

#### 대안 영역 검토
- (a) **per-backend ratio 의 weighted average** — backend 별 가중치 영역 (vertex 0.3 / legacy 0.2 / SS 0.25 / OA 0.25 등, 영역 정합 영역)
- (b) **all_uniq 정의 영역 — 학술 영역 가능 source 만** — vertex 비학술 blog filter layer 추가 (forbes / medium / zigpoll 등 deny list 영역)
- (c) **academic_hit_count primary metric 전환** — ratio 영역은 secondary, hit_count >= 8 threshold 영역 (Phase 2 commit 3 박제 영역, 검증 영역 §academic-5 이전)
- (d) **per-source-type weighted score** — vertex 학술 source 별 가중치 (논문 1.0 / 블로그 0.0 / 트레이드 publication 0.3 등)

### 2-3. ACADEMIC_DOMAINS set 자동 확장 algorithm (catch 67 future, LOW 영역)

- **현재 영역**: set 40 entries (Phase 2 commit 3 영역 +4), 수동 영역 update
- **자동 분류 패턴**:
  · 대학 도메인: `.edu` / `.ac.*` (영국/한국/일본) / `.uni-*.de` 영역 (독일) / `.university.*` 영역
  · 학술 keyword: `journal.*` / `*.publication.*` / `archive.*` / `proceedings.*` 영역
  · publisher 영역: 정기적 review cycle (measure 결과 unknown 학술 영역 분석)
- **future 영역** (§academic-5 우선순위 ↓, set 자동 영역 효과 영역 정합 영역 후 cycle)

### 2-4. catch 58 multi-query 확장 (single query 영역 진입 후, LOW 영역)

- **직전 영역**: §academic-4 단일 query (`consumer behavior in influencer marketing`) 반복
- **확장 영역**: query expansion (synonyms / related terms / sub-topics)
  · 예: + "social media advertising effects" + "influencer authenticity perception"
- **§academic-5 우선순위 ↓** (vertex 영역 보강 + 산식 영역 변경 영역 우선)

---

## 3. 예상 cycle 면적

| 영역 | 예상 |
|---|---|
| Step A (audit) | ~+150~250 line (audit .md 박제 영역) |
| Step B (design) | ~+300~500 line (design .md 박제 영역) |
| Step C (commit + 측정) | ~+200~400 line (code patch + 박제 영역) |
| 총 영역 | ~+650~1150 line (§academic-4 와 유사 영역) |

4-backend architecture 영역 유지 (catch 51 fix 정합 영역 보존).
vertex 영역 patch + 산식 layer 변경 영역 중심.

---

## 4. 진입 시점 영역

- §academic-4 Phase 4 cycle close 완료 후 별 turn 영역
- entry pre-brainstorm (Claude chat) → Step A audit (Claude Code) → Step B design → Step C commit + 측정 → cycle close

---

## 5. 사용자 결정 영역 (entry 시점)

- (a) 진입 시점 영역 — 즉시 진입 / 별 영역 우선순위 사이 결정
- (b) scope 영역 — 4 핵심 영역 모두 / vertex + 산식 만 / vertex 만 등 결정
- (c) 우선순위 영역 — HIGH (vertex) → MID (산식) → LOW (set 자동 / multi-query) 영역 정합 영역

---

## 6. 박제 chain reference (§academic-4 영역)

- commit 1: `942328f` (3 신규 module + Sub-decision 1A → 1A_prime)
- commit 2 + amend: `e3f3de9` (시스템 통합 + 1B 전환 + dotenv chain + catch 62/64/65)
- commit 3: `ce95ad5` (Step C-1 close + Step C-2 patch + catch 66/67 + set 보강)
- commit 4 (예정): cycle close + 본 sketch 박제 영역

---

*§academic-5 entry sketch 영역 — §academic-5 entry pre-brainstorm 시점 사용자 컨펌 후 Step A audit 진입 영역 정합*

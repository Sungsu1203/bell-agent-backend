# Semantic Scholar — Sub-decision 1A_prime (1A 후속) 정합
# default: SKIP=1 (anonymous pool throttle isolation)
# key 발급 후 (catch 61 진입): API_KEY 주입 + SKIP=0 변경 → x-api-key 헤더 자동 활성
# onboarding: cp .env.semanticscholar.example .env.semanticscholar && edit
SEMANTIC_SCHOLAR_MAILTO=sungsu.oh@bellcomm.co.kr
SEMANTIC_SCHOLAR_SKIP=1
SEMANTIC_SCHOLAR_API_KEY=
